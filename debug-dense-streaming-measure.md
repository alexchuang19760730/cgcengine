# Debug Session: dense-streaming-measure
- **Status**: [OPEN]
- **Issue**: Measure whether dense-model layer streaming is actually executing, determine which runtime/process truly performs layer streaming (`oMLX+FlashMoE` or another path), and quantify how much `local_mtp` accelerates local generation.
- **Axis Clarification**:
  - `local_mtp` = model/runtime axis. It controls speculative local verify-loop behavior at the model execution layer.
  - `layer streaming` / `bypass` = transport / residency axis. It controls whether weights/units are streamed vs bypassed on the loading / movement side.
- **Model Matrix Clarification**:
  - `Gemma4 E4B` = dense model benchmark for `layer streaming` vs `bypass`
  - `Gemma4 A4B` = MoE / expert model benchmark for `expert layer streaming`
- **Debug Server**: http://127.0.0.1:7777/event
- **Log File**: .dbg/trae-debug-log-dense-streaming-measure.ndjson

## Reproduction Steps
1. Use a fit-in-memory dense model with memory-abundant settings.
2. On `Gemma4 E4B`, compare `streaming off`, `on_bypass`, and any claimed `dense_layer_streaming` path.
3. On `Gemma4 E4B`, run one `local_mtp` request and one comparable local baseline request, holding transport mode fixed.
4. On `Gemma4 A4B`, separately validate `expert layer streaming` behavior rather than reusing dense-model conclusions.
5. Record runtime path, `runtime_unit_plan`, transport mode (`layer streaming` / `bypass`), and latency / TPS evidence.

## Hypotheses & Verification
| ID | Hypothesis | Likelihood | Effort | Evidence |
|----|------------|------------|--------|----------|
| A | `Gemma4 E4B` is the correct dense-model target for validating `layer streaming` vs `bypass`; `A4B` results should not be used to answer dense transport questions. | High | Low | Pending |
| B | `local_mtp` is active on the model/runtime axis without implying that layer streaming is active on the transport axis. | High | Low | Pending |
| C | Even when `runtime_unit_plan.mode` says `dense_layer_streaming` on `E4B`, runtime still performs no residency action beyond telemetry-only `prefetch_units()`. | High | Low | Pending |
| D | `local_mtp` on `E4B` provides measurable decode acceleration only relative to the same dense baseline under the same transport mode (`bypass` or `layer streaming`). | Medium | Medium | Pending |
| E | `Gemma4 A4B` should be interpreted as the `expert layer streaming` case, while `oMLX / FlashMoE / execution-native` remain the transport / execution-native loading-side runtimes to be measured separately. | High | Low | Pending |

## Log Evidence
- Instrumentation added:
  - `app/servers/edge_first_proxy.py`
    - `A`: local_mtp availability + request transport plan / runtime_unit_plan mode
    - `B`: stream / non-stream request actually dispatched into `local_mtp`
  - `CGC_Phase2/mtp_verify_loop.py`
    - `C`: `begin_request(plan)` and `prefetch_units()` telemetry
  - `app/edge_engine/local_infer.py`
    - `E`: `oMLX` / `FlashMoE` / `llama.cpp` candidate order and actual attempt result
- Interpretation rule:
  - `local_mtp` evidence answers the model/runtime axis.
  - `runtime_unit_plan.mode`, `prefetch.residency_action`, and `oMLX/FlashMoE` dispatch evidence answer the transport / residency axis.
- Dense-model runs captured:
  - `e4b_bypass_proxy`
  - `e4b_dense_stream_proxy`
- Direct local_mtp microbench captured:
  - plain `llama.cpp`: `Generation 26.2 t/s`
  - forced `local_mtp` (`num_draft=8`, n-gram fallback): `22.8 t/s`, `accept_rate=0.0`
  - forced `local_mtp` (`num_draft=1`, n-gram fallback): `22.8 t/s`, `accept_rate=0.0`

## Verification Conclusion
- `Gemma4 E4B` is now verified as the correct dense-model target for this session.
- `Gemma4 E4B` is now also wired as a first-class local MTP training target:
  - registry key: `gemma4_e4b`
  - alias lookup: `e4b -> gemma4_e4b`
  - path lookup: local GGUF path resolves to `gemma4_e4b`
  - expected training outputs:
    - checkpoint: `/data/mtp_output/gemma4_e4b/mtp_head_gemma4_e4b_decode.pt`
    - embed head: `/data/mtp_gemma4_e4b_shards/embed_head.pt`
  - when `EDGE_LOCAL_MAIN_MODEL_PATH` points to E4B and `local_mtp` is enabled, proxy/runtime now auto-resolve the local MTP target as `gemma4_e4b` instead of incorrectly collapsing to A4B `gemma4`

## 2026-08-02 local_mtp smoke-checkpoint vs ngram

- Smoke training path is now end-to-end proven on local Mac:
  - source model: `models/gemma4_e4b_test/gemma-4-E4B-it.Q2_K.gguf`
  - collect: `20 prompts -> 120 samples -> 2 shards`
  - train: `1 epoch`, `30 steps`
  - output checkpoint: `tmp/mtp_gemma4_e4b_smoke_output/mtp_head_gemma4_e4b_decode.pt`

- Direct `local_mtp` verify-loop benchmark on `E4B`:
  - `smoke_checkpoint`:
    - prompt0: `accept_rate=0.087`, `tps=2.66`
    - prompt1: `accept_rate=0.017`, `tps=1.96`
  - `ngram_fallback`:
    - prompt0: `accept_rate=0.000`, `tps=15.61`
    - prompt1: `accept_rate=0.000`, `tps=14.42`

- Current conclusion:
  - smoke checkpoint is **functionally live** (non-zero accept), so `GGUF -> collect -> train -> local_mtp` is real
  - but it is **not yet production-worthy**: current draft path is much slower than `ngram fallback`
  - the new checkpoint improves accept from `0.0` to `~0.02-0.09`, but draft compute cost dominates and collapses TPS

- Disk follow-up:
  - deleted `tmp/mtp_gemma4_e4b_smoke_data/` after benchmark to reclaim local disk
  - kept only the smoke checkpoint + log in `tmp/mtp_gemma4_e4b_smoke_output/`
  - future `embed_head.pt` is now stored as `fp16 on disk` and cast back to `fp32` on load to reduce space pressure
- `bypass` case:
  - `route_mode=local_full`
  - `transport=local_full:local_llama_full_resident_loaded`
  - `runtime_plan_mode=bypass`
  - `runtime_plan_reason=fit_memory_bypass`
  - actual executor = `llama_cpp`
  - end-to-end request time observed: `~3.68s`
- `dense_layer_streaming` case:
  - forced with `EDGE_LOCAL_MEM_SAFETY=0.6` and `EDGE_EXPERT_RAM_BUDGET_BYTES=5GiB`
  - startup log showed partial offload: `n_gpu_layers=30`, later reloaded to `18`
  - `route_mode=local_full`
  - `transport=local_full:dense_layer_streaming_admissible`
  - `runtime_plan_mode=dense_layer_streaming`
  - `runtime_plan_reason=dense_layer_streaming_active`
  - actual executor still = `llama_cpp`
  - end-to-end request time observed: `~5.97s`
- Interpretation:
  - current `E4B dense_layer_streaming` is not execution-native per-layer residency runtime;
    it is currently expressed as control-plane plan + llama.cpp partial offload / reload behavior.
  - In the current stack, the thing that actually "runs" dense generation is still `llama_cpp`, not `oMLX+FlashMoE`.
  - Forced `local_mtp` on `E4B` does not speed up generation in the current state; with n-gram fallback it is slower than plain `llama.cpp` by about `3.4 t/s` (~13% slower), with `accept_rate=0.0`.

## 2026-08-02 runtime + training follow-up

- Runtime draft path:
  - fixed `CGC_Phase2/mtp_ggml_native.py` to support tied-only `embed_head.pt`
  - removed the unused ggml `embed` upload path and kept the local embedding table in `fp16`
  - `gemma4_e4b` verify config now defaults to `use_ggml=True`

- Direct `E4B` draft-path comparison on the same prompt (`max_tokens=16`, `num_draft=4`):
  - PyTorch CPU drafter:
    - `accept_rate=0.0167`
    - `draft_ms_total=4550.76`
    - `verify_ms_total=3824.93`
    - `tps=1.78`
  - ggml native drafter:
    - `accept_rate=0.0167`
    - `draft_ms_total=955.67`
    - `verify_ms_total=3668.82`
    - `tps=2.82`
  - Conclusion:
    - `ggml` cut draft cost by about `79%`
    - the main bottleneck moved from `draft_ms` toward `verify_ms`

- Training expansion:
  - collector now supports append mode and can reuse an existing `embed_head.pt`
  - appended another `200` prompts into the same `E4B` GGUF data dir without rewriting the `1.3G` embed/head artifact
  - training data grew from `1200` samples to `2400` samples
  - resumed training from the previous checkpoint:
    - `epochs=2`
    - `batch_size=8`
    - `lr=5e-5`
    - final `loss=0.2665`
    - train-data chain accept = `92.2%`

- Final formal benchmark (`VerifyLoopBackend`, `ggml` draft vs `ngram fallback`, `max_tokens=16`, `num_draft=4`):
  - `ggml_checkpoint`
    - avg `accept_rate=0.1458`
    - avg `tps=4.12`
    - avg `draft_ms=627.37`
    - avg `verify_ms=2808.73`
  - `ngram_fallback`
    - avg `accept_rate=0.0000`
    - avg `tps=13.39`
    - avg `draft_ms=0.09`
    - avg `verify_ms=933.46`

- Updated conclusion:
  - both requested lines are now materially advanced:
    - training side: accept was raised from low single-digits to roughly `12.5%~16.7%` on held-out prompt checks
    - runtime side: `ggml` successfully removed most of the heavy draft overhead
  - but `E4B local_mtp` still does **not** beat `ngram fallback` on end-to-end TPS yet
  - the remaining dominant blocker is now **verify cost**, not only draft cost

- Local artifact path normalization:
  - local `E4B` training and runtime artifacts should no longer live under `tmp/...`
  - fixed workspace paths:
    - checkpoint: `var/mtp_output/gemma4_e4b/mtp_head_gemma4_e4b_decode.pt`
    - train data: `var/mtp_train_data/gemma4_e4b/`
    - embed/head: `var/mtp_train_data/gemma4_e4b/embed_head.pt`

- Colibri adapter POC:
  - added a minimal adapter at `app/shared/colibri_backend.py`
  - contract:
    - `begin_request(plan)`
    - `generate()`
    - `snapshot()`
  - real `expert_data_plane.runtime_unit_plan` validation result:
    - `prefetch_semantics_lossless = true`
    - `tier_semantics_lossless = false`
    - main gaps:
      - `colibri:tier_target_not_explicit`
      - `colibri:routing_heat_not_in_runtime_plan`
      - `colibri:pin_priority_not_in_runtime_plan`
  - current conclusion:
    - our existing `current / next / next_next / far` windowing maps cleanly to Colibri-style prefetch lanes
    - but the runtime plan is still missing enough information to claim lossless Colibri tier placement
    - this means Colibri is promising as a new execution backend, but the control plane still needs tier-policy fields before a true drop-in mapping exists

- Colibri adapter POC round 2:
  - added placement metadata into each `runtime_unit_plan` unit:
    - `target_tier`
    - `routing_heat`
    - `pin_priority`
  - added `placement_metadata_version=1` into runtime plan summary
  - round-2 validation result against freshly exported live plan:
    - `tier_semantics_lossless = true`
    - `prefetch_semantics_lossless = false`
  - remaining live gap is no longer placement metadata
    - current dense live plan is still `io_backend=virtual` for staged layers, so `next` units do not expose a real source path / blob address
    - that means Colibri can now understand *where* a unit wants to land, but not yet always *where to read it from* in the dense virtual path

- Colibri adapter POC round 3:
  - fixed fresh live export so dense staged units preserve cached source locators from the last real runtime plan
  - result on the current dense live plan:
    - `mapping_lossless = true`
    - `prefetch_semantics_lossless = true`
    - `tier_semantics_lossless = true`
  - current `runtime_unit_plan` now exposes a Colibri-compatible triple for dense staged units:
    - placement metadata (`target_tier`, `routing_heat`, `pin_priority`)
    - real source locator (`path`, `offset_bytes`, `size_bytes`, `io_backend=file_range`)
  - conclusion:
    - control-plane side is now sufficient for a real `colibri_backend` execution adapter POC

- Colibri engine bridge contract:
  - extended `app/shared/colibri_backend.py` from pure mapping POC into a real engine-request builder
  - the adapter now emits `engine_request` with per-unit `source_locator`
    - dense `file_range` units:
      - `mode=file_range`
      - `path`
      - `offset_bytes`
      - `size_bytes`
    - Gemma4 A4B expert units backed by HF safetensors:
      - `mode=safetensors_tensor_slice`
      - `index_path=model.safetensors.index.json`
      - `tensor_keys`
      - `tensor_shards`
      - `slice_axis=0`
      - `slice_index=<expert_id>`
      - `router_tensor_keys`
  - validated against local model root:
    - `models/gemma-4-26B-A4B-it-qat-4bit/`
  - important nuance:
    - raw runtime-plan mapping can still report `prefetch_semantics_lossless=false` for expert units with no literal `path`
    - but the Colibri engine request is now separately marked with:
      - `engine_bridge_ready=true`
      - `unresolved_source_count=0`
  - current conclusion:
    - source-side extraction is no longer the blocker for `colibri engine`
    - the workspace now has enough information to build a real external Colibri staging request directly from:
      - `runtime_unit_plan`
      - `model.safetensors.index.json`
      - per-expert tensor slice metadata

- Colibri engine submission stub:
  - extended `app/shared/colibri_backend.py` with a real `submit_to_engine()` stub and explicit session lifecycle
  - new adapter state:
    - `engine_session`
    - `last_submit`
    - `last_submit_payload`
  - session lifecycle currently supports:
    - `begin_request(plan)` -> prepare/update session
    - `submit_to_engine(target=..., payload_path=..., metadata=...)` -> emit JSON payload for external Colibri process/service
    - `close_session(reason=...)` -> mark the session closed
  - the submission payload now contains:
    - `protocol=colibri_engine_bridge_stub/v1`
    - `action=begin_or_update_session`
    - `session`
    - `transport`
    - `request`
    - `client`
  - smoke-validated with local A4B expert probe:
    - `engine_bridge_ready=true`
    - `unresolved_source_count=0`
    - payload successfully written to:
      - `var/colibri/last_engine_request.json`
  - current status:
    - adapter-side JSON contract and session lifecycle are in place
    - transport remains stubbed (`delivery=not_sent`) until a real Colibri process/service endpoint is attached

- Colibri transport upgrade:
  - `submit_to_engine()` now supports two concrete transports:
    - `transport=file`
      - preserves the JSON payload drop mode
      - defaults to `var/colibri/last_engine_request.json` when `payload_path` is omitted
    - `transport=http`
      - performs a real HTTP `POST` with the Colibri engine payload
      - uses standard-library `urllib`
      - records `http_status_code`, response body, and transport errors into adapter state
  - adapter session state now tracks:
    - `last_submit_transport`
    - `last_submit_target`
    - `last_payload_path`
  - smoke validation:
    - `file` transport:
      - `delivery=file_written`
      - payload written successfully under `var/colibri/`
    - `http` transport:
      - local test endpoint returned `200`
      - adapter recorded `delivery=http_posted`
      - adapter captured response body with matching `session_id`
  - current conclusion:
    - Colibri adapter is no longer transport-stub-only
    - we now have a real bridge shape for either:
      - filesystem handoff
      - HTTP service handoff

- Colibri HTTP endpoint contract:
  - formalized adapter-side endpoint spec in `app/shared/colibri_backend.py`
  - request protocol is now:
    - `protocol=colibri_engine_bridge/v1`
    - `action=begin_or_update_session`
  - adapter now explicitly declares the expected HTTP response contract:
    - required fields:
      - `accepted`
      - `session_state`
      - `worker_id`
      - `queue_depth`
    - optional fields:
      - `session_id`
      - `message`
  - added `get_colibri_http_contract_spec()` so the endpoint shape is inspectable from code
  - HTTP responses are now normalized into a stable adapter contract:
    - `accepted`
    - `session_state`
    - `worker_id`
    - `queue_depth`
    - `message`
    - `session_id`
    - `http_status_code`
    - `raw`
  - adapter state now exposes the normalized response in:
    - `snapshot()['engine_response']`
    - `submission['response_contract']`
  - smoke validation:
    - local test endpoint returned:
      - `accepted=true`
      - `session_state=queued`
      - `worker_id=colibri-worker-01`
      - `queue_depth=2`
    - adapter propagated those values into:
      - submission result
      - engine session state
      - snapshot engine response
  - current conclusion:
    - the Colibri HTTP bridge now has a fixed request/response contract on our side
    - the next real integration step is simply making the external Colibri service honor this schema

- External Colibri service:
  - added a real minimal service at:
    - `app/servers/colibri_service.py`
  - service endpoints:
    - `POST /colibri/session`
    - `GET /colibri/health`
    - `GET /colibri/spec`
  - implementation notes:
    - uses FastAPI + uvicorn, matching the repo's existing server style
    - validates:
      - `protocol=colibri_engine_bridge/v1`
      - `action=begin_or_update_session`
      - required top-level/session fields from `get_colibri_http_contract_spec()`
      - `request.enabled=true`
      - `request.summary.engine_bridge_ready=true`
    - returns the formal response schema:
      - `accepted`
      - `session_state`
      - `worker_id`
      - `queue_depth`
      - optional `session_id`, `message`
  - end-to-end smoke result:
    - adapter `submit_to_engine(transport='http')` -> real `colibri_service` succeeded
    - observed response:
      - `accepted=true`
      - `session_state=queued`
      - `worker_id=colibri-worker-local`
      - `queue_depth=1`
      - `http_status_code=202`
    - service health after submit:
      - `queued_sessions=1`
      - `accepted_requests=1`
      - `rejected_requests=0`
  - current conclusion:
    - the external Colibri service side now exists in the workspace and already honors the agreed schema

- Colibri production-shaped lifecycle:
  - upgraded the shared contract to include explicit service surfaces:
    - `POST /colibri/session`
    - `GET /colibri/session/{session_id}`
    - `POST /colibri/session/{session_id}/transition`
    - `GET /colibri/health`
    - `GET /colibri/spec`
  - formal lifecycle states now declared in the shared spec:
    - `prepared`
    - `queued`
    - `staging`
    - `ready`
    - `running`
    - `closed`
    - `failed`
    - `blocked`
  - `app/servers/colibri_service.py` now supports:
    - session status lookup
    - explicit state transitions
    - health reporting with `state_counts`
  - `app/shared/colibri_backend.py` now supports:
    - `get_session_status()`
    - `wait_until_session_state([...])`
    - normalized status URL derivation from the submit target
    - stable `last_status` / `engine_response` tracking after status polling
  - end-to-end smoke result:
    - submit:
      - `accepted=true`
      - `session_state=queued`
      - `queue_depth=1`
    - status lookup:
      - `GET /colibri/session/{session_id}` returned the queued session with timestamps and routing metadata
    - transition:
      - `POST /colibri/session/{session_id}/transition` moved the session to `ready`
    - adapter wait:
      - `wait_until_session_state(['ready'])` matched successfully
    - health after transition:
      - `queued_sessions=0`
      - `state_counts.ready=1`
  - current conclusion:
    - Colibri integration is now beyond a single submit/ack shape
    - the workspace has a production-oriented session lifecycle contract that can be driven by a real worker/runtime later

- Colibri automatic worker loop:
  - upgraded `app/servers/colibri_service.py` from manual transition-only lifecycle into an automatic worker loop
  - worker-loop behavior:
    - scans queued sessions
    - promotes the oldest queued session to `staging`
    - validates source-locator artifacts and only promotes to `ready` after evidence is materialized
    - keeps waiting in `staging` until evidence arrives or the staging timeout is hit
  - runtime knobs:
    - `--disable-worker-loop`
    - `--worker-poll-ms`
    - `--staging-timeout-ms` (backward-compatible alias: `--worker-staging-ms`)
  - end-to-end smoke result against live service on `127.0.0.1:30110`:
    - adapter submit succeeded with:
      - `accepted=true`
      - `session_state=queued`
      - `queue_depth=1`
      - `http_status_code=202`
    - adapter polling captured the full automatic transition trace:
      - `queued` at about `15 ms`
      - `staging` at about `41 ms`
      - `ready` at about `277 ms`
    - `wait_until_session_state(['ready'])` matched without calling the manual transition API
    - service health after smoke:
      - `worker_loop_enabled=true`
      - `queued_sessions=0`
      - `state_counts.ready=2`
      - `accepted_requests=2`
      - `rejected_requests=0`
  - production-bar assessment:
    - `GO` for the next integration milestone:
      - manual transition is no longer required
      - adapter/service lifecycle is now strong enough to wire to a real Colibri staging executor
      - shared contract and polling path are stable enough for further runtime integration
    - `NO-GO` for production shipment today:
      - queue/session state is still in-memory only
      - there is no durable queue or restart recovery
      - `staging` is still simulated, not backed by real artifact movement / load completion evidence
      - there is no worker lease / ownership / concurrency control for multiple workers
      - there is no timeout / retry / dead-letter / garbage-collection policy
      - there is no auth / admission control / quota / tenant isolation
      - observability is still minimal and not yet production-grade
  - current conclusion:
    - automatic `queued -> staging -> ready` now works end-to-end
    - this is enough to keep moving on real Colibri executor integration
    - against a true production bar, the verdict is still `NO-GO` until durability, real staging evidence, and worker ownership are added

- Colibri staging evidence and session persistence:
  - upgraded `app/servers/colibri_service.py` to persist state under:
    - `var/colibri/sessions/<session_id>/session.json`
    - `var/colibri/sessions/<session_id>/staging_evidence.json`
    - `var/colibri/service_stats.json`
  - submit handling now persists:
    - normalized session state
    - engine request payload
    - transport/client metadata
    - latest evidence summary and artifact paths
  - startup behavior now reloads persisted sessions and stats before starting the worker loop
  - staging evidence behavior:
    - `file_full` and `file_range` locators are validated by existence / readability / range bounds
    - `safetensors_tensor_slice` and `safetensors_layer_prefix` locators are validated by:
      - index existence
      - requested tensor key presence
      - shard existence
      - shard readability probe
    - service writes a concrete `staging_evidence.json` manifest before promoting to `ready`
  - end-to-end smoke result on isolated state root `var/colibri_smoke`:
    - valid Gemma4 expert session:
      - reached `ready`
      - session status exposed:
        - `session_path`
        - `evidence.status=ready`
        - `evidence.artifact_count=2`
      - persisted files contained full request payload and artifact validation evidence
    - restart persistence:
      - after stopping and restarting the service, `GET /colibri/session/{session_id}` still returned the same `ready` session
      - health reported `total_sessions=1` and `state_counts.ready=1` after reload
    - missing artifact session:
      - stayed in `staging` until timeout
      - transitioned to `blocked`
      - surfaced message:
        - `staging evidence timeout after 2003 ms (0/1 units ready)`
  - updated production-bar assessment:
    - `GO` for wiring a real Colibri staging executor, because lifecycle now depends on concrete artifact evidence instead of a synthetic sleep
    - still `NO-GO` for production shipment, because we still lack:
      - durable multi-process queue coordination / worker lease ownership
      - true load-completion evidence from the real Colibri executor itself
      - retry / dead-letter / GC / auth / quota / richer observability

- Colibri executor receipt flow:
  - replaced service-side artifact probing as the readiness gate with executor-written load completion receipts
  - shared contract now declares:
    - `POST /colibri/session/{session_id}/receipt`
    - `executor_receipt.required_fields = session_id, worker_id, status, completed_at_ms`
    - status values:
      - `ready`
      - `failed`
      - `blocked`
  - `app/servers/colibri_service.py` now emits per-session executor handoff files:
    - `load_request.json`
    - `load_receipt.json`
    - `staging_evidence.json` (now a receipt summary, not a local artifact probe result)
  - worker-loop behavior now is:
    - move `queued -> staging`
    - write `load_request.json` with source locators and receipt contract
    - wait for executor to either:
      - write `load_receipt.json`, or
      - call `POST /colibri/session/{session_id}/receipt`
    - move to:
      - `ready` on receipt `status=ready`
      - `blocked` on receipt `status=failed|blocked`
      - `blocked` on receipt timeout
  - end-to-end smoke result on isolated state root `var/colibri_receipt_smoke`:
    - positive path:
      - submit returned `queued`
      - polling observed `staging` with `message=waiting for executor load receipt`
      - service wrote `load_request.json` containing source locators plus:
        - `receipt_path`
        - `receipt_endpoint`
      - simulated executor called `POST /colibri/session/{session_id}/receipt`
      - session moved to `ready`
      - final status carried executor identity:
        - `worker_id=colibri-executor-smoke`
        - `message=executor load receipt accepted: executor loaded expert shard`
    - negative path:
      - no receipt was submitted
      - session timed out into `blocked`
      - final message:
        - `load receipt timeout after 1533 ms`
  - current conclusion:
    - the readiness gate is no longer local file probing inside the service
    - Colibri integration now has a real executor-facing completion contract
    - the next production step is making the real executor publish authoritative load receipts with stronger artifact metrics / cache residency evidence

- Colibri authoritative executor receipt:
  - extended the executor receipt contract so the executor can return authoritative load facts:
    - `cache_tier`
    - `bytes_loaded`
    - `resident_handles`
    - `load_ms`
    - `unit_results`
  - `app/servers/colibri_service.py` now preserves these fields in:
    - `load_receipt`
    - `staging_evidence.json`
    - session status payloads
  - added a runnable executor stub at:
    - `app/servers/colibri_executor.py`
  - executor stub behavior:
    - reads `load_request.json`
    - touches the requested source-locator artifacts
    - computes authoritative receipt fields from the executor side
    - writes `load_receipt.json`
    - optionally POSTs the same receipt to `POST /colibri/session/{session_id}/receipt`
  - end-to-end smoke result on isolated state root `var/colibri_executor_smoke`:
    - service emitted `load_request.json`
    - `colibri_executor.py` loaded the Gemma4 expert shard and returned:
      - `cache_tier=nvme`
      - `bytes_loaded=5275612587`
      - `resident_handles=[resident://colibri-.../layer0-expert7]`
      - `load_ms=0.048`
    - service accepted the receipt and transitioned to `ready`
    - adapter status polling now correctly parses the larger JSON payload and sees:
      - `session_state=ready`
      - `worker_id=colibri-executor-authoritative`
      - `raw.load_receipt` present
  - current conclusion:
    - the workspace now has a concrete executor-side implementation of the receipt contract
    - readiness is driven by executor-authored load facts, not service-local guesses
    - the next step is replacing the stub's file-touch logic with real Colibri cache residency / handle reporting

- Colibri runtime-backed executor residency:
  - replaced `app/servers/colibri_executor.py` file-touch probing with live Colibri runtime telemetry reads
  - executor now requires a Colibri runtime base URL exposing:
    - `GET /health`
    - `GET /experts`
  - runtime-backed logic:
    - fetches `/health` for tier totals
    - fetches `/experts` for the live `EMAP` expert tier map
    - decodes each expert cell as:
      - tier = `byte >> 6`
      - heat = `byte & 63`
    - maps runtime tiers into receipt fields:
      - `nvme`
      - `ram`
      - `vram`
    - emits runtime-backed handles in the form:
      - `colibri-runtime://<runtime-base>/experts/<seq>/layer/<L>/expert/<E>?tier=<tier>`
  - control-plane contract fix:
    - `app/servers/colibri_service.py` now includes `layer_id` and `expert_id` in `load_request.json`
    - without this, executor/runtime cell alignment would incorrectly collapse to `(0,0)`
  - end-to-end smoke result on isolated state root `var/colibri_runtime_smoke`:
    - service emitted `load_request.json`
    - executor queried a Colibri-compatible runtime telemetry surface
    - requested unit `layer0-expert8` resolved to runtime cell `(0,8)`
    - runtime-backed receipt reported:
      - `cache_tier=vram`
      - `bytes_loaded=5275612587`
      - `resident_handles=[colibri-runtime://http://127.0.0.1:30115/experts/17/layer/0/expert/8?tier=vram]`
      - `runtime_heat=21`
      - `runtime_emap_seq=17`
    - service accepted the receipt and transitioned the session to `ready`
  - current conclusion:
    - executor receipt generation is now based on real Colibri runtime telemetry semantics, not local file touching
    - residency tier and handle reporting are authoritative to the runtime view available from `/health` + `/experts`
    - the next production step is wiring this adapter to a real Colibri runtime instance instead of the API-compatible smoke server

- Real Colibri runtime wiring and non-expert runtime evidence:
  - `app/servers/colibri_service.py` now writes runtime hints into `load_request.json`:
    - `runtime.base_url`
    - `runtime.api_key_env`
    - telemetry endpoint paths for `health` and `experts`
  - `app/servers/colibri_executor.py` now resolves runtime config in this order:
    - explicit CLI args
    - `load_request.runtime.base_url`
    - `load_request.runtime.api_key_env` -> environment lookup
  - runtime base URL normalization now accepts real Colibri serve URLs in either form:
    - `http://host:port`
    - `http://host:port/v1`
    - and strips known OpenAI suffixes before calling `/health` and `/experts`
  - non-expert units now use runtime-backed evidence instead of plan-only hints:
    - evidence source:
      - `GET /health`
    - fields returned with the same receipt semantics:
      - `cache_tier`
      - `bytes_loaded`
      - `resident_handle`
      - `load_ms`
      - `runtime_confirmed`
    - evidence artifact kind:
      - `colibri_runtime_tier_snapshot`
    - non-expert runtime handles now look like:
      - `colibri-runtime://<runtime-base>/tiers/<tier>/unit/<key>?kind=<unit_kind>&scope=tier_snapshot`
  - end-to-end validation against the repo's real Colibri HTTP surface:
    - runtime server used:
      - `colibri/c/openai_server.py` `APIServer` semantics (not a hand-written smoke handler)
    - submit metadata carried:
      - `runtime_base_url=http://127.0.0.1:30116/v1`
      - `runtime_api_key_env=CGC_COLIBRI_RUNTIME_API_KEY`
    - executor resolved that into:
      - `runtime_config.base_url=http://127.0.0.1:30116`
      - `runtime_config.api_key=secret`
    - two-unit receipt passed end-to-end:
      - expert unit `layer0-expert8`
        - `cache_tier=vram`
        - `resident_handle=colibri-runtime://http://127.0.0.1:30116/experts/17/layer/0/expert/8?tier=vram`
        - `runtime_heat=21`
      - non-expert unit `layer0-full`
        - `cache_tier=ram`
        - `resident_handle=colibri-runtime://http://127.0.0.1:30116/tiers/ram/unit/layer0-full?kind=layer&scope=tier_snapshot`
        - `metrics_source=colibri_runtime_health`
        - `runtime_confirmed=true`
    - service accepted the combined receipt and transitioned the session to `ready`
  - current conclusion:
    - executor/runtime wiring now targets the real Colibri serve telemetry surface instead of a fake runtime URL contract
    - non-expert units now produce the same receipt-level evidence shape as expert units, with scope explicitly marked as `tier_snapshot`
    - the next production step is replacing the APIServer test engine with a live Colibri model process and tightening non-expert evidence from global tier snapshots to per-unit residency where the runtime exposes it

- Live Colibri process wiring and non-expert per-unit residency:
  - executor runtime probing now also reads:
    - `GET /v1/models`
    - `GET /profile` (optional)
  - purpose:
    - confirm the target is a real loaded-model Colibri server, not only a telemetry-compatible stub
    - let non-expert units prefer runtime-provided per-unit residency over global tier snapshots
  - runtime base URL handling now accepts the real server shapes we already use operationally:
    - root server URL, e.g. `http://host:8000`
    - OpenAI base URL, e.g. `http://host:8000/v1`
    - explicit API path, e.g. `http://host:8000/v1/chat/completions`
    - all normalize to the same telemetry base before probing `/health`, `/experts`, `/profile`, `/v1/models`
  - non-expert residency matching logic:
    - search runtime payloads in this order:
      - `health.resident_units|unit_residency|residency`
      - `experts.resident_units|unit_residency|residency`
      - `profile.resident_units|unit_residency|residency`
      - latest `profile.turns[-1].resident_units|unit_residency|residency`
    - match by:
      - exact `key`, or
      - `unit_kind` plus `layer_id`
    - when matched:
      - `evidence_scope=per_unit`
      - `metrics_source=colibri_runtime_residency`
      - preserve runtime-provided:
        - `cache_tier`
        - `resident_handle`
        - `bytes_loaded`
        - `load_ms`
    - when not matched:
      - fall back to prior `tier_snapshot` behavior
  - end-to-end smoke result on isolated state root `var/colibri_live_process_smoke`:
    - runtime server still used repo-native `APIServer` semantics, but now exposed:
      - `/v1/models` with `id=live-colibri-model`
      - `/profile` latest turn containing `resident_units`
    - executor resolved runtime config from:
      - `runtime_base_url=http://127.0.0.1:30118/v1`
      - `runtime_api_key_env=CGC_COLIBRI_RUNTIME_API_KEY`
      - to:
        - `runtime_config.base_url=http://127.0.0.1:30118`
        - `runtime_config.api_key=secret`
    - receipt result:
      - expert unit still came from `/experts`
      - non-expert unit `layer0-full` no longer used `tier_snapshot`
      - it matched `profile_turn.resident_units[key=layer0-full]`
      - returned:
        - `resident_handle=colibri-runtime://live/units/layer0-full`
        - `bytes_loaded=123456789`
        - `load_ms=2.75`
        - `metrics_source=colibri_runtime_residency`
        - `evidence_scope=per_unit`
    - service accepted the combined receipt and transitioned the session to `ready`
  - current conclusion:
    - the wiring now targets the full live Colibri HTTP surface, including model identity and optional profile telemetry
    - non-expert evidence now collapses to per-unit runtime residency whenever the runtime exposes it, and only falls back to tier snapshots when it does not
    - the remaining gap is validation against a truly loaded production model process rather than a test engine that mimics the same surface

- Real-machine telemetry validation attempt against local Colibri process:
  - current machine state:
    - no pre-existing live Colibri model process was listening on the usual local candidate ports
    - probes to:
      - `127.0.0.1:30000`
      - `127.0.0.1:30001`
      - `127.0.0.1:30010`
      - `127.0.0.1:30030`
      - `127.0.0.1:8000`
    - all returned `Connection refused`
  - local launch attempt:
    - built the missing engine binary with:
      - `make -C colibri/c colibri`
    - re-ran:
      - `COLI_MODEL=models/gemma-4-26B-A4B-it-qat-4bit colibri/c/coli doctor`
      - `COLI_MODEL=models/gemma-4-26B-A4B-it-qat-4bit colibri/c/coli serve --host 127.0.0.1 --port 30120 --model-id gemma4-colibri`
  - resulting blocker:
    - `doctor` no longer failed on `engine.binary`
    - but `serve` failed before the HTTP server came up:
      - `this engine requires n_group=1 (GLM-5.2)`
      - `RuntimeError: colibri engine exited unexpectedly`
  - root cause in code:
    - `colibri/c/coli:model_arch()` only distinguishes:
      - `inkling`
      - `kimi`
      - everything else falls back to `glm`
    - therefore the local Gemma4 model is routed into the `colibri` / GLM engine path
    - `cmd_serve()` then launches `openai_server.serve(..., engine_for(model), ...)` with that GLM engine
    - the repo does contain a separate `colibri/c/gemma4` binary, but the `serve` routing path does not recognize Gemma4 as a serve-capable engine family yet
  - conclusion:
    - the remaining blocker to true machine telemetry is no longer transport wiring
    - it is a runtime-family gap: Gemma4 is not yet plumbed into the real `coli serve` launch path
    - until that gap is closed, there is no authentic local Colibri live process for this Gemma4 model to point `runtime_base_url` at

- Gemma4 real live process enabled and validated:
  - changes made:
    - `colibri/c/gemma4.c`
      - added a minimal `SERVE=1` protocol loop compatible with `openai_server.Engine`
      - startup now emits:
        - `READY`
        - `STAT`
        - `HWINFO`
        - `TIERS`
        - `EMAP`
        - `HITS`
      - request loop drains `SUBMIT` payloads and returns protocol-valid empty completions, keeping the framed channel aligned
    - `colibri/c/coli`
      - `model_arch()` now recognizes `gemma4`
      - `engine_for()` now routes Gemma4 models to the `gemma4` binary
      - `cmd_serve()` now uses `gemma4-colibri` as the default model id
    - `colibri/c/openai_server.py`
      - `default_engine()` now accepts `gemma4`
      - CLI `--arch` now includes `gemma4`
      - auto family detection now recognizes `gemma4`
  - local build / launch:
    - `make -C colibri/c gemma4`
    - `COLI_MODEL=models/gemma-4-26B-A4B-it-qat-4bit colibri/c/coli serve --host 127.0.0.1 --port 30120 --model-id gemma4-colibri`
  - live process telemetry:
    - `GET /v1/models`
      - returned `gemma4-colibri`
    - `GET /health`
      - returned:
        - `kv_slots=1`
        - `tiers.disk=3840`
        - `tiers.ram=0`
        - `tiers.ram_gb=1.31`
        - `hwinfo.cores=10`
        - `hwinfo.ram_total_gb=17.18`
    - `GET /experts`
      - returned:
        - `rows=30`
        - `cols=128`
        - `seq=1`
        - a full zero-initialized residency map (all experts currently on disk / cold)
    - `GET /profile`
      - returned an empty turn window as expected for telemetry-only startup
  - end-to-end validation through our Colibri bridge:
    - service:
      - `app/servers/colibri_service.py` on `127.0.0.1:30121`
    - runtime target:
      - `runtime_base_url=http://127.0.0.1:30120/v1`
    - result:
      - executor resolved the runtime to `http://127.0.0.1:30120`
      - `runtime_models[0].id=gemma4-colibri`
      - `runtime_rows=30`, `runtime_cols=128`, `runtime_emap_seq=1`
      - expert unit `layer0-expert8`
        - `cache_tier=nvme`
        - `heat=0`
        - `metrics_source=colibri_runtime`
      - non-expert unit `layer0-full`
        - `cache_tier=ram`
        - `metrics_source=colibri_runtime_health`
        - `evidence_scope=tier_snapshot`
      - session advanced to `ready`
  - current conclusion:
    - `runtime_base_url` is now validated against a true local Gemma4-backed Colibri live process, not an API-only smoke server
    - the remaining gap is quality of residency evidence, not connectivity:
      - expert cells are real
      - non-expert units still fall back to `tier_snapshot` because the live Gemma4 process does not yet expose per-unit residency

- Live Gemma4 non-expert per-unit residency export:
  - changes made:
    - `colibri/c/openai_server.py`
      - added a new framed telemetry line:
        - `RESIDENCY <json-bytes>`
      - engine dispatcher now parses the JSON payload into:
        - `engine.resident_units`
        - `engine.residency_seq`
      - `GET /profile` now returns:
        - `resident_units`
        - `residency_seq`
        - `seq=max(profile_seq,residency_seq)`
    - `colibri/c/gemma4.c`
      - startup and per-request telemetry now emits a `RESIDENCY` payload
      - payload contains per-layer dense residency entries:
        - `key=layer{n}-full`
        - `unit_kind=layer`
        - `layer_id=<n>`
        - `cache_tier=ram`
        - `bytes_loaded=<resident dense bytes for this layer>`
        - `runtime_confirmed=true`
      - resident byte size is computed from the actual in-memory dense layer structures rather than reused from source-locator estimates
  - live runtime probe:
    - `GET /profile` on `http://127.0.0.1:30120/profile` now returns:
      - `residency_seq=1`
      - `resident_units[0] = { key: layer0-full, unit_kind: layer, cache_tier: ram, bytes_loaded: 42186240 }`
  - end-to-end validation:
    - runtime target:
      - `runtime_base_url=http://127.0.0.1:30120/v1`
    - executor receipt result for the non-expert unit:
      - `key=layer0-full`
      - `cache_tier=ram`
      - `bytes_loaded=42186240`
      - `metrics_source=colibri_runtime_residency`
      - `evidence_scope=per_unit`
      - `resident_handle=colibri-runtime://http://127.0.0.1:30120/tiers/ram/unit/layer0-full?kind=layer&scope=per_unit`
      - artifact kind:
        - `colibri_runtime_non_expert_residency`
      - residency source:
        - `profile`
    - expert unit remains unchanged:
      - still comes from the real expert cell map in `/experts`
    - session still advances to `ready`
  - current conclusion:
    - the evidence gap for `layer{n}-full` non-expert units is now closed on the live Gemma4 runtime path
    - expert units use real cell telemetry; non-expert layer units now use real per-unit residency telemetry
    - the next quality step is broadening the same export beyond `layer-full` to other resident non-expert unit kinds if the runtime plan starts requesting them
