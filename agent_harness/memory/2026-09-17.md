# 2026-09-17 — flashkv-devserver（引擎層線）

> **這是快照，不是權威副本。**
> 權威位置：`.workbuddy/memory/2026-09-17.md`（由 host 持續寫入）。
> 本檔於 2026-09-17 由 `agent_harness/scripts/import_harness_snapshot.py` 複製進 repo，唯一目的是讓 `agent_harness/`
> 底下的內容能被 `agent_harness/scripts/auto_git_push.ps1` 定時推送；原檔改了這裡**不會**自動跟上。
> 索引與漂移檢查見 `agent_harness/engine_loop/memory/INDEX.jsonl`。

本輪（跨午夜）的逐日記錄仍在 **`2026-09-16.md`**（append-only、`§EN-` 前綴、§EN-15 是最新一節），
因為這一條線的 session 從 09-16 起跑並延續過來，拆檔會讓同一條線的證據散在兩處。

## 本日新增（09-17 00:00–01:30，全部記在 2026-09-16.md 的 §EN-15）

- **`slot→expert` 反查量測完成 ⇒ 「同一個 slot 裝了不同專家」被否證**：`wrong = 0` 且 `owner == ids`
  在 440/440 格、每個臂都成立。
- 新的乾淨對照：`錨 vs p25-keepleaf` 兩個讀數都 **440/440 SAME** ⇒ 光是建出 S1 節點不改變池。
- 真正的讀數：**路由不同，且從第一個 1-token 步的 layer 0 起就不同** ⇒ 池是後果、不是載體。
- 自曝設計缺陷：`SLOT-SEL` 在池自洽時**在數學上等於 ids 的摘要**（已用 `idsum/idsxor/idwsum` 證實）。

新檔：`Backup/compare_slot_owner.py`；新臂 `p25-gputime-churn`／`p25-keepleaf-churn`。
引擎改動只在 `src/llama.cpp/src/llama-context.cpp`（churn 區塊＋`expert_cache_on_topk`，只印不進 gate）。

## 01:40–02:45 第二輪（§EN-16，記在 2026-09-16.md）

- 觀測點移到「第一次 2-token 池路徑 pass」的 layer 0：**layer 0 全鏈 SAME、ids 120/120 SAME、
  池佈局與被消費集合 40/40 SAME**，唯一不同的是 `l_out-1`。
- **★ 因果測試（`CGC_S1_MIN_IL=2`，零改碼）**：第一個分歧節點**跟著閘走**（MIN_IL=1 → `l_out-1`；
  MIN_IL=2 → `ffn_moe_out-2`，且 `l_out-1` 變 SAME）⇒ 「某層壞了」與「數值傳染」都作廢。
- 新工具：`analyze_capture_nodes.py` 的 `--select-t N`／`--list-stages`；引擎側兩個讀數的閘放寬到
  `n_tokens <= 2`。
- 報告：`docs/S1_MINIL_TRACKING_20260917.html`。

## 02:47–03:05 第三輪（§EN-17，記在 2026-09-16.md）

- `src[2]` 那一格做了：**主機側儀器（`CGC_MMID_MV_DBG`）在 S1 臂上是瞎的** —— 編碼時刻
  `op->src[2]->data` 還沒被填（`id_oob_vs_ne02=16/16`、值是 float 位元模式、指紋印不出）。
  錨臂則正常（`id_oob=0`、指紋完整）。⇒「同 ids 讀到不同位元組」**仍未測**。
- 新事實：同一個 ids 運算元，兩臂的填充時序不同（數值無害，裝置讀到的一致）。
- 修掉自己造的比較器短路缺陷（指紋要按 row id 獨立比，否則「0 差異」是空洞的）。
- 新檔：`Backup/compare_mmid_fp.py`、`Backup/run_spac_cold_ab.sh`；`run_ids_dst_capture.sh` 加
  `MMID=1|2|3` 穿透（拒收 0）。
- **已排程**：一次性自動化 `automation-1789584808814` @ 2026-09-17 04:00 跑 COLD 交錯 A/B
  （off/on/off/on，四臂各需 1800 s 靜置，約 2h08m）。報告：`docs/S1_SRC2_INSTRUMENT_BLIND_20260917.html`。

## 02:55–03:2x 第四輪：`MEMORY.md` 依主題拆檔（維護，無引擎改動）

**動機**：`MEMORY.md` 漲到 **19,240 B / 221 行**，session 開始注入時**被截斷**（尾巴被丟掉）⇒ 長期記憶
的尾段等於不存在。按主題拆成 **1 索引 ＋ 3 主題檔**：

| 檔 | 大小 | 內容 |
|---|---|---|
| `MEMORY.md` | **5,612 B**（原 19,240） | 主題導覽表、分工、入口（含索引重生順序）、量測衛生、skill 清單、一段**明確標日期的狀態摘要** |
| `MEMORY_PERF.md` | 8,312 B | profile／模型／幾何、decode 速度四定義表、prefill 250 條件式交付、指紋／戳記 |
| `MEMORY_S1.md` | 6,296 B | S1 分歧定位（儀器、現行結論、已否證機制、§EN-16／§EN-17、下一步） |
| `MEMORY_FACTS.md` | 5,313 B | 長期事實／陷阱、`agent_harness/` 介面、**記憶機制** |

**先驗工具鏈再動手**（避免拆完才發現 schema 只吃單檔）：三支 indexer 全部用 **glob** 而不是白名單 ——
`build_memory_index.py:66` 的 `*.md`、`index_assets.py:352` 的 `.workbuddy/memory/*.md`、
`import_harness_snapshot.py:72`（其 docstring 甚至明寫「任何丟進去的 .md 也會被匯入」）⇒ **拆分是工具
預期支援的**。另外查過：**沒有任何腳本或 skill 引用「MEMORY.md 的 S1 節」這種節名**（只有泛指的 path）。

**內容完整性**：跨 4 檔查 16 個關鍵字串（`2d4e5099`／`clamped_selected`／`id_oob_vs_ne02`／
`SIGBUS`／`ref_iq3_pool8gb_M2_6144_bitident_v5_spac` …）全部命中，無遺漏。四檔合計 25,581 B（比原檔多
6.3 KB＝新增的導覽表、狀態摘要與三份 banner；**只有 `MEMORY.md` 會被注入**，所以變大不影響）。

**索引已重生**（順序：`build_memory_index.py` → `index_assets.py`）：`7 file(s), 83 section(s)`、
`80 assets`；兩條 `--check` 都綠（`no drift` / `existence + bytes + mtime all agree`）。新檔確實進了
`INDEX.jsonl` 與 `MANIFEST.jsonl`（role＝`log`，因為 `.workbuddy/memory/` 在 `VOLATILE_PREFIXES`）。

**★ 順手量到一個重要的既有事實**：**整個 `.workbuddy/` 在 `.gitignore:41` 被忽略，`git ls-files
.workbuddy` 是空的** ⇒ 記憶本體**從來不在版控內**（`git status` 看不到它、`git add` 也不收）⇒ 要跨機器
或進 `auto_git_push.ps1` 推送線，唯一的路是 `agent_harness/memory/` 的實體快照。當時快照對應的是
**09-16 19:34** 的 `MEMORY.md`（21,248 B）且沒有 `2026-09-17.md` ⇒ 已落後。
**處置＝刻意不跑** `import_harness_snapshot.py`：它會把**當日日誌全文**（+88 KB）與 4 個 skill 一起
寫進 `agent_harness/`，那是 D6 明訂的「手動」政策決定，而且 `agent_harness/` 歸另一條線。**⇒ 這是
留給那一條線（或使用者）的一個動作，不是自動的**；義務已寫進 `MEMORY_FACTS.md` 的「記憶機制」段。

**skill 同步**（兩個 `agent_created: true`）：`cgc-decode-attribution` 的「現況結論」段開頭加指向
`MEMORY_S1.md` 的註（本 skill 的 S1 段是摘要，不一致時以記憶檔為準）；`cgc-commit-gate` 的 E2 欠帳段
補上「09-17 起是 1 索引＋3 主題檔，三支 indexer 都用 glob ⇒ 新增／刪除任何 `.md` 都要重生索引」。

**改動邊界**：`.workbuddy/memory/` 4 檔（新增 3、重寫 1）＋ `agent_harness/engine_loop/` 兩個**衍生**
索引檔（`memory/INDEX.jsonl`、`MANIFEST.jsonl`）＋ 2 個 user-level skill。**引擎源碼與 `src/` 一行未動，
未 commit。**（`agent_harness/` 底下只有那兩個衍生索引被動到，`git status` 已核對。）

## 03:0x 查證：「skill/memory 定期上傳 GitHub」現在有沒有在發生 ⇒ **沒有**

使用者問「skill.md memory.md 定期上傳到 github 這個目前有發生嗎」。實查（全部唯讀）：

| 檢查 | 結果 |
|---|---|
| `pwsh` / `powershell` | **都沒有** ⇒ 那支 `.ps1` 在這台機器**不可能執行** |
| crontab ＋ `~/Library/LaunchAgents` ＋ `launchctl list` | **無任何相關條目** |
| `agent_harness/scripts/logs/`（腳本第一次跑就會建） | **不存在** ⇒ **從未在本機執行過** |
| 腳本硬編碼 | `$RepoDir = "D:\alex\flashkv0516\cgcengine_full"`、remote `cgc0907`、branch `fusionroutemot` ⇒ **另一台 Windows 機**的工作 |
| GitHub 實況（`git ls-remote`） | `fusionroutemot` = `1f3b0a78d "auto(agent_harness): periodic backup 2026-09-16 14:04"` —— **正是該腳本的訊息格式 ⇒ 它至少成功跑過一次**，之後沒有 |
| 我們那三個快照 commit | `812a3e77a`／`649f70e47`／`4233efe42` **都不在 `fusionroutemot` 上** |
| 本線所在分支 | `demo/sweet-spot-windows-fix`（remote 實況 = `8140f06c6` = 本地 HEAD）⇒ **與 auto-push 的目標分支不同一條** |

**⇒ 結論三句**：①機制**曾經**運作（09-16 14:04 那一筆），②**現在沒有**（本機不能跑、遠端 13 小時未動），
③**而且要推也沒東西可推**——`MEMORY.md` 的快照對應的是 09-16 19:34 的原檔、`2026-09-17.md` 從未快照，
**而且 09-16 20:30 之後的全部工作（S1 r1–r26 ＋ 本次拆檔）在本地都還沒 commit**。

**順帶排除一個誤判**：`git remote -v` 裡的 `local-dev` → `/Users/alexchuang/Documents/flashkv0516`
**不是**本專案的另一份，那是**訓練／SFT 專案**（`datasets/`、`finetune/`、`sft_data/`、`harness/`，
branch `dev` at `640e56aa8` 09-09），它底下**根本沒有 `memory/` 或 `skills/` 快照**。
Windows 的 `D:\alex\flashkv0516\cgcengine_full` 是 `cgcengine_full` 子目錄，與它不同。
（查這條時踩到一個 shell 陷阱：**`cd` 寫在 `if` 區塊內會外洩到後續指令**，害我一度在錯的 repo 裡
`ls`；改用絕對路徑重查才對。）

**沒有動手改任何東西，也沒有建立 launchd job**：要不要把這條鏈搬到 mac、以哪條分支為準，是決策不是維護。
