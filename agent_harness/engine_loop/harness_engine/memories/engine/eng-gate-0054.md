[engine] **一個從來沒有在真陽性下被驗過的閘門，與一個乾淨的窗長得一模一樣；而在這個 repo 裡，那個閘門已經有人寫好了 —— 手寫替代品是純損失。** 「有沒有別人在量測」是這一輪所有動作的前置。repo 的權威實作是 `agent_harness/engine_loop/runners/preflight.sh`：**兩段式**（`pgrep -f` 只負責蒐集候選；身分由 `basename(ps -o comm=)` 確切比對），而它的 `--self-test` **5/5 且含正負對照**（合成名字：①②當候選、③ 命令列提到名字的 wrapper 被排除、④ 真的以那個名字執行的行程被認出、⑤ 2 個候選只留 1 個殘留）。我沒有用它，而是手寫了 `ps -Ao comm= | grep -x llama-server` —— **在 macOS 上 `comm` 不是 basename 而是完整路徑**（實測），所以它**永遠不會命中**，因而「沒有別人在跑」與「有、但我比對錯欄位」在輸出上無法區分。判準兩條：① 任何閘門要用「已知為真」的狀態跑一次，確認它**會紅**；② **動手寫閘門之前，先找 repo 有沒有**這個檢查（這次有，而且比我的好）。附帶的同源陷阱：用 `pgrep -f`／`grep command` 比對時**自己的命令列也在比對範圍內**，要寫成 `[l]lama-server` 這種自我排除形式，或改用 `ps -o comm=` 取身分。

<!-- generated by harness_engine/build_memories.py from traces/lessons.jsonl -->
<!-- lesson_id: eng-gate-0054 | class: gate-integrity | superseded_by: - -->

**class**: `gate-integrity`

## 為什麼（because）

2026-09-17 17:09，使用者說「趁機器空檔來跑」，我用 `ps -Ao comm= | grep -x llama-server` 得到「無」就起了一台 13 GB 的 llama-server（`runners/server.sh` → `scripts/run_server.sh`，pid 34133）。兩分鐘後使用者喊停，我要把它殺掉時才發現：**它當時正在跑，而我同一條指令在它活著的時候回報的是「無」**。

## 反例（counterexample_observed）

殺掉之後的對照：`ps -Ao pid=,comm= | grep -E '^ *34133'` → `34133 /Users/alexchuang/Documents/flashkv-devserver/src/llama.cpp/build/bin/llama-server`（**完整路徑**，所以 `grep -x llama-server` 不命中）；而 `ps -Ao pid=,command= | awk '$2 ~ /llama-server/'` → 正確印出 `34133 …bin/llama-server`。**同一個行程、同一時刻、兩個欄位，一個命中一個不命中。** 附帶：修好之後仍要防自我命中 —— 我一開始的 `ps -Ao command= | grep -c llama-server` 數到 2，那 2 個都是**我自己那條 shell**（它的命令行裡有 `echo "含 llama-server 的行程"` 這串中文字）。 ★ 第三組對照（這個 lesson 真正想說的那一組）：跑 `bash runners/preflight.sh --self-test` → **5/5 通過**，含 ③ 的**陰性**（wrapper 被排除）與 ④ 的**陽性**（真引擎被認出）—— 也就是說**正確的閘門兩天前就在 repo 裡，而且自帶正負對照**。我重寫了一個更差的版本，並且因為它從不觸發而誤判了一次窗。

## 適用於（applies_to）

- `agent_harness/engine_loop/runners/preflight.sh`
- `agent_harness/engine_loop/runners/rebuild.sh`
- `agent_harness/tb_loop/config.env`

---

*這份 memory 是衍生物。權威是 `agent_harness/engine_loop/traces/lessons.jsonl`；*
*要改內容請改那份 record，再重跑 `build_memories.py`。手改此檔會被下一次重生蓋掉。*
