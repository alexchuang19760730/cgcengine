[engine] **`tag`（臂名）不代表模型檔 —— 任何摘要都必須留著模型與建置的出處，否則事後無法分辨。** `llama_bench_matrix.py` 的 json **有** `scalars.MODEL`、`rows[].model_filename` 與 `rows[].build_commit`（2026-09-17 逐鍵核對：`batch/batch_why/cache/env/error/extra_env/incomplete/profile/rows/scalars/tag/thermal/ubatch/wall_s`）—— 但**只有 `tag` 是可讀的識別**，而 tag 是 `PROFILE[:ENV=VAL]` 的組合，**不含** `-m`。所以：**(a) 不要把產出摘要成「只有 tag」的形式**（那會讓模型無法回溯）；**(b) 引用時讀 `scalars.MODEL` 或 `rows[].model_filename`，不要從臂名推**。在 `ARMS` 表裡**只有 `prod25-stream-mtpoff` 設了 `CGC_SERVER_MTP`** ⇒ **`--arm prefill250` 與 `--arm prod25` 解析出來是同一個檔（`Nail-…-denseIQ4X.gguf`）**，只有顯式 `CGC_SERVER_MTP=0` 才換成 `Qwen3.6-35B-A3B-UD-IQ3_XXS.gguf`。所以「臂名不同 ⇒ 模型不同」是錯的，「臂名相同 ⇒ 模型相同」也不可靠（profile 預設會變）。**零 GPU 的查法**：`prefill_certifiability.py --dry-run` 印出的 `cmd` 行含 `-m`。

<!-- generated by harness_engine/build_memories.py from traces/lessons.jsonl -->
<!-- lesson_id: eng-mh-0055 | class: measurement-hygiene | superseded_by: - -->

**class**: `measurement-hygiene`

## 為什麼（because）

2026-09-17 16:1x，本線要把 prefill 也統一 llama-bench 時，在 `MEMORY_PERF.md` 的 decode 列上看到標註「`prefill250+SPAC=1`、`-b512`、NOMINAL、**MTP=0 模型**」——若照這條讀，llama-bench 的 decode 基線（10.78／10.91）與 prefill 的 `pp2048` cell 就是**兩個不同的模型檔**，那一對數字根本不自洽。實際用 `prefill_certifiability.py --dry-run` 逐字讀 `-m` 行：`--arm prefill250` → `Nail-…denseIQ4X.gguf`、`--arm prod25` → **同一個檔**、`--arm 'prod25:CGC_SERVER_MTP=0'` → `Qwen3.6-35B-A3B-UD-IQ3_XXS.gguf`。⇒ 那條標註是錯的（或至少與現行 harness 不符），而 decode 的 10.79（09-15 `prod25-stream`）／10.89（今天）／10.78／10.91 **全部在同一支 denseIQ4X 上** ⇒ pp/tg 這一對其實是自洽的。⇒ 一個錯的模型標註，差一點讓我把一對本來可用的數字當成不可用。

## 反例（counterexample_observed）

**我在寫這條之後 20 分鐘就發現它的事實描述是錯的**：初版寫「matrix 的 json 只存 tag」，實際它同時存了 `scalars.MODEL` 與 `rows[].model_filename` ⇒ 那句話把「摘要要留什麼」與「原始產出有什麼」混為一談。修正後的規則保留了原意（不要只留 tag），但把出處指對。 原 counterexample: 修前：memory 寫「MTP=0 模型」；`--dry-run` 讀到的 `-m` 是 `Nail-Qwen3.6-35B-A3B-MTP-UD-IQ3_XXS-denseIQ4X.gguf`（兩個 arm 都是）。而 `--arms` 表裡 grep `CGC_SERVER_MTP` 只有 1 個命中（`prod25-stream-mtpoff`）。

## 適用於（applies_to）

- `scripts/check/llama_bench_matrix.py`
- `scripts/check/prefill_certifiability.py`
- `scripts/check/decode_sweep.py`
- `.workbuddy/memory/MEMORY_PERF.md`

---

*這份 memory 是衍生物。權威是 `agent_harness/engine_loop/traces/lessons.jsonl`；*
*要改內容請改那份 record，再重跑 `build_memories.py`。手改此檔會被下一次重生蓋掉。*
