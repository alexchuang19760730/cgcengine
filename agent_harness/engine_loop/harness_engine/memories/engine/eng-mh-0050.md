[engine] Before designing a change to the engine, grep for the seam that already exists: if a concept already appears per-layer in the engine's own diagnostic output, the plumbing for it is already there and the change may be configuration rather than code. Ask what the engine ALREADY prints about the quantity, and treat a per-item census line as proof that per-item values are supported end to end. The payoff is not just less work: a config-only change needs no rebuild, which in this repo means it cannot clobber another line's mapped dylibs, and it needs no numerical gate because no source changed.

<!-- generated by harness_engine/build_memories.py from traces/lessons.jsonl -->
<!-- lesson_id: eng-mh-0050 | class: measurement-hygiene | superseded_by: - -->

**class**: `measurement-hygiene`

## 為什麼（because）

M6 had been scoped as an engine change ('allow per-layer quotas instead of one MAX for the whole class'). Reading the loader found that per-layer slot counts already existed behind `LLAMA_EXPERT_CACHE_LAYER_CAPS`, read by BOTH sides (the loader's ne[2] shrink and the cache's per-layer slot vectors), and -- decisively -- that `run_server.sh` already sets it on every launch with a default of "40-40:256". The engine even prints a census of it: `LAYER_CAPS per-layer caps: total 5976 slots (avg 145.8/layer, min 143/layer)`. A census line whose total is exactly 40*143+256 is hard evidence that per-layer values flow through the whole path. The intended change therefore reduced to one environment string, with the same budget giving the 37 typical layers 179 -> 232 slots.

## 反例（counterexample_observed）

2026-09-17: the plan document had proposed a source change plus a rebuild plus a domain-specific gate run. The config route instead needs no rebuild (so it cannot interfere with the concurrently-running measurement line, the failure that lesson eng-mh-0048 records) and no D5, because no file under src/ changes. Verified read-only: 41-layer census totals match the formula to the slot at 143/179 (Nail) and 29/59 (Ornith).

## 適用於（applies_to）

- `src/llama.cpp/src/llama-model-loader.cpp`
- `src/llama.cpp/src/llama-expert-cache.h`
- `scripts/run_server.sh`

---

*這份 memory 是衍生物。權威是 `agent_harness/engine_loop/traces/lessons.jsonl`；*
*要改內容請改那份 record，再重跑 `build_memories.py`。手改此檔會被下一次重生蓋掉。*
