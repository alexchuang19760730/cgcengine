[engine] **寫在 docstring 裡的「未驗證」是一張欠條，而它的面額通常遠小於你以為的。** `build_prompt()` 的 docstring 掛著「★ NOT YET VERIFIED AGAINST A REAL MODEL … `15+27` 必須答 `42`」已經一天，理由一直是「唯一那顆 GPU 被另一條線的量測佔著」—— 而**關掉它的成本是兩次短呼叫、約 20 秒**（一次正向、一次陰性），不是那一輪 15 小時的大跑。判準：把每一條「未驗證」的宣稱當成一張欠條，問「**最少**要多少才能關掉它」；若答案是「一次短請求」，就先做，不要把它綁在大跑上（大跑的窗難等，短請求的窗到處都是）。★ 第二半同樣要緊：**正向不算證據，除非配上陰性對照**。這次的陰性（同一個 prompt，只把生成段縮成裸的 `<|im_start|>assistant\n`）重現了那個 scaffold 存在的理由 —— 進入 thinking 模式、把 token 額度用完而不作答；沒有它，「42」只說明問題太簡單。

<!-- generated by harness_engine/build_memories.py from traces/lessons.jsonl -->
<!-- lesson_id: eng-smoke-0004 | class: smoke | superseded_by: - -->

**class**: `smoke`

## 為什麼（because）

`agent_harness/engine_loop/distill/closed_loop.py` 的 `build_prompt()` 與 `llm_client.py` 的檔頭都寫著「scaffold 已實作但未對真模型驗證，跑任何多呼叫之前先驗 `15+27`→`42`」。2026-09-17 17:2x，另一條線的 `mtp_accept_ab.py` 跑完、`runners/preflight.sh` 確認引擎行程為 0／:8080 空之後，兩次呼叫就把它關掉了。

## 反例（counterexample_observed）

正向：`42`（gen_tok=3、11.5 s）—— prompt 位元組由 `build_prompt()` 自己產生（不手抄），經 `llm_client.py` 送 raw `/completion`。陰性：gen_tok=64、輸出 `<think>Here's a thinking process: 1. **Analyze User Input:** …` 且**沒有答案**（額度用完）。★ 同一批的 timings 是**冷啟**：prefill 10／37 t/s、decode 6／12 t/s，而機器當時 swap 用到 5467／6144 MB、Pages free ~20 MB；同日同一顆模型的正式量測是 prefill ~278 t/s、decode 8.8 t/s ⇒ **那兩個數字不可以當性能引用**（差 20 倍以上，而原因與模型無關）。

## 適用於（applies_to）

- `agent_harness/engine_loop/distill/closed_loop.py`
- `agent_harness/engine_loop/distill/llm_client.py`
- `agent_harness/engine_loop/distill/closed_loop_questions.md`

---

*這份 memory 是衍生物。權威是 `agent_harness/engine_loop/traces/lessons.jsonl`；*
*要改內容請改那份 record，再重跑 `build_memories.py`。手改此檔會被下一次重生蓋掉。*
