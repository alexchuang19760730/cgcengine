[engine] The first divergence between the baseline arm and the S1 arm does NOT sit at a fixed layer number -- it sits at the first layer served by the GPU slot table, i.e. it tracks CGC_S1_MIN_IL. Measured with the gate moved and nothing else changed: MIN_IL=1 gives first_diff at `l_out-1`, MIN_IL=2 gives first_diff at `ffn_moe_out-2` while `ffn_moe_out-1` and `l_out-1` both become bit-identical. So 'layer N is broken' and 'the divergence propagates numerically from layer 0' are both dead; every earlier sentence of the form 'the first divergence is at layer N' has to be rewritten as 'the first GPU-table-served layer'.

<!-- generated by harness_engine/build_memories.py from traces/lessons.jsonl -->
<!-- lesson_id: eng-diag-0028 | class: diagnosis | superseded_by: - -->

**class**: `diagnosis`

## 為什麼（because）

This also retires the previous localisation (`eng-diag-0027`, which named layer 2's gated delta-net core), because two of its premises did not survive: (a) its window started at element 0 of every tensor, and a ggml tensor is ne0-fastest, so on a (2048,T) output that window IS token 0 -- every 'SAME' in that round meant 'same at token 0', not 'same'; (b) the same layer 1 is bit-identical when the host leaf serves it and differs when the GPU table does, so the carrier is the served path and not the layer. The cleanest form of the result is a single sentence with the gate as the independent variable.

## 反例（counterexample_observed）

`CGC_S1_MIN_IL=2` run (two arms x two rounds, same-arm controls clean on both): first_diff moved from `l_out-1` to `ffn_moe_out-2`, and `l_out-1` flipped from DIFF to SAME. On that same pass layer 0's 15 captured nodes, the `mul_mat_id` ids (120/120) and the pool layout / consumed set (40/40) are all identical -- so nothing about layer 0 or the input to the gated layer differs.

## 適用於（applies_to）

- `src/llama.cpp/src/llama-context.cpp`
- `src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp`

---

*這份 memory 是衍生物。權威是 `agent_harness/engine_loop/traces/lessons.jsonl`；*
*要改內容請改那份 record，再重跑 `build_memories.py`。手改此檔會被下一次重生蓋掉。*
