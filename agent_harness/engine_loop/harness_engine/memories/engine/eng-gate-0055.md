[engine] **讀 D5 的裁決時，先把三個指標的名字展開成定義，不要把它們當成同一個主張的三個副本。** 權威定義在 `scripts/check/cgc_logits_oracle_compare.py` 的 `METRIC_DEFS`：**M1 `numeric_identity` ＝ 逐位元**（*same `row_fnv1a64` for every step (= bit-identical logits)*）；**M2 `decision_agreement` ＝ argmax 相同**（*same chosen token*）；**M3 `topk_set_agreement` ＝ top-N id 集合相同**（*same candidate set*）。三者是**階梯** M1 ⊂ M2 ⊂ M3 —— 逐位元相同**蘊含**後兩者，所以「三個都 9/9」**不等於**「三個都逐位元相同」；階梯存在的理由是**部分分數可讀**（平台漂移時看得出是在哪一級崩的）。**第二件要一起講的是單位**：`n_compared` 的計量單位是「**探針點**」而不是 prompt（現行參考檔 9 點 ＝ 6 `DEF` ＋ 3 `MTP`、step 0–5、`token_idx=0`、一條確定性 prompt、答案 `42`）⇒ **9/9 不是「9 條 prompt」也不是「整趟等價」，它是一條 prompt 上的九個取樣位置**；要宣稱「整趟 bit-identical」需要別的儀器。**第三件：D5 是「不變性／確定性」閘門，不是「模型對不對」的證明** —— 它證明的是「這個改動沒動到數字」；而且 `comparable=False` 時那三個 9/9 **不是裁決**（先讀 comparable）。

<!-- generated by harness_engine/build_memories.py from traces/lessons.jsonl -->
<!-- lesson_id: eng-gate-0055 | class: gate-integrity | superseded_by: - -->

**class**: `gate-integrity`

## 為什麼（because）

2026-09-17 18:0x，使用者問「目前生產級 prefill/decode 速度如何，**M1/M2/M3 oracle 都是 bit identical 嗎**」。那個問句本身就是這個混淆的證據：三個指標在 `m3_layergpu_final_20260917` 與今天多次都是 `9/9 9/9 9/9`，所以很容易被讀成「三個獨立的主張都通過了」。實際上只有 M1 帶 bit-identical 的語意；M2 是 argmax（比 M1 粗）、M3 是 top-N 集合（更粗）。而 `n_compared=9` 的 9 是九個 `(step, token_idx, ctx_type)` 鍵、不是九條 prompt —— 參考檔 `ref_iq3_pool8gb_M2_6144_bitident_v6_nbaware.jsonl` **只有 9 行**、`step` 範圍 0–5。本輪把這三件事寫成 `MEMORY_FACTS.md` 的一條（附 `METRIC_DEFS` 原文），並在 commit message 中逐字引用定義。

## 反例（counterexample_observed）

`Backup/m123_oracle_gate/summary_m3_layergpu_final_20260917.json`：`m1_numeric_identity='9/9'`、`m2_decision_agreement='9/9'`、`m3_topk_set_agreement='9/9'`、`n_compared=9`、`cross_tab={'num_eq_dec_eq':9,'num_eq_dec_ne':0,'num_ne_dec_eq':0,'num_ne_dec_ne':0}`。而 `METRIC_DEFS` 的括號分別寫 *bit-identical logits* / *same chosen token* / *same candidate set* —— 三者不同義。同一份參考檔 `wc -l` ＝ 9 行，`step` 0–5、`ctx_type` 為 `{'DEF':6,'MTP':3}`。

## 適用於（applies_to）

- `scripts/check/m123_oracle_gate.py`
- `scripts/check/cgc_logits_oracle_compare.py`
- `.workbuddy/memory/MEMORY_FACTS.md`

---

*這份 memory 是衍生物。權威是 `agent_harness/engine_loop/traces/lessons.jsonl`；*
*要改內容請改那份 record，再重跑 `build_memories.py`。手改此檔會被下一次重生蓋掉。*
