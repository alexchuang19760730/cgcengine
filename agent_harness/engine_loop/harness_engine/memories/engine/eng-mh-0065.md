[engine] **一個 A/B 的兩臂必須有相同的熱歷程；判準是 `thermal_hist`（全程分佈），不是 `thermal_launch`（單點）。** 判別式：兩臂的 `thermal_hist` 的第一大類標籤要相同，且另一臂不得有對方沒有的更低標籤。只要一邊有 `MODERATE`/`HEAVY` 而另一邊沒有，那個 median 差異就不可歸因。緩解：改用同熱態的**那一組配對**（ABBA 的其中一組），而不是 `M = sqrt(AB x BA)` —— 後者會把被污染的 AB 一起平均進去。

<!-- generated by harness_engine/build_memories.py from traces/lessons.jsonl -->
<!-- lesson_id: eng-mh-0065 | class: measurement-hygiene | superseded_by: - -->

**class**: `measurement-hygiene`

## 為什麼（because）

`thermal_launch` 只記啟動那一刻的快照，而一個臂的每一個 round 都可能落在不同的熱壓等級上（同一臂內實測差 34%）。於是「兩臂的 launch 都印 HEAVY」看起來像同熱態，實際上一個臂的一半 round 是 MODERATE。更陰險的是：被污染的那一臂的 median 會**偏向**某一邊，於是得到一個方向明確、看起來很合理的假結論。

## 反例（counterexample_observed）

2026-09-18 的 down-combine A/B（旗艦模型）：`en-dc-on` 與 `en-dc-ctl` 的 `thermal_launch` **都是 HEAVY**，但 `en-dc-on` 的 `thermal_hist = {HEAVY: 8}` 而 `en-dc-ctl` 是 `{HEAVY: 1, MODERATE: 7}` ⇒ 後者的 median 高了 **69%**（4.69 vs 2.78 t/s），而池統計**完全相同**（hit 82.70 對 82.70、io 1967.3 對 1966.2 MB、draft accept 0.42857 對 0.42857）⇒ 那個「融合慢 41%」全是熱態。改看反序那一組（兩臂都全程 MODERATE）才得到真實的 +25%。

## 適用於（applies_to）

- `scripts/check/decode_sweep.py`
- `agent_harness/engine_loop/traces/lessons.jsonl`

---

*這份 memory 是衍生物。權威是 `agent_harness/engine_loop/traces/lessons.jsonl`；*
*要改內容請改那份 record，再重跑 `build_memories.py`。手改此檔會被下一次重生蓋掉。*
