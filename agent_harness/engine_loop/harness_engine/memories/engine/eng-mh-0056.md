[engine] **引用一個數字之前，先讀它旁邊那一段。** `llm_client.py` 檔頭給了 `6.74 tokens per second`，但**同一段的下文**寫著它來自一行 `n_tokens = 400, progress = 0.01` 的進度輸出、當時 `vm.swapusage` 13,674/14,336 MB、`Pages free` 58 MB，並且直接下了判語：**「那樣的狀態下產生的每個數字都是關於 swap 的數字，而不是關於模型的」**，還附上與本 repo `prefill250` 的 **37× 差距**。我讀到了那個 6.74，**卻沒有讀它下面三行**，於是把「76 分鐘／呼叫 → 12 次 prefill → 15 小時窗」講了四次（PLAN、README、日誌、給使用者的回覆），而正確的量級是 **~1 小時**（250 t/s × 12 次完整 prefill ≈ 28 分鐘 ＋ 96 次短生成）。判準：**任何數字在引用前，先看它有沒有附「量測當下的機器狀態」與「它能不能被引用」的判語** —— 這個 repo 的寫法是「數字 ＋ 狀態 ＋ 判語」三件一組，**只抄數字等於把判語丟掉**。★ 這一條的毒性在於那個數字**看起來很精確**（`6.74 t/s`、`59.34 s`、`400 tokens`）—— 精確度反而降低了我去問「這是怎麼量到的」的機率（`eng-mh-0003` 的同一個形狀，這次是我自己犯）。

<!-- generated by harness_engine/build_memories.py from traces/lessons.jsonl -->
<!-- lesson_id: eng-mh-0056 | class: measurement-hygiene | superseded_by: - -->

**class**: `measurement-hygiene`

## 為什麼（because）

2026-09-17 17:2x 使用者問「D6 欠帳共用同一個窗，怎麼解決？」。我在回答前重讀那段文字，才發現它旁邊就寫著「不可引用」，而我在同一天已經引用了四次。

## 反例（counterexample_observed）

同一個 6.74 t/s 可以外推出兩種窗長：96 次各自 prefill ⇒ **121 小時**；改成臂內連續、只付 12 次完整 prefill ⇒ **15 小時**。journal 裡這兩行後面接著一句「**兩者都不可行**」—— 於是一個 25 分鐘就能跑完的東西，在紙上被寫成了一個排不進去的窗，並因此被擱置了一整個下午。反面對照：同一顆模型、同一天，另一條線量到的 `prefill250` 是 **278.56／261.17／275.01 t/s**（2873-token prompt，附帶 `IDLE_BEFORE=0`、build 指紋、熱標籤），那是一個**有狀態描述的量測**所以可以引用 —— 兩者差 37×，差別全在量測當下的機器狀態。

## 適用於（applies_to）

- `agent_harness/engine_loop/distill/llm_client.py`
- `agent_harness/engine_loop/distill/closed_loop.py`
- `agent_harness/PLAN_ENGINE_LOOP_2026-09-15.md`

---

*這份 memory 是衍生物。權威是 `agent_harness/engine_loop/traces/lessons.jsonl`；*
*要改內容請改那份 record，再重跑 `build_memories.py`。手改此檔會被下一次重生蓋掉。*
