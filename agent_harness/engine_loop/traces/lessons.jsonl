{"type": "lesson", "lesson_id": "eng-mh-0003", "class": "measurement-hygiene", "rule": "Quote a throughput delta only as the paired per-rep median of an INTERLEAVED A/B with >=3 rounds, with the build fingerprint pinned and the md5 set printed next to the number.", "because": "A single round of CGC_SUBMIT_AHEAD measured x1.78. The interleaved x3 run measured x1.711 (range 1.327-1.924) and the md5 differed on EVERY rep, so the single round was not merely imprecise, it was not quotable at all.", "counterexample_observed": "eng-20260915-192032-p25-submit-ahead-r1", "applies_to": ["scripts/check/ab_interleave.py", "scripts/check/decode_sweep.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-lf-0007", "class": "log-forensics", "rule": "Treat the output of a rate-limited assertion as a SAMPLE, never a census: it cannot support any statement of the form 'only layers X and Y are affected'.", "because": "CGC-MMID-ASSERT prints the first 8 violations verbatim and then only every 1000th. Ten printed lines were read as 'only layer 15 is affected' while the same log carried total=202000, i.e. an affected-layer set far larger than the printed sample.", "counterexample_observed": null, "applies_to": ["scripts/check/decode_sweep.py", "agent_harness/engine_loop/traces/emit_episodes.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0011", "class": "diagnosis", "rule": "A diagnostic field that is structurally constant carries no information: before reading a zero (or any fixed value) as evidence, find the code path that writes it and check it can vary at all.", "because": "Metal's shared and private buffer types BOTH return false from *_is_host(), so ggml_backend_buffer_is_host() prints 0 for every Metal buffer. A whole line of reasoning ('the table must be in a private buffer') rested on a field that cannot take another value.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-device.m", "src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0012", "class": "diagnosis", "rule": "In a graph that mixes backends, establish WHO COMPUTES and WHO READS before inspecting values: dump the split table and the per-node backend first (GGML_SCHED_DEBUG=2), then talk about contents.", "because": "S1's only crash all day was a SIGSEGV inside ggml_compute_forward_mul_mat_id on libggml-cpu. The per-node dump showed the entire difference between the two arms in one line: baseline node #73 ffn_moe_gate-0 [CPU] src[2]=ffn_moe_topk_remap-0 [CPU] vs S1 src[2]=CPU#ffn_moe_slots-0# [NULL]. Layer 0's MoE FFN runs on CPU/BLAS (its expert tensor stays full-width at 82M vs 45M for blk.1), so its ids were a CPU graph input; the GPU table turned them into a Metal product that needed a cross-backend copy of a RESHAPE view.", "counterexample_observed": null, "applies_to": ["scripts/run_server.sh", "src/llama.cpp/src/llama-graph.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0002", "class": "gate-integrity", "rule": "A gate must first be shown to REJECT something. If the reference artifact was produced by the configuration under test, the self-comparison only proves non-determinism, never correctness.", "because": "The bit-identical reference was itself a cache-ON product, so 19/19 could only demonstrate determinism and pool-size invariance. m123_oracle_gate.py now refuses cache-ON .cap files as ground truth (--oracle-abs), and the two selftests exist to prove the gate turns red.", "counterexample_observed": null, "applies_to": ["scripts/check/m123_oracle_gate.py", "scripts/check/oracle_truth_gate_selftest.py", "scripts/check/feasibility_gate_selftest.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0005", "class": "source-reading", "rule": "Do not trust a variable name for its semantics: check what the caller actually passes, especially when the same name exists at two layers of the stack.", "because": "build_moe_ffn's n_tokens is NOT ubatch.n_tokens - the hook observed t->ne[1] == 2 while the ubatch was 1 (all 80 hook lines reported n_tok=2). Writing n_tokens == 1 as the decode condition meant the decode step never built the table at all; only the dummy reserve build did.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-graph.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0006", "class": "source-reading", "rule": "Before ggml_reshape_*, confirm the tensor is contiguous; a view that keeps a stride will trip GGML_ASSERT(ggml_is_contiguous) and abort the graph build.", "because": "ggml_argsort_top_k returns ggml_argsort followed by ggml_view_4d that keeps nb[1] = n_expert*4 and only cuts ne[0] to k, so the result is not contiguous. ggml_reshape_1d aborted inside the very first reserve build, before a single token was generated.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-graph.cpp", "src/llama.cpp/ggml/src/ggml.c"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-bound-0001", "class": "honest-bounds", "rule": "A probe whose output is deliberately wrong may be run, but its numbers must carry the UPPER BOUND label everywhere they appear and must never enter a public table.", "because": "CGC_SUBMIT_AHEAD submits segment i+1 before the top-k hook rewrites its remap leaf, so its output is corrupt by construction. Its x1.711 is the CEILING for the whole 'restore inter-segment overlap' family - useful as a bound, actively misleading as a speedup.", "counterexample_observed": "eng-20260915-193322-p25-submit-ahead-r1", "applies_to": ["scripts/check/decode_sweep.py", "agent_harness/engine_loop/traces/emit_episodes.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0013", "class": "diagnosis", "rule": "A diagnostic must be shown to print before its silence is read as a finding: zero lines and 'nothing to report' are indistinguishable in a log.", "because": "GGML_SCHED_DEBUG=1's '## SPLIT' lines go through GGML_LOG_DEBUG and were filtered by the default verbosity (INFO), so the log had 0 of them. One probe run measured nothing, and the two possible readings ('only one split' vs 'the dump was suppressed') imply opposite conclusions.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/ggml/src/ggml-backend.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-smoke-0002", "class": "smoke", "rule": "An entry-point script needs a real smoke run (one import, one minimal invocation), not a syntax check: bash -n cannot see a broken package path.", "because": "agent_harness' three entry points (run_round.sh, gen_sft.sh, finetune/eval_round.sh) all pass --agent-import-path 'tb_loop.agents....' while setting PYTHONPATH to agent_harness/ itself, so 'import tb_loop' raises ModuleNotFoundError. All three were unrunnable, and bash -n reported them fine.", "counterexample_observed": null, "applies_to": ["agent_harness/run_round.sh", "agent_harness/gen_sft.sh", "agent_harness/finetune/eval_round.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0014", "class": "diagnosis", "rule": "When replacing one data flow with an equivalent one, re-inventory every call the ORIGINAL made that looked like mere insurance - those calls are usually the implicit contracts, and they are the ones that get dropped.", "because": "S1 replaced the host-written remap leaf with get_rows and dropped ggml_set_output(remap). Without it ggml-alloc collapsed 10 of 11 layers' gather outputs onto one address (ids_data=0x128179d60 x10), so every layer read the same 16 ints. Two days of hypotheses about table contents and host visibility were all invalid; adding the one line moved the asserts from 41 to 9 and made ids_data distinct per layer.", "counterexample_observed": "eng-20260915-194332-p25-slotgpu-r1", "applies_to": ["src/llama.cpp/src/llama-graph.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0015", "class": "diagnosis", "rule": "A CPU-side read of a GPU-computed tensor is a race unless it happens after a synchronize; validate such a tensor only post-sync or outside the graph, never at dispatch-encode time.", "because": "CGC-MMID-ASSERT reads op->src[2]->data on the host at Metal ENCODE time. For the host-written leaf that was harmless; for S1's GPU-computed ids it read the allocator's leftovers - an F32 router probability (0x3F64E6C4 ~ 0.893), +NaN, and a mix of legal and illegal indices within one node. Reading the same buffer after ggml_backend_sched_synchronize() gave [2 105 7 9 5 106 1 84 0 103 0 74 121 110 3 99], exactly matching the hook's published table lookups ids=[193->2 105->105 229->7 ...]. Every id_oob reported by that probe was a false alarm.", "counterexample_observed": "eng-20260915-200143-p25-slotgpu-r1", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0016", "class": "diagnosis", "rule": "A transient tensor's buffer cannot be read after the graph completes: ggml-alloc recycles it as soon as its last consumer has run, so such a read returns a different tensor's data. Pin it, or sample it while it is still live.", "because": "The first version of the post-sync readback ('CGC-S1: POST') also read the gather's INDEX vector to recompute table[idx] == gather. That vector is the CONT output feeding the gather and is not an output, so by the time the graph had completed its buffer had been handed to another tensor: layer 1 read small ints, layers 2 and 3 read F32 bit patterns (idx=1043923934), and the probe reported 16/16 mismatches that were entirely its own artifact. This is the exact mirror of the encode-time race recorded in eng-diag-0015 - one probe read a buffer before it was written, this one read a dead buffer after. The correct comparison pins the value or samples it at the right moment, which is what the hook-side EXPECT lines already do.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0004", "class": "measurement-hygiene", "rule": "Two generation digests are comparable only at the same ACTUAL generated length (predicted_n), not at the same --n-predict budget. When two completions stop at different lengths their digest difference is confounded with length, and the length-independent evidence is the fixed-width text prefix.", "because": "Two halves, both measured on 2026-09-15. (1) The bisect at --n-predict 24 reported p25-gputime md5 dc055e63 while two runs 25 minutes earlier at --n-predict 40 both reported 3d8fa55f, and the llama binary had been rebuilt in between -- which made 'the new diagnostic probes changed the model output' the obvious reading and would have sent me off to bisect the probe code. decode_bench.py hashes the whole completion and stores predicted_n beside it as n_tokens_sample, so that difference is pure truncation. (2) The sharper half: rung 2 ran every arm at the SAME --n-predict 24 and the actual generated lengths still came out 24/24/12/22/7/6, so on the five rows whose digest differs from the anchor the difference is confounded twice over -- it cannot by itself separate 'the trajectory diverged' from 'the same prefix, stopped earlier'. What does separate them is sample = texts[0][:160]: the anchor begins with a <think> reasoning preamble and every affected arm differs from its very first character, which is a length-free claim. Comparing digests is a claim about equality, so the generated length is part of the claim.", "counterexample_observed": null, "applies_to": ["scripts/check/decode_bench.py", "scripts/check/decode_sweep.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0003", "class": "gate-integrity", "rule": "When a change reroutes the code path that hosts a counter, that counter's zero is not evidence of absence: prove the instrument still runs on the layers being claimed before quoting any of its values.", "because": "CGC_SLOT_TABLE_GPU builds the GPU table INSTEAD of the host remap leaf -- llama-graph.cpp:2178-2242 is an if/else, so for il >= CGC_S1_MIN_IL the leaf is never created and cache_remap_tensors[il] stays empty. The ONE increment of cache->n_zero_mapped_selected (llama-context.cpp:5475) sits inside the leaf-write block, which the hook skips when remap == nullptr. Every served layer therefore stops reporting the 'a selected expert read the ZERO slot' quality leak, and the teardown line (llama-expert-cache.cpp:2229) prints n_zero_mapped_selected=0 -- byte-identical to a run with no leak. The S1 A/B is documented as a pure scheduler/dispatch change; this monitoring change it carries silently is not in that description, and a gate that reads that counter would pass for the wrong reason. Note the code comment at llama-context.h:364 claims the opposite ('the leaf itself is still built when this is on'), which is how the trap survived review.", "counterexample_observed": "eng-20260915-210508-p25-slotgpu-l20-r1", "applies_to": ["src/llama.cpp/src/llama-graph.cpp", "src/llama.cpp/src/llama-context.cpp", "scripts/check/m123_oracle_gate.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0004", "class": "gate-integrity", "rule": "A drift check must test the property whose change it claims to detect, and must separate what is ALLOWED to change from what is not. A check that only tests existence and a check that reddens on every run are both ignored -- the second failure mode is the mirror of the first, and both are cured only by classifying the subject.", "because": "index_assets.py --check printed 'manifest OK: 56 assets, 0 drift' while MANIFEST.jsonl still carried scripts/check/decode_sweep.py at 33992 bytes / mtime 20:52:01 although the file on disk was 38127 bytes / 21:07:40. It only asked os.path.exists per recorded path, so editing any indexed file was invisible. Comparing bytes+mtime surfaced 18 real drifts at once -- but 3 of them were Backup/phase_decomp, Backup/cgc_logs and .workbuddy/memory/2026-09-15.md, i.e. the loop's OWN outputs, which change on every run by design; shipping only that half would have left the check permanently red and therefore trained the reader to ignore it. The fix needed both halves: compare bytes+mtime for source assets, and mark run outputs volatile (existence-only). Verified red-then-green: 18 drifts -> classify -> 12 drifts (the 6 genuine edits stay) -> regenerate -> 0 drifts.", "counterexample_observed": null, "applies_to": ["agent_harness/engine_loop/index_assets.py", "agent_harness/engine_loop/MANIFEST.jsonl"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0017", "class": "diagnosis", "rule": "A node is present only when the graph traversal AND the allocator can both see it: repointing a consumer at a different tensor orphans everything upstream of the old one, and ggml-alloc aborts the reserve build on a root that nothing consumes.", "because": "The CGC_S1_KEEP_LEAF control repoints mul_mat_id from the GPU gather (slots) to the host leaf. That silently turned CONT, GET_ROWS and the VIEW into orphans AND left slot_table -- still a graph root, still ggml_set_output -- with no consumer, so the scheduler never assigned it a backend and the process died at model load with ggml-alloc.c:623 GGML_ASSERT(buffer_id >= 0), stack ggml_gallocr_allocate_node <- ggml_gallocr_reserve_n_impl <- ggml_backend_sched_reserve <- llama_context::graph_reserve (llama_server_20260915_211705.log). Constructing a tensor does not put it in the graph; only ggml_build_forward_expand does. The fix is one line -- keep the chain live by expanding slots as well -- and it is the same lesson this investigation keeps meeting from a new side (set_output missing -> aliasing; index vector read late -> dead buffer; slot_table unconsumed -> unassigned backend): the graph is a traversal, and every assumption about who computes or holds a buffer has to be checked against that traversal, not against the source that created the tensor.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-graph.cpp", "src/llama.cpp/ggml/src/ggml-alloc.c"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0005", "class": "measurement-hygiene", "rule": "Do not let an aggregate comparison of a vector whose rows have different meanings stand in for the per-row comparison: one benign row's difference will read as total disagreement and hide the rows that agree.", "because": "The CGC-S1 post-sync readback prints a single `same=` bit for the whole id vector via memcmp. On every POST line it read same=0, which reads as 'the GPU gather disagrees with the host mapping at every layer, everywhere'. Split by token the picture is the opposite: token 0 agrees at all 39 layers across 6 samples each, and only rows >= 1 differ -- and those rows are padding whose difference is provably harmless (min_il=20 carries 20 layers of it and is bit-identical). The aggregate bit therefore supported the wrong conclusion and would have sent the investigation into the publish path for a defect that is not there. The general form: when the rows of a vector are not interchangeable (token 0 of a decode step vs a padding row, a real expert id vs a slot index), an aggregate equality test answers a question nobody asked.", "counterexample_observed": "eng-20260915-212050-p25-keepleaf-dbg-r1", "applies_to": ["src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0005", "class": "gate-integrity", "rule": "Before recording a switch's null result as a refutation, read the branch condition that consumes it: an env var tested with `getenv(...) != nullptr` selects on PRESENCE, so setting it to \"0\" (or to an empty string) takes the same branch as \"1\".", "because": "CGC_OA_ASYNC was compared with `!= nullptr` at ggml/src/ggml-backend.cpp:1745, and scripts/run_server.sh puts CGC_OA_ASYNC into the server env unconditionally, so there was no value -- not \"0\", not \"\" -- that selected the non-segmented path. The A/B pair built to test whether the segment ordering causes the S1 divergence therefore re-ran the segmented arms exactly: md5 sets {dc055e63} and {29ca694a, 672585db, b8c705cc}, pool counters misses 13040 / 14296 and file_reads 36360 / 39948, all digit-for-digit identical to the segmented run. The near-miss is the expensive part: the pair looks like a clean refutation of the ordering hypothesis and would have closed a live hypothesis on the strength of an experiment that changed no variable. This is the mirror image of the run_server.sh allowlist trap (an unlisted variable is silently DROPPED, so 'no effect' and 'not set' are indistinguishable); here the variable IS set and its value is IGNORED, so 'no effect' and 'the switch is not wired' are indistinguishable. The positive discipline that follows: a null result from a switch is evidence only once the switch has been shown to change something, and reading the consumer is the cheapest such demonstration -- cheaper than the run it is meant to justify.", "counterexample_observed": "eng-20260915-213356-p25-slotgpu-noasync-r1", "applies_to": ["src/llama.cpp/ggml/src/ggml-backend.cpp", "scripts/run_server.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0018", "class": "diagnosis", "rule": "A graph node needs a CONSUMER, not just a traversal path: a tensor that is reached as a root with nothing consuming it is never assigned a backend, and ggml-alloc aborts on it (`ggml-alloc.c:623 GGML_ASSERT(buffer_id >= 0)`) before the first token.", "because": "eng-diag-0017 established the weaker half of this (constructing a tensor is not putting it in the graph -- only ggml_build_forward_expand does). CGC_S1_BUILD_LEAF=1 exercised the other half and found that being in the graph is also not enough. The arm builds the host remap leaf and deliberately leaves it unconsumed, which is exactly what the header comment on cache_slot_table_tensors has always claimed S1 does ('the leaf itself is still built when this is on: it is simply not consumed ... a pure scheduler/dispatch change'). With `slots` neither consumed nor expanded, the GET_ROWS/CONT/VIEW chain drops out of the graph, `slot_table` is left alive only by the unconditional `ggml_build_forward_expand(gf, slot_table)` above it, and the new `remap` leaf is expanded too -- so the graph ends up with two roots that nothing reads. The server aborted in llama_context::graph_reserve, i.e. during context construction, having generated nothing. The practical form: 'build the node' is a sentence about the builder's intent; 'the node exists' requires a consumer, and a design that only satisfies the first cannot be measured at all -- so it must not be planned as the control for a hypothesis.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-graph.cpp", "src/llama.cpp/src/llama-context.h"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0006", "class": "measurement-hygiene", "rule": "An instrument that has only ever run on the CONTROL arm does not license any statement about the arm under test, however directly it addresses the question.", "because": "The per-layer readback that 'exonerated the ids' ran in p25-keepleaf-dbg, whose defining property is that mul_mat_id consumes the HOST LEAF -- so it verified the gather's output inside a graph that does not consume the gather. That is a real measurement, but it is about a configuration the real arm never enters, and the same instrument on the real arm costs one arm in one sweep: p25-slotgpu-dbg prints leaf=[n/a] on every POST line (there is no leaf to compare against) yet still agrees with the hook's EXPECT on all 39 layers x 6 builds, which is the statement the earlier run was being quoted for. The cheap generality: when a probe needs a control arm to have something to compare against, that very fact means the result does not transfer; run the probe on both and say which one the number came from.", "counterexample_observed": "eng-20260915-212050-p25-keepleaf-dbg-r1", "applies_to": ["scripts/check/decode_sweep.py", "src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0007", "class": "measurement-hygiene", "rule": "A build fingerprint must be discovered by glob over every shared library the run loads (`libggml*.dylib`, `libllama*.dylib`, the server binary), never from a hand-picked file list; a knob whose code lives in a library the fingerprint does not hash makes two genuinely different builds compare as one.", "because": "Today's gate fix lives in ggml/src/ggml-backend.cpp, which links into libggml-base.0.19.0.dylib (proved by build/ggml/src/CMakeFiles/ggml-base.dir/ggml-backend.cpp.o, not by inference). decode_sweep.py:33 and ab_interleave.py:42 hash exactly three files -- llama-server, libggml-metal.0.19.0.dylib, libllama.0.0.239.dylib -- so the pair taken at 21:34 (gate NOT yet fixed: both noasync arms bit-copied their segmented counterparts) and the pair taken at 21:41 (gate fixed: p25-slotgpu-noasync = {ff68c5a2}) carry the SAME fingerprint server=131bc5316ebf metal=9729ad35ddd1 llama=9066e1697a58. The project's own rule is 'compare only within one fingerprint', so the two runs were stamped comparable while their scheduler code differed. Secondary defect in the same function: the paths contain literal version numbers, so a version bump silently degrades the entry to the string \"missing\" instead of failing.", "counterexample_observed": "eng-20260915-213356-p25-slotgpu-noasync-r1", "applies_to": ["scripts/check/decode_sweep.py", "scripts/check/ab_interleave.py", "scripts/check/knifeedge_matrix.py", "agent_harness/CONVENTIONS.md", "agent_harness/engine_loop/README.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0008", "class": "measurement-hygiene", "rule": "Before quoting a probe as evidence about what a CONSUMER received, establish when the probe runs relative to the write it is measuring; a host-side read taken during graph encoding of a device-written tensor measures the buffer's pre-write occupant, so it tracks WHO writes the buffer, not whether the value is correct.", "because": "CGC-MMID-ASSERT reads ((int32_t *) op->src[2]->data)[i] inside the mul_mat_id ENCODE path of ggml-metal-ops.cpp. While src[2] was the host-written leaf that read was meaningful (id_oob=0/240); once src[2] became the device-computed gather output the same probe reported id_oob=16/16 at layer 1 and a cumulative 202000, with first=1063585220 (0x3F65.. = F32 0.90) and 1058604036 (0x3F19.. = F32 0.60) -- i.e. router probabilities, the previous occupant of a reused buffer. The same arm under the non-segmented dispatcher read id_oob=0/288. Both readings produced readable French, so neither number tracked the answer. The baseline's 0 is what makes this dangerous: it looks like 'the baseline is clean' and invites the reading 'S1 corrupts the ids'. The sound provenance fields (ids_name, ids_op, ids_data) were in the same log line all along, and they already answer the only question this probe can actually answer: host-written vs device-written.", "counterexample_observed": "eng-20260915-213356-p25-slotgpu-noasync-r1", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "scripts/check/decode_sweep.py", "docs/REMAP_ROUNDTRIP_REMOVAL_PLAN_2026-09-15.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0006", "class": "gate-integrity", "rule": "When a gate is changed from testing presence to testing value, re-derive the effective behaviour of EVERY configuration that set the variable, and restore the previous effective behaviour as the new default; verifying that one anchor digest is unchanged is not behavioural preservation, because the configurations that explicitly set the now-meaningful value are exactly the ones the fix cannot move.", "because": "CGC_OA_ASYNC was gated on `getenv(...) != nullptr` while run_server.sh unconditionally put the variable into SERVER_ENV, so the value was never read and every profile ran segmented -- including the four whose launcher default was 0. Making the gate value-aware at 21:36 therefore flipped off / prefill250 / qa-zh / longform-zh from segmented to non-segmented, i.e. from 6.95 t/s to 0.72 t/s in the same build (measured on the baseline arm pair, same md5, same pool counters). The anchor digest dc055e63 stayed identical throughout, and prod25 is precisely the one profile that sets the variable to 1 explicitly -- so the check that was run could not have detected the change it was standing in for. prefill250 is the profile the oracle gate and bench matrix run under, so the blast radius reached the verification chain.", "counterexample_observed": null, "applies_to": ["scripts/run_server.sh", "src/llama.cpp/ggml/src/ggml-backend.cpp", "scripts/check/decode_sweep.py", "scripts/check/m123_oracle_gate.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0007", "class": "gate-integrity", "rule": "A bit-identical gate is only meaningful for a graph that is itself reproducible: before quoting 'candidate == reference' as a result, establish that the same arm, build and generation length reproduces one and the same digest over N rounds. For a run-to-run unstable arm the gate compares the candidate against one arbitrary sample of the reference, so a PASS is a coincidence and the gate has no discriminating power.", "because": "On this project the neutral-baseline arm p25-gputime is stable at every length (N=24 -> dc055e63 across every sweep and every rebuild; N=40 -> 3d8fa55f; N=100 -> 28097996), while the S1 arm p25-slotgpu under the segmented dispatcher produced THREE different digests in three rounds of one sweep on one build (29ca694a / 672585db / b8c705cc) at N=24, and produced eight further distinct values across the sweeps at other lengths. Under the non-segmented dispatcher the same arm collapses to a single stable ff68c5a2. So the same gate would be deterministic on one arm and a coin flip on the other, with no change to the gate. The failure mode is therefore silent in the direction that matters: the reference dump is a single sample, so an unstable candidate that happens to sample the reference value once reports PASS. Note also that three digests of the baseline arm that look like dispatcher sensitivity are actually generation-length effects -- md5 is only comparable within one build fingerprint AND one predicted_n, which is why the earlier 3d8fa55f/28097996 readings must not be attributed to the dispatcher.", "counterexample_observed": "eng-20260915-213356-p25-slotgpu-noasync-r1", "applies_to": ["scripts/check/m123_oracle_gate.py", "scripts/check/decode_sweep.py", "scripts/check/knifeedge_matrix.py", "agent_harness/CONVENTIONS.md", "agent_harness/engine_loop/README.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0008", "class": "gate-integrity", "rule": "A comparability stamp is only as behavioural as the gate that wrote it: once a gate stops testing presence and starts testing value, every sidecar produced under the old gate records an INTENT rather than an observation, so its knob values are unverifiable metadata. Decide the question with the measurement that is independent of the stamp -- bit-identical logits -- then repair the stamp; do not disarm the check with --allow-incomparable.", "because": "ref_iq3_pool8gb_M2_6144_bitident.jsonl was dumped 2026-09-15 17:21 against the presence-based CGC_OA_ASYNC gate, so its .cap reads CGC_OA_ASYNC='0' even though that run used the segmented dispatcher -- at the time 0 and 1 selected the same branch because run_server.sh always exports the variable and the gate only asked whether it existed. After the 21:36 value-aware fix and the launcher default restored to 1, prefill250 resolved to 1 and the gate reported INVALID COMPARISON (ENV.CGC_OA_ASYNC: ref='0' now='1') on EVERY run, i.e. the comparability check that exists to prevent cross-config verdicts had gone dark for exactly the profile the gate runs under. The stamp was stale, not the run: M1 was 9/9 bit-identical against the old reference, and the non-segmented path lands on ff68c5a2 rather than the anchor dc055e63, so both sides provably ran the segmented path. Re-baselining produced a byte-identical dump (md5 d2f0b9a01fc5404ebb04dcf3583e133f for v2 and v3 alike), which is what makes correcting the sidecar a repair rather than a moved goalpost.", "counterexample_observed": null, "applies_to": ["scripts/check/m123_oracle_gate.py", "scripts/run_server.sh", "scripts/check/llama_bench_matrix.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0009", "class": "measurement-hygiene", "rule": "Read an instrument's call graph and its guards before specifying or trusting it. 'How often does this mechanism run' answers whether the mechanism EXISTS, never how often the STATE moves; and a probe that is never allowlisted has never been run through the launcher at all, so its verdict is a no-op mistaken for a negative result. When you replace a degenerate instrument, check the replacement for the same two defects -- it is usually degenerate the same way.", "because": "Three instruments were checked before writing one line of the measurement D3 depends on, and all three failed on one of those two axes. (1) The instrument §9.17(d) specified -- publish_slot_table call count per decode step -- is degenerate by construction: both of its call sites are inside the S1 remap hook itself (llama-context.cpp:5448 and :5584) and the header comment says it is 'reached only from the S1 path', so the answer is 0 on the baseline arm and once per served layer on S1. It measures mechanism presence, not state velocity -- exactly the defect §9.17(d) had already diagnosed in the CGC-SLOT-TABLE clamp count it was supposed to replace, repeated by the replacement. (2) The cheapest existing churn proxy, LLAMA_EXPERT_CACHE_STEP_DBG, had never been in run_server.sh's allowlist, which is an explicit list passed through `env \"${SERVER_ENV[@]}\"` rather than a prefix rule; so the diagnostic quoted in-code as the source of the 2026-08-28 'structural churn' verdict could not be enabled through the launcher, and every attempt since was a silent no-op indistinguishable from 'the change had no effect' -- the trap the same file already documents for NOHOOK/NOGATHER. (3) With that fixed and verified (dry run shows ENV LLAMA_EXPERT_CACHE_STEP_DBG=1 reaching the launch line), the probe still produced zero lines on a prod25 (MTP off) decode, because its block sits inside `if ((verify_fast || draft_fast) && cgc_fast_eligible)` and every step reported `CGC-WARM ... fast=0`. The zero was a dead end, not a zero churn: the instrument's guard, not the phenomenon, decided the reading.", "counterexample_observed": "A zero line count that reads as 'no churn' while the block was in fact never entered -- the same shape as the CGC-SLOT-TABLE 0-line count and as CGC_IDS_CAPTURE before it was allowlisted.", "applies_to": ["scripts/run_server.sh", "src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/src/llama-expert-cache.cpp", "docs/REMAP_ROUNDTRIP_REMOVAL_PLAN_2026-09-15.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0010", "class": "measurement-hygiene", "rule": "Before attributing ANY single-shot throughput difference, measure the repeatability floor of the SAME binary on the same machine: run it twice and take the spread. If the effect you are attributing is smaller than that spread, the comparison has no discriminating power and the correct output is 'not resolvable', not 'direction unknown'. An instrument whose noise exceeds the signal is not a weak instrument, it is a non-instrument.", "because": "§3 of the whitepaper listed a 6-12% deficit of the rebuilt binary against an 18:00 run of an older one, honestly flagged as unquotable for want of an interleaved A/B -- a framing that implicitly assumed the gap needed explaining. Two runs of the IDENTICAL binary 34 minutes apart (22:30 and 23:04, no rebuild in between: libggml-metal and llama-server both timestamped 22:14) produced tg@0 8.67 -> 6.91 (-20.3%), pp@1024 79.88 -> 105.71 (+32.3%), pp@4096 81.52 -> 103.11 (+26.5%) and tg@512 6.72 -> 8.85 (+31.7%), with per-cell ± as large as the effect (pp@512 ±12.65 on 103.25). So the quantity §3 was comparing across builds moves by up to 32% with nothing changed at all, which makes the 6-12% not merely unquotable but unmeasurable by that method. The two runs also disagree on pool behaviour (hit 97.1%/519501 reads vs 97.3%/16163 misses), so it is not only the timing that drifts between launches.", "counterexample_observed": "The same binary, unmodified, two consecutive runs: tg@0 8.67 vs 6.91 t/s.", "applies_to": ["scripts/check/llama_bench_matrix.py", "docs/PREFILL250_DECODE25_WHITEPAPER_20260915_2240.html", "agent_harness/CONVENTIONS.md", "scripts/check/ab_interleave.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0009", "class": "gate-integrity", "rule": "A gate quantity computed at one call site and DISCARDED at another is not a gate quantity. Enforce it by funnelling every site through one counted entry point, not by adding a counter and hoping each caller reads it -- the caller that does not read it is exactly the caller the gate exists for.", "because": "llama_expert_cache_publish_slot_table returns the number of entries it had to clamp to 0 because the layer has no reserved ZERO slot, and §8.3 identified that count as the signal of a SILENT correctness failure: with zero_slot < 0 a non-resident selected expert makes the host leaf write -1 (loud -- the consumer reports an out-of-range id) while the table clamps it to 0 (index 0 is legal, so mul_mat_id reads a DIFFERENT expert's weights). There are exactly two call sites. The fast-path site checked the return value and printed it. The pool-path site discarded it outright -- and the pool path is the one EVERY MTP-off S1 arm actually takes, because CGC_SERVER_MTP=0 makes verify_fast and draft_fast both false so the `(verify_fast || draft_fast) && cgc_fast_eligible` guard is never entered. Every S1 measurement to date therefore computed the silent-corruption counter and threw it away. A second, independent divergence sat in the same pair: the two sites write the LEAF under different mappings (slot_table_safe vs the raw slot table) while publishing the TABLE through the same function, so their table/leaf equivalence is asserted, not shared.", "counterexample_observed": "A counter that is computed on every publish of the arm being measured, and read by nobody on that path.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/src/llama-expert-cache.h", "src/llama.cpp/src/llama-expert-cache.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0007", "class": "source-reading", "rule": "An edit tool's success message is not evidence the edit is in the tree, and edits issued in PARALLEL against one file silently lose writes. After every batch of edits to a single file, verify with `git diff -U0 -- <file> | grep '^@@'` plus a targeted grep for the removed strings; never stack a build on top of an unverified edit.", "because": "Three Edit calls were issued in a single message against llama-context.cpp (remove cgc_s1_tag() from the L4 identity map, remove it from the gather identity map, and drop the two guard terms that tested it). All three returned 'Successfully edited file'. Grepping the file afterwards found the first two strings still present at lines 5145 and 5966 while the third was gone -- one landed, two were silently dropped, and the reports were identical. Compounding it, the same run had removed the DEFINITIONS of cgc_s1_tag()/cgc_s1_ident() earlier, so the file was left referring to functions that no longer existed: a state that cannot compile, and that no success message would have revealed. This is the second occurrence of the class in one session -- an earlier attempt to add the ffn_moe_down capture and rewrite the fast-path call site also reported success without writing bytes, caught only because the string `CGC-S1: OUT` was found inside the built dylib while never appearing in any log. The distinctive signature is a probe that is compiled in and provably never fires.", "counterexample_observed": "Three 'Successfully edited' returns on one file: one landed, two absent from disk, and the file left calling deleted functions.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/src/llama-expert-cache.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0011", "class": "measurement-hygiene", "rule": "A capped diagnostic readback must be gated on the PHASE that contains the question, not merely on the knob that arms it. If the probe is counter-limited, the first N graphs of a process are warmup and prefill; numbering 'graph 3' across the whole process addresses a graph where the mechanism under test may not even be running. Gate on the phase (batched vs not) and print the ordinal inside that phase.", "because": "The ffn_moe_down output capture was first written with `static int cgc_s1_out_n = 0; if (cgc_s1_out_n < 6)` around a loop that dumps every captured layer. graph_compute is called for every graph, so that budget is consumed by the first six graph computes of the process -- warmup and prefill chunk processing -- before the first decoded token exists. Prefill takes a different branch of the expert-cache hook entirely (it builds no remap leaf; the hook itself checks n_tokens), so the arms would have been compared at a graph where the S1 mechanism under test is not engaged, and the comparison would have reported 'identical' for a reason that has nothing to do with the mechanism. The fix is one line of predicate: the second parameter of graph_compute is `batched` (= ubatch.n_tokens > 1), so `if (cgc_s1_outcap && !batched)` both spends the budget on decode graphs and makes the printed ordinal an honest address within the phase being discussed.", "counterexample_observed": "A 6-graph readback budget spent entirely on warmup and prefill, in a probe whose stated purpose was to compare one decode graph between two arms.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0012", "class": "measurement-hygiene", "rule": "A probe that must perturb the allocator to read its value has a cost measured in PINS PER GRAPH, not in bytes, and that cost can only be found by experiment. Do not derive the safe budget from tensor dimensions, and do not creep up on the limit: the failure mode is a fail-stop abort at load, not a slowdown.", "because": "CGC_S1_OUT_CAP pins ffn_moe_down with ggml_set_output so a post-synchronize read sees a live buffer. The first version pinned every layer in every graph and both arms died at the load-time warmup decode with kIOGPUCommandBufferCallbackErrorOutOfMemory -> CGC_METAL_FAIL_STOP abort (2/2), while the control arm without the knob started and decoded at 20.2 t/s in the same session. The obvious explanation -- bytes -- was written into the code comment as '2048 x 6144 x 4B = 50 MB per layer, ~2 GB for 40 layers' and it is WRONG in the direction that matters. Corrected, the boundary is: 40 layers on every graph dies; 11 layers on prefill only ALSO dies; 4 layers on prefill runs; 40 layers on decode runs. At the graph that dies the hook reports ntok=2, so ffn_moe_down is [2048, 8, 2] F32 and eleven pinned layers hold 11 x 131 KB = 1.4 MB. A megabyte and a half does not exhaust a Metal working set, so the controlling variable is the COUNT of tensors marked as graph outputs -- which changes the scheduler's allocation and split -- and it is non-monotone in the phase (prefill chunks are excluded, yet prefill-only pinning is the variant that dies). Two consequences worth keeping: the safe number is empirical (between 4 and 11 here), and a byte budget would have been wrong twice, once as 2 GB and once as 5 MB.", "counterexample_observed": "Eleven pinned tensors totalling 1.4 MB abort a 16 GB machine's Metal command buffer, while forty pinned tensors on the same tensors abort too -- and forty pinned tensors on a different phase do not.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/src/llama-context.h", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0019", "class": "diagnosis", "rule": "If a difference reproduces with the SAME structure at two DISJOINT slices of the axis you suspected, that axis is not the variable -- stop deepening it and look for a property of the container instead. Two windows that agree on 'which items differ' rather than 'the first item that differs' have already answered the question.", "because": "The S1 divergence had been chased through the layer axis: the decode capture reported 480/480 layers differing, including layer 0, so the natural next move was to walk deeper layer windows to find the first bad layer. That ladder was uninterpretable from the start -- decode step 0's layer-0 input descends from the prefill's KV and the two arms' prefills already disagree (M1 5/9), so every decode layer is downstream of an earlier divergence, and there is no 'identical input' origin at which a first divergence can be defined. Re-running it where the input IS identical by construction (prefill, layer 0 = embedding) produced the answer: probing layers 0-3 and then layers 4-7 (disjoint, 8 layers, 6 graphs, 2 arms = 96 pairings) returned the SAME structure both times. Crucially the two rungs did not agree on which layer first diverges -- they agreed that divergence tracks a property of the GRAPH, not of the layer: slab-served graphs (256/256 experts resident) were bit-identical across all 8 probed layers, pool-served graphs (143/256) diverged from the first pooled layer on. The predictor was verified against an independent runtime marker, `CGC-PREFILL-STREAM: il=0 kind=0 ntok=182`, which appears on exactly the two all-EQ graphs. The cost of the misdirection was real: the first capture attempt pinned all 40 layers in every graph and killed the process at load (see eng-mh-0012), and two layer windows were measured before the orthogonal property was noticed.", "counterexample_observed": "A layer ladder measured twice, 0-3 and 4-7, whose two windows agreed on the divergence structure rather than on a first-divergent index -- the signature that the layer index was never the variable, while the analysis was still framed as 'find the first bad layer'.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "scripts/check/decode_sweep.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0010", "class": "gate-integrity", "rule": "An env var whose READER tests presence (`getenv(...) != nullptr`) is a trap when its documented OFF value is the string \"0\": setting it to 0 turns the feature ON. Before citing any knob's value from a resolved-config record, grep the readers and check whether they test presence or value -- and never use a presence-gated knob's recorded value as evidence of behaviour.", "because": "The profile at run_server.sh:939-945 sets LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0=0 under a comment dating from 2026-09-09 whose entire purpose is to bring blk.0 BACK into the pool ('已證實 4/10 品質殺手 ... 甜蜜點 ... layer 0 在 pool 內) 無 skip0 = 90-100%'), and :1493 hands the array to the child as real environment variables via `env \"${SERVER_ENV[@]}\"`. All three readers of that variable test presence -- llama.cpp:396, llama-expert-cache.cpp:1708, llama-context.cpp:5222 -- so \"0\" is non-null, the gate is TRUE, and blk.0 has been excluded from the pool the entire time: llama-model-loader.cpp:1464 forces l4_kind = -1 for layer 0 and its FFN reads the full-width tensor with raw ids. The runtime confirms it independently: CGC-DECPROF reports layers=39 on all 19 steps of a log and layers=40 on none. The consequence is that a documented quality fix has been inert for six days, and -- separately -- that the resolved-config sidecars (gate caps, llama-bench json) all record `LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0: \"0\"`, i.e. they record an INTENT while the engine implements its opposite. This is the same disease as the stale oracle cap (dec-20260915-2233) and it is the third member of the presence-vs-value family in this project after CGC_OA_ASYNC (dec-20260915-2142, whose value-aware fix silently re-routed four profiles and forced a gate re-baseline). Presence-gating is not always wrong -- it is how this project spells 'on' for many diagnostics -- but it makes the pair (0, unset) mean the same thing, so any profile that writes `=0` expecting the feature off is writing a no-op.", "counterexample_observed": "LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0=0 in the production profile, with all three readers testing presence, running for six days with the feature ON and the profile comment asserting the opposite; the only signal that anything was wrong was CGC-DECPROF saying layers=39.", "applies_to": ["scripts/run_server.sh", "src/llama.cpp/src/llama.cpp", "src/llama.cpp/src/llama-expert-cache.cpp", "src/llama.cpp/src/llama-context.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0011", "class": "gate-integrity", "rule": "A fix that changes what a config value MEANS while leaving the string unchanged is invisible to a resolved-config comparability check, because that check is a string diff. It will report a plain FAIL that is a category error, not a regression. So: (a) re-baseline deliberately and retire the old reference, and (b) run the PRE-fix value as a control arm first -- it must reproduce the old reference bit-identically, which is simultaneously the proof that the old reference was dumped under the old behaviour and that the predicate change is numerically inert in every other dimension.", "because": "LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0's three readers were switched from a presence test to value semantics on 2026-09-16. Both the pre-fix and post-fix resolved configs contain the literal string \"0\" (the profile writes =0 either way), so m123_oracle_gate.py's comparability precondition -- which diffs the resolved ENV of reference and fresh dump -- sees ZERO differences and proceeds to a normal verdict. Measured: the control arm (CGC_SERVER_SKIP0=1, i.e. the pre-fix behaviour under the new binary) reproduced ref_iq3_pool8gb_M2_6144_bitident_v3.jsonl bit-identically (M1 9/9, M2 9/9, M3 9/9, full-row fnv1a64 9/9), while the new default (=0, layer 0 pooled) scored M1 4/9 / M2 9/9 / M3 6/9 with cross-tab {same/same: 4, diff/same: 5, diff/diff: 0}. Without the control arm the 4/9 would have been indistinguishable from 'the fix broke something'. This is the opposite failure mode to the CGC_OA_ASYNC round (dec-20260915-2215), where the resolved string itself moved (CGC_OA_ASYNC 0 -> 1) and the gate raised INVALID COMPARISON on its own, waking someone up. Value-semantics changes get no such alarm, so the re-baseline has to be a declared act rather than a detected one.", "counterexample_observed": "Two runs with byte-identical resolved env strings (LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0=0 in both) that are nonetheless not comparable: one dumped 39 pooled layers, the other 40. The gate's own re-baseline hint ('A cross-config diff is NOT a regression') never fires, because there is no cross-config diff to report.", "applies_to": ["scripts/check/m123_oracle_gate.py", "Backup/knifeedge_matrix/ref_iq3_pool8gb_M2_6144_bitident_v4_skip0off.jsonl", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0012", "class": "measurement-hygiene", "rule": "A reading that is identical in every arm cannot be evidence about which arm you are in. Before using a log line to prove a state change, check that it actually varies with that state -- otherwise you are quoting a planned/target value as a measurement.", "because": "The 2026-09-09 sweet-spot log was read as '5976 slots = layer 0 in the pool', and 5976 was treated as a fingerprint of the pooled-40 configuration. Measured 2026-09-16: `llama_expert_cache: LAYER_CAPS per-layer caps: total 5976 slots (avg 145.8/layer, min 143/layer)` is printed IDENTICALLY in the skip0=1 arm (39 pooled layers) and the skip0=0 arm (40 pooled layers), because it is the planned sum of LAYER_CAPS (40 x 143 + 256 for the MTP layer) and does not consult the pooled-layer set at all. What actually discriminates the two arms is the teardown pool-integrity scan: owner-set slots 5793 -> 5935 (a delta of exactly +142, one layer's worth) and zero-regions 0 -> 4 (all four in layer 0, first at layer=0 slot=18 kind=0 owner_expert=252). Same disease as eng-gate-0010: a recorded value read as behaviour.", "counterexample_observed": "`total 5976 slots` identical across two arms that differ by exactly one pooled layer, with 2026-09-09 treating that same number as proof that layer 0 was pooled.", "applies_to": ["docs/PREFILL250_DECODE25_WHITEPAPER_20260915_2355.html", "agent_harness/CONVENTIONS.md", "docs/SKIP0_VALUE_SEMANTICS_20260916_0021.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0013", "class": "measurement-hygiene", "rule": "A launch configuration must be read by DUMPING it under the configuration you care about, not inferred from the default. Capture the resolved env as a normalised fingerprint so that 'the launch environment did not change' becomes a checkable claim -- and normalise correctly, or the instrument reports noise as change.", "because": "run_server.sh's CGC_LOOP_GUARD push sat inside the else-branch of the CGC_SOFT_POOL_L1 test (a missing `fi`), so setting CGC_SOFT_POOL_L1 -- exactly what the comment 6 lines above recommends ('opt back in with CGC_SOFT_POOL_L0=48 CGC_SOFT_POOL_L1=48') -- silently dropped CGC_LOOP_GUARD from the env handed to the server, and CGC_LOOP_GUARD=1 could not restore it because 'push the guard' lived in the branch that only runs when L1 is UNSET. Measured with CGC_DUMP_ENV=1 across four inputs: default -> pushed, L0=48 -> pushed, L1=48 -> DROPPED, L0=48 L1=48 -> DROPPED, plus L1=48 CGC_LOOP_GUARD=1 -> still DROPPED (the earlier 'L0+L1 keeps it' reading was a zsh quoting artifact of my own probe, not a property of the script). No default profile sets L1, so reading the default would never have exposed it. The fingerprint then carried the argument: the normalised dump md5 for prefill250 stabilised at e68a5dc537d626622a9af6c9c7ddf665 once the two volatile line classes were removed, and was IDENTICAL before and after the fix -- which justifies 'this change cannot move the gate numbers' without re-running the gate to find out. Raw (un-normalised) md5 is not stable: prefill250 differed on consecutive runs (8726fb39 vs f898bc94) while prod25 happened to match itself, i.e. a naive fingerprint is wrong in both directions.", "counterexample_observed": "The dump's own volatility: `CGCENV LOG .../llama_server_YYYYMMDD_HHMMSS.log` carries a timestamp, and the `[preflight] free=NN%` / `[guard] ... free=NN%` lines track memory pressure. Without filtering both classes, 'stable' and 'changed' are both unreliable verdicts -- and unfiltered, two profiles in the SAME run set disagreed about which one was stable.", "applies_to": ["scripts/run_server.sh", "agent_harness/CONVENTIONS.md", "docs/PRECOMMIT_HOOK_GUIDE_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0013", "class": "gate-integrity", "rule": "Before analysing whether a setting is missing from a resolved configuration, check the control flow that emits it. A setting can be absent because of an if/else nesting error rather than because a default is off -- and the two are indistinguishable from the resolved output alone, while requiring opposite fixes.", "because": "CGC_LOOP_GUARD was absent from the launch env whenever CGC_SOFT_POOL_L1 was set. That is NOT 'guarded by a default of off': the guard block had been swallowed into the L1 else-branch, so the knob was UNREACHABLE in that configuration, and CGC_LOOP_GUARD=1 could not turn it back on. Evidence: grep for LOOP_GUARD in run_server.sh returns exactly one push site (lines 1388-1390), which is textually inside the L1 else-branch, closed by a doubled `fi`; and the measured 4-input table (default / L0 / L1 / L0+L1) matches the nesting prediction rather than a default-value prediction. Why the distinction is expensive: the wrong reading ('it is just off by default') leads to 'pass the flag' -- which is precisely the action that does not work here. The same day's presence-gated defect is the mirror image: there the value lied while the structure was fine; here the structure lies while the value is ignored.", "counterexample_observed": "`CGC_SOFT_POOL_L1=48 CGC_LOOP_GUARD=1` -- a user explicitly requesting the guard ON -- produced no CGC_LOOP_GUARD in the env, because the request and the emission live in mutually exclusive branches of the same if. The knob was not defaulted off; it was unreachable.", "applies_to": ["scripts/run_server.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0008", "class": "source-reading", "rule": "A path returned by a directory-resolving tool is expressed relative to THAT tool's working directory, not yours. Anchor every cross-directory path to your own base before using it. A relative return that is perfectly correct inside the tool's own cwd silently mis-targets everywhere else -- and the failure can be a total no-op that still prints success.", "because": "`git -C <main-worktree> rev-parse --git-path hooks`, run from a DIFFERENT cwd, returns `.git/hooks`. That value is not wrong, it is incomplete: it omits its base. Substituting it for a previously-absolute value (--absolute-git-dir) in check_build_tracked.sh --install-hook therefore looked like a one-line fix while actually relocating the write target to whatever the shell's cwd happened to be. In this repo the shell cwd is a linked worktree, where `.git` is a FILE, so `mkdir -p` / `cat >` / `chmod +x` all failed and the script still printed \"installed pre-commit hook: <path>\". Measured: `ls -ld <linked-worktree>/.git/hooks` -> \"Not a directory\". This is a strictly worse failure mode than the bug being fixed: the original wrote to a real directory that git does not read (recoverable by moving one file), whereas the naive fix writes nothing at all while reporting success (nothing to move).", "counterexample_observed": "`HOOK_DIR=\"$(git -C \\\"$REPO_ROOT\\\" rev-parse --git-path hooks)\"` followed by `cat > \"$HOOK_DIR/pre-commit\"` -- the literal reading of the approved one-line change. From the main worktree the resolver returns the relative `.git/hooks`; from a linked worktree the shell cwd resolves it onto `<linked-worktree>/.git/hooks`, where `.git` is a regular file. mkdir/cat/chmod each fail, the script prints its success line, and exit code stays 0. Both the write target and the error are invisible.", "applies_to": ["scripts/check_build_tracked.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
