{"type": "lesson", "lesson_id": "eng-mh-0003", "class": "measurement-hygiene", "rule": "Quote a throughput delta only as the paired per-rep median of an INTERLEAVED A/B with >=3 rounds, with the build fingerprint pinned and the md5 set printed next to the number.", "because": "A single round of CGC_SUBMIT_AHEAD measured x1.78. The interleaved x3 run measured x1.711 (range 1.327-1.924) and the md5 differed on EVERY rep, so the single round was not merely imprecise, it was not quotable at all.", "counterexample_observed": "eng-20260915-192032-p25-submit-ahead-r1", "applies_to": ["scripts/check/ab_interleave.py", "scripts/check/decode_sweep.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-lf-0007", "class": "log-forensics", "rule": "Treat the output of a rate-limited assertion as a SAMPLE, never a census: it cannot support any statement of the form 'only layers X and Y are affected'.", "because": "CGC-MMID-ASSERT prints the first 8 violations verbatim and then only every 1000th. Ten printed lines were read as 'only layer 15 is affected' while the same log carried total=202000, i.e. an affected-layer set far larger than the printed sample.", "counterexample_observed": null, "applies_to": ["scripts/check/decode_sweep.py", "agent_harness/engine_loop/traces/emit_episodes.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0011", "class": "diagnosis", "rule": "A diagnostic field that is structurally constant carries no information: before reading a zero (or any fixed value) as evidence, find the code path that writes it and check it can vary at all.", "because": "Metal's shared and private buffer types BOTH return false from *_is_host(), so ggml_backend_buffer_is_host() prints 0 for every Metal buffer. A whole line of reasoning ('the table must be in a private buffer') rested on a field that cannot take another value.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-device.m", "src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0012", "class": "diagnosis", "rule": "In a graph that mixes backends, establish WHO COMPUTES and WHO READS before inspecting values: dump the split table and the per-node backend first (GGML_SCHED_DEBUG=2), then talk about contents.", "because": "S1's only crash all day was a SIGSEGV inside ggml_compute_forward_mul_mat_id on libggml-cpu. The per-node dump showed the entire difference between the two arms in one line: baseline node #73 ffn_moe_gate-0 [CPU] src[2]=ffn_moe_topk_remap-0 [CPU] vs S1 src[2]=CPU#ffn_moe_slots-0# [NULL]. Layer 0's MoE FFN runs on CPU/BLAS (its expert tensor stays full-width at 82M vs 45M for blk.1), so its ids were a CPU graph input; the GPU table turned them into a Metal product that needed a cross-backend copy of a RESHAPE view.", "counterexample_observed": null, "applies_to": ["scripts/run_server.sh", "src/llama.cpp/src/llama-graph.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0002", "class": "gate-integrity", "rule": "A gate must first be shown to REJECT something. If the reference artifact was produced by the configuration under test, the self-comparison only proves non-determinism, never correctness.", "because": "The bit-identical reference was itself a cache-ON product, so 19/19 could only demonstrate determinism and pool-size invariance. m123_oracle_gate.py now refuses cache-ON .cap files as ground truth (--oracle-abs), and the two selftests exist to prove the gate turns red.", "counterexample_observed": null, "applies_to": ["scripts/check/m123_oracle_gate.py", "scripts/check/oracle_truth_gate_selftest.py", "scripts/check/feasibility_gate_selftest.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0005", "class": "source-reading", "rule": "Do not trust a variable name for its semantics: check what the caller actually passes, especially when the same name exists at two layers of the stack.", "because": "build_moe_ffn's n_tokens is NOT ubatch.n_tokens - the hook observed t->ne[1] == 2 while the ubatch was 1 (all 80 hook lines reported n_tok=2). Writing n_tokens == 1 as the decode condition meant the decode step never built the table at all; only the dummy reserve build did.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-graph.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0006", "class": "source-reading", "rule": "Before ggml_reshape_*, confirm the tensor is contiguous; a view that keeps a stride will trip GGML_ASSERT(ggml_is_contiguous) and abort the graph build.", "because": "ggml_argsort_top_k returns ggml_argsort followed by ggml_view_4d that keeps nb[1] = n_expert*4 and only cuts ne[0] to k, so the result is not contiguous. ggml_reshape_1d aborted inside the very first reserve build, before a single token was generated.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-graph.cpp", "src/llama.cpp/ggml/src/ggml.c"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-bound-0001", "class": "honest-bounds", "rule": "A probe whose output is deliberately wrong may be run, but its numbers must carry the UPPER BOUND label everywhere they appear and must never enter a public table.", "because": "CGC_SUBMIT_AHEAD submits segment i+1 before the top-k hook rewrites its remap leaf, so its output is corrupt by construction. Its x1.711 is the CEILING for the whole 'restore inter-segment overlap' family - useful as a bound, actively misleading as a speedup.", "counterexample_observed": "eng-20260915-193322-p25-submit-ahead-r1", "applies_to": ["scripts/check/decode_sweep.py", "agent_harness/engine_loop/traces/emit_episodes.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0013", "class": "diagnosis", "rule": "A diagnostic must be shown to print before its silence is read as a finding: zero lines and 'nothing to report' are indistinguishable in a log.", "because": "GGML_SCHED_DEBUG=1's '## SPLIT' lines go through GGML_LOG_DEBUG and were filtered by the default verbosity (INFO), so the log had 0 of them. One probe run measured nothing, and the two possible readings ('only one split' vs 'the dump was suppressed') imply opposite conclusions.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/ggml/src/ggml-backend.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-smoke-0002", "class": "smoke", "rule": "An entry-point script needs a real smoke run (one import, one minimal invocation), not a syntax check: bash -n cannot see a broken package path.", "because": "agent_harness' three entry points (run_round.sh, gen_sft.sh, finetune/eval_round.sh) all pass --agent-import-path 'tb_loop.agents....' while setting PYTHONPATH to agent_harness/ itself, so 'import tb_loop' raises ModuleNotFoundError. All three were unrunnable, and bash -n reported them fine.", "counterexample_observed": null, "applies_to": ["agent_harness/tb_loop/run_round.sh", "agent_harness/tb_loop/gen_sft.sh", "agent_harness/tb_loop/finetune/eval_round.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0014", "class": "diagnosis", "rule": "When replacing one data flow with an equivalent one, re-inventory every call the ORIGINAL made that looked like mere insurance - those calls are usually the implicit contracts, and they are the ones that get dropped.", "because": "S1 replaced the host-written remap leaf with get_rows and dropped ggml_set_output(remap). Without it ggml-alloc collapsed 10 of 11 layers' gather outputs onto one address (ids_data=0x128179d60 x10), so every layer read the same 16 ints. Two days of hypotheses about table contents and host visibility were all invalid; adding the one line moved the asserts from 41 to 9 and made ids_data distinct per layer.", "counterexample_observed": "eng-20260915-194332-p25-slotgpu-r1", "applies_to": ["src/llama.cpp/src/llama-graph.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0015", "class": "diagnosis", "rule": "A CPU-side read of a GPU-computed tensor is a race unless it happens after a synchronize; validate such a tensor only post-sync or outside the graph, never at dispatch-encode time.", "because": "CGC-MMID-ASSERT reads op->src[2]->data on the host at Metal ENCODE time. For the host-written leaf that was harmless; for S1's GPU-computed ids it read the allocator's leftovers - an F32 router probability (0x3F64E6C4 ~ 0.893), +NaN, and a mix of legal and illegal indices within one node. Reading the same buffer after ggml_backend_sched_synchronize() gave [2 105 7 9 5 106 1 84 0 103 0 74 121 110 3 99], exactly matching the hook's published table lookups ids=[193->2 105->105 229->7 ...]. Every id_oob reported by that probe was a false alarm.", "counterexample_observed": "eng-20260915-200143-p25-slotgpu-r1", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0016", "class": "diagnosis", "rule": "A transient tensor's buffer cannot be read after the graph completes: ggml-alloc recycles it as soon as its last consumer has run, so such a read returns a different tensor's data. Pin it, or sample it while it is still live.", "because": "The first version of the post-sync readback ('CGC-S1: POST') also read the gather's INDEX vector to recompute table[idx] == gather. That vector is the CONT output feeding the gather and is not an output, so by the time the graph had completed its buffer had been handed to another tensor: layer 1 read small ints, layers 2 and 3 read F32 bit patterns (idx=1043923934), and the probe reported 16/16 mismatches that were entirely its own artifact. This is the exact mirror of the encode-time race recorded in eng-diag-0015 - one probe read a buffer before it was written, this one read a dead buffer after. The correct comparison pins the value or samples it at the right moment, which is what the hook-side EXPECT lines already do.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0004", "class": "measurement-hygiene", "rule": "Two generation digests are comparable only at the same ACTUAL generated length (predicted_n), not at the same --n-predict budget. When two completions stop at different lengths their digest difference is confounded with length, and the length-independent evidence is the fixed-width text prefix.", "because": "Two halves, both measured on 2026-09-15. (1) The bisect at --n-predict 24 reported p25-gputime md5 dc055e63 while two runs 25 minutes earlier at --n-predict 40 both reported 3d8fa55f, and the llama binary had been rebuilt in between -- which made 'the new diagnostic probes changed the model output' the obvious reading and would have sent me off to bisect the probe code. decode_bench.py hashes the whole completion and stores predicted_n beside it as n_tokens_sample, so that difference is pure truncation. (2) The sharper half: rung 2 ran every arm at the SAME --n-predict 24 and the actual generated lengths still came out 24/24/12/22/7/6, so on the five rows whose digest differs from the anchor the difference is confounded twice over -- it cannot by itself separate 'the trajectory diverged' from 'the same prefix, stopped earlier'. What does separate them is sample = texts[0][:160]: the anchor begins with a <think> reasoning preamble and every affected arm differs from its very first character, which is a length-free claim. Comparing digests is a claim about equality, so the generated length is part of the claim.", "counterexample_observed": null, "applies_to": ["scripts/check/decode_bench.py", "scripts/check/decode_sweep.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0003", "class": "gate-integrity", "rule": "When a change reroutes the code path that hosts a counter, that counter's zero is not evidence of absence: prove the instrument still runs on the layers being claimed before quoting any of its values.", "because": "CGC_SLOT_TABLE_GPU builds the GPU table INSTEAD of the host remap leaf -- llama-graph.cpp:2178-2242 is an if/else, so for il >= CGC_S1_MIN_IL the leaf is never created and cache_remap_tensors[il] stays empty. The ONE increment of cache->n_zero_mapped_selected (llama-context.cpp:5475) sits inside the leaf-write block, which the hook skips when remap == nullptr. Every served layer therefore stops reporting the 'a selected expert read the ZERO slot' quality leak, and the teardown line (llama-expert-cache.cpp:2229) prints n_zero_mapped_selected=0 -- byte-identical to a run with no leak. The S1 A/B is documented as a pure scheduler/dispatch change; this monitoring change it carries silently is not in that description, and a gate that reads that counter would pass for the wrong reason. Note the code comment at llama-context.h:364 claims the opposite ('the leaf itself is still built when this is on'), which is how the trap survived review.", "counterexample_observed": "eng-20260915-210508-p25-slotgpu-l20-r1", "applies_to": ["src/llama.cpp/src/llama-graph.cpp", "src/llama.cpp/src/llama-context.cpp", "scripts/check/m123_oracle_gate.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0004", "class": "gate-integrity", "rule": "A drift check must test the property whose change it claims to detect, and must separate what is ALLOWED to change from what is not. A check that only tests existence and a check that reddens on every run are both ignored -- the second failure mode is the mirror of the first, and both are cured only by classifying the subject.", "because": "index_assets.py --check printed 'manifest OK: 56 assets, 0 drift' while MANIFEST.jsonl still carried scripts/check/decode_sweep.py at 33992 bytes / mtime 20:52:01 although the file on disk was 38127 bytes / 21:07:40. It only asked os.path.exists per recorded path, so editing any indexed file was invisible. Comparing bytes+mtime surfaced 18 real drifts at once -- but 3 of them were Backup/phase_decomp, Backup/cgc_logs and .workbuddy/memory/2026-09-15.md, i.e. the loop's OWN outputs, which change on every run by design; shipping only that half would have left the check permanently red and therefore trained the reader to ignore it. The fix needed both halves: compare bytes+mtime for source assets, and mark run outputs volatile (existence-only). Verified red-then-green: 18 drifts -> classify -> 12 drifts (the 6 genuine edits stay) -> regenerate -> 0 drifts.", "counterexample_observed": null, "applies_to": ["agent_harness/engine_loop/index_assets.py", "agent_harness/engine_loop/MANIFEST.jsonl"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0017", "class": "diagnosis", "rule": "A node is present only when the graph traversal AND the allocator can both see it: repointing a consumer at a different tensor orphans everything upstream of the old one, and ggml-alloc aborts the reserve build on a root that nothing consumes.", "because": "The CGC_S1_KEEP_LEAF control repoints mul_mat_id from the GPU gather (slots) to the host leaf. That silently turned CONT, GET_ROWS and the VIEW into orphans AND left slot_table -- still a graph root, still ggml_set_output -- with no consumer, so the scheduler never assigned it a backend and the process died at model load with ggml-alloc.c:623 GGML_ASSERT(buffer_id >= 0), stack ggml_gallocr_allocate_node <- ggml_gallocr_reserve_n_impl <- ggml_backend_sched_reserve <- llama_context::graph_reserve (llama_server_20260915_211705.log). Constructing a tensor does not put it in the graph; only ggml_build_forward_expand does. The fix is one line -- keep the chain live by expanding slots as well -- and it is the same lesson this investigation keeps meeting from a new side (set_output missing -> aliasing; index vector read late -> dead buffer; slot_table unconsumed -> unassigned backend): the graph is a traversal, and every assumption about who computes or holds a buffer has to be checked against that traversal, not against the source that created the tensor.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-graph.cpp", "src/llama.cpp/ggml/src/ggml-alloc.c"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0005", "class": "measurement-hygiene", "rule": "Do not let an aggregate comparison of a vector whose rows have different meanings stand in for the per-row comparison: one benign row's difference will read as total disagreement and hide the rows that agree.", "because": "The CGC-S1 post-sync readback prints a single `same=` bit for the whole id vector via memcmp. On every POST line it read same=0, which reads as 'the GPU gather disagrees with the host mapping at every layer, everywhere'. Split by token the picture is the opposite: token 0 agrees at all 39 layers across 6 samples each, and only rows >= 1 differ -- and those rows are padding whose difference is provably harmless (min_il=20 carries 20 layers of it and is bit-identical). The aggregate bit therefore supported the wrong conclusion and would have sent the investigation into the publish path for a defect that is not there. The general form: when the rows of a vector are not interchangeable (token 0 of a decode step vs a padding row, a real expert id vs a slot index), an aggregate equality test answers a question nobody asked.", "counterexample_observed": "eng-20260915-212050-p25-keepleaf-dbg-r1", "applies_to": ["src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0005", "class": "gate-integrity", "rule": "Before recording a switch's null result as a refutation, read the branch condition that consumes it: an env var tested with `getenv(...) != nullptr` selects on PRESENCE, so setting it to \"0\" (or to an empty string) takes the same branch as \"1\".", "because": "CGC_OA_ASYNC was compared with `!= nullptr` at ggml/src/ggml-backend.cpp:1745, and scripts/run_server.sh puts CGC_OA_ASYNC into the server env unconditionally, so there was no value -- not \"0\", not \"\" -- that selected the non-segmented path. The A/B pair built to test whether the segment ordering causes the S1 divergence therefore re-ran the segmented arms exactly: md5 sets {dc055e63} and {29ca694a, 672585db, b8c705cc}, pool counters misses 13040 / 14296 and file_reads 36360 / 39948, all digit-for-digit identical to the segmented run. The near-miss is the expensive part: the pair looks like a clean refutation of the ordering hypothesis and would have closed a live hypothesis on the strength of an experiment that changed no variable. This is the mirror image of the run_server.sh allowlist trap (an unlisted variable is silently DROPPED, so 'no effect' and 'not set' are indistinguishable); here the variable IS set and its value is IGNORED, so 'no effect' and 'the switch is not wired' are indistinguishable. The positive discipline that follows: a null result from a switch is evidence only once the switch has been shown to change something, and reading the consumer is the cheapest such demonstration -- cheaper than the run it is meant to justify.", "counterexample_observed": "eng-20260915-213356-p25-slotgpu-noasync-r1", "applies_to": ["src/llama.cpp/ggml/src/ggml-backend.cpp", "scripts/run_server.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0018", "class": "diagnosis", "rule": "A graph node needs a CONSUMER, not just a traversal path: a tensor that is reached as a root with nothing consuming it is never assigned a backend, and ggml-alloc aborts on it (`ggml-alloc.c:623 GGML_ASSERT(buffer_id >= 0)`) before the first token.", "because": "eng-diag-0017 established the weaker half of this (constructing a tensor is not putting it in the graph -- only ggml_build_forward_expand does). CGC_S1_BUILD_LEAF=1 exercised the other half and found that being in the graph is also not enough. The arm builds the host remap leaf and deliberately leaves it unconsumed, which is exactly what the header comment on cache_slot_table_tensors has always claimed S1 does ('the leaf itself is still built when this is on: it is simply not consumed ... a pure scheduler/dispatch change'). With `slots` neither consumed nor expanded, the GET_ROWS/CONT/VIEW chain drops out of the graph, `slot_table` is left alive only by the unconditional `ggml_build_forward_expand(gf, slot_table)` above it, and the new `remap` leaf is expanded too -- so the graph ends up with two roots that nothing reads. The server aborted in llama_context::graph_reserve, i.e. during context construction, having generated nothing. The practical form: 'build the node' is a sentence about the builder's intent; 'the node exists' requires a consumer, and a design that only satisfies the first cannot be measured at all -- so it must not be planned as the control for a hypothesis.", "counterexample_observed": null, "applies_to": ["src/llama.cpp/src/llama-graph.cpp", "src/llama.cpp/src/llama-context.h"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0006", "class": "measurement-hygiene", "rule": "An instrument that has only ever run on the CONTROL arm does not license any statement about the arm under test, however directly it addresses the question.", "because": "The per-layer readback that 'exonerated the ids' ran in p25-keepleaf-dbg, whose defining property is that mul_mat_id consumes the HOST LEAF -- so it verified the gather's output inside a graph that does not consume the gather. That is a real measurement, but it is about a configuration the real arm never enters, and the same instrument on the real arm costs one arm in one sweep: p25-slotgpu-dbg prints leaf=[n/a] on every POST line (there is no leaf to compare against) yet still agrees with the hook's EXPECT on all 39 layers x 6 builds, which is the statement the earlier run was being quoted for. The cheap generality: when a probe needs a control arm to have something to compare against, that very fact means the result does not transfer; run the probe on both and say which one the number came from.", "counterexample_observed": "eng-20260915-212050-p25-keepleaf-dbg-r1", "applies_to": ["scripts/check/decode_sweep.py", "src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0007", "class": "measurement-hygiene", "rule": "A build fingerprint must be discovered by glob over every shared library the run loads (`libggml*.dylib`, `libllama*.dylib`, the server binary), never from a hand-picked file list; a knob whose code lives in a library the fingerprint does not hash makes two genuinely different builds compare as one.", "because": "Today's gate fix lives in ggml/src/ggml-backend.cpp, which links into libggml-base.0.19.0.dylib (proved by build/ggml/src/CMakeFiles/ggml-base.dir/ggml-backend.cpp.o, not by inference). decode_sweep.py:33 and ab_interleave.py:42 hash exactly three files -- llama-server, libggml-metal.0.19.0.dylib, libllama.0.0.239.dylib -- so the pair taken at 21:34 (gate NOT yet fixed: both noasync arms bit-copied their segmented counterparts) and the pair taken at 21:41 (gate fixed: p25-slotgpu-noasync = {ff68c5a2}) carry the SAME fingerprint server=131bc5316ebf metal=9729ad35ddd1 llama=9066e1697a58. The project's own rule is 'compare only within one fingerprint', so the two runs were stamped comparable while their scheduler code differed. Secondary defect in the same function: the paths contain literal version numbers, so a version bump silently degrades the entry to the string \"missing\" instead of failing.", "counterexample_observed": "eng-20260915-213356-p25-slotgpu-noasync-r1", "applies_to": ["scripts/check/decode_sweep.py", "scripts/check/ab_interleave.py", "scripts/check/knifeedge_matrix.py", "agent_harness/CONVENTIONS.md", "agent_harness/engine_loop/README.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0008", "class": "measurement-hygiene", "rule": "Before quoting a probe as evidence about what a CONSUMER received, establish when the probe runs relative to the write it is measuring; a host-side read taken during graph encoding of a device-written tensor measures the buffer's pre-write occupant, so it tracks WHO writes the buffer, not whether the value is correct.", "because": "CGC-MMID-ASSERT reads ((int32_t *) op->src[2]->data)[i] inside the mul_mat_id ENCODE path of ggml-metal-ops.cpp. While src[2] was the host-written leaf that read was meaningful (id_oob=0/240); once src[2] became the device-computed gather output the same probe reported id_oob=16/16 at layer 1 and a cumulative 202000, with first=1063585220 (0x3F65.. = F32 0.90) and 1058604036 (0x3F19.. = F32 0.60) -- i.e. router probabilities, the previous occupant of a reused buffer. The same arm under the non-segmented dispatcher read id_oob=0/288. Both readings produced readable French, so neither number tracked the answer. The baseline's 0 is what makes this dangerous: it looks like 'the baseline is clean' and invites the reading 'S1 corrupts the ids'. The sound provenance fields (ids_name, ids_op, ids_data) were in the same log line all along, and they already answer the only question this probe can actually answer: host-written vs device-written.", "counterexample_observed": "eng-20260915-213356-p25-slotgpu-noasync-r1", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "scripts/check/decode_sweep.py", "docs/REMAP_ROUNDTRIP_REMOVAL_PLAN_2026-09-15.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0006", "class": "gate-integrity", "rule": "When a gate is changed from testing presence to testing value, re-derive the effective behaviour of EVERY configuration that set the variable, and restore the previous effective behaviour as the new default; verifying that one anchor digest is unchanged is not behavioural preservation, because the configurations that explicitly set the now-meaningful value are exactly the ones the fix cannot move.", "because": "CGC_OA_ASYNC was gated on `getenv(...) != nullptr` while run_server.sh unconditionally put the variable into SERVER_ENV, so the value was never read and every profile ran segmented -- including the four whose launcher default was 0. Making the gate value-aware at 21:36 therefore flipped off / prefill250 / qa-zh / longform-zh from segmented to non-segmented, i.e. from 6.95 t/s to 0.72 t/s in the same build (measured on the baseline arm pair, same md5, same pool counters). The anchor digest dc055e63 stayed identical throughout, and prod25 is precisely the one profile that sets the variable to 1 explicitly -- so the check that was run could not have detected the change it was standing in for. prefill250 is the profile the oracle gate and bench matrix run under, so the blast radius reached the verification chain.", "counterexample_observed": null, "applies_to": ["scripts/run_server.sh", "src/llama.cpp/ggml/src/ggml-backend.cpp", "scripts/check/decode_sweep.py", "scripts/check/m123_oracle_gate.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0007", "class": "gate-integrity", "rule": "A bit-identical gate is only meaningful for a graph that is itself reproducible: before quoting 'candidate == reference' as a result, establish that the same arm, build and generation length reproduces one and the same digest over N rounds. For a run-to-run unstable arm the gate compares the candidate against one arbitrary sample of the reference, so a PASS is a coincidence and the gate has no discriminating power.", "because": "On this project the neutral-baseline arm p25-gputime is stable at every length (N=24 -> dc055e63 across every sweep and every rebuild; N=40 -> 3d8fa55f; N=100 -> 28097996), while the S1 arm p25-slotgpu under the segmented dispatcher produced THREE different digests in three rounds of one sweep on one build (29ca694a / 672585db / b8c705cc) at N=24, and produced eight further distinct values across the sweeps at other lengths. Under the non-segmented dispatcher the same arm collapses to a single stable ff68c5a2. So the same gate would be deterministic on one arm and a coin flip on the other, with no change to the gate. The failure mode is therefore silent in the direction that matters: the reference dump is a single sample, so an unstable candidate that happens to sample the reference value once reports PASS. Note also that three digests of the baseline arm that look like dispatcher sensitivity are actually generation-length effects -- md5 is only comparable within one build fingerprint AND one predicted_n, which is why the earlier 3d8fa55f/28097996 readings must not be attributed to the dispatcher.", "counterexample_observed": "eng-20260915-213356-p25-slotgpu-noasync-r1", "applies_to": ["scripts/check/m123_oracle_gate.py", "scripts/check/decode_sweep.py", "scripts/check/knifeedge_matrix.py", "agent_harness/CONVENTIONS.md", "agent_harness/engine_loop/README.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0008", "class": "gate-integrity", "rule": "A comparability stamp is only as behavioural as the gate that wrote it: once a gate stops testing presence and starts testing value, every sidecar produced under the old gate records an INTENT rather than an observation, so its knob values are unverifiable metadata. Decide the question with the measurement that is independent of the stamp -- bit-identical logits -- then repair the stamp; do not disarm the check with --allow-incomparable.", "because": "ref_iq3_pool8gb_M2_6144_bitident.jsonl was dumped 2026-09-15 17:21 against the presence-based CGC_OA_ASYNC gate, so its .cap reads CGC_OA_ASYNC='0' even though that run used the segmented dispatcher -- at the time 0 and 1 selected the same branch because run_server.sh always exports the variable and the gate only asked whether it existed. After the 21:36 value-aware fix and the launcher default restored to 1, prefill250 resolved to 1 and the gate reported INVALID COMPARISON (ENV.CGC_OA_ASYNC: ref='0' now='1') on EVERY run, i.e. the comparability check that exists to prevent cross-config verdicts had gone dark for exactly the profile the gate runs under. The stamp was stale, not the run: M1 was 9/9 bit-identical against the old reference, and the non-segmented path lands on ff68c5a2 rather than the anchor dc055e63, so both sides provably ran the segmented path. Re-baselining produced a byte-identical dump (md5 d2f0b9a01fc5404ebb04dcf3583e133f for v2 and v3 alike), which is what makes correcting the sidecar a repair rather than a moved goalpost.", "counterexample_observed": null, "applies_to": ["scripts/check/m123_oracle_gate.py", "scripts/run_server.sh", "scripts/check/llama_bench_matrix.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0009", "class": "measurement-hygiene", "rule": "Read an instrument's call graph and its guards before specifying or trusting it. 'How often does this mechanism run' answers whether the mechanism EXISTS, never how often the STATE moves; and a probe that is never allowlisted has never been run through the launcher at all, so its verdict is a no-op mistaken for a negative result. When you replace a degenerate instrument, check the replacement for the same two defects -- it is usually degenerate the same way.", "because": "Three instruments were checked before writing one line of the measurement D3 depends on, and all three failed on one of those two axes. (1) The instrument §9.17(d) specified -- publish_slot_table call count per decode step -- is degenerate by construction: both of its call sites are inside the S1 remap hook itself (llama-context.cpp:5448 and :5584) and the header comment says it is 'reached only from the S1 path', so the answer is 0 on the baseline arm and once per served layer on S1. It measures mechanism presence, not state velocity -- exactly the defect §9.17(d) had already diagnosed in the CGC-SLOT-TABLE clamp count it was supposed to replace, repeated by the replacement. (2) The cheapest existing churn proxy, LLAMA_EXPERT_CACHE_STEP_DBG, had never been in run_server.sh's allowlist, which is an explicit list passed through `env \"${SERVER_ENV[@]}\"` rather than a prefix rule; so the diagnostic quoted in-code as the source of the 2026-08-28 'structural churn' verdict could not be enabled through the launcher, and every attempt since was a silent no-op indistinguishable from 'the change had no effect' -- the trap the same file already documents for NOHOOK/NOGATHER. (3) With that fixed and verified (dry run shows ENV LLAMA_EXPERT_CACHE_STEP_DBG=1 reaching the launch line), the probe still produced zero lines on a prod25 (MTP off) decode, because its block sits inside `if ((verify_fast || draft_fast) && cgc_fast_eligible)` and every step reported `CGC-WARM ... fast=0`. The zero was a dead end, not a zero churn: the instrument's guard, not the phenomenon, decided the reading.", "counterexample_observed": "A zero line count that reads as 'no churn' while the block was in fact never entered -- the same shape as the CGC-SLOT-TABLE 0-line count and as CGC_IDS_CAPTURE before it was allowlisted.", "applies_to": ["scripts/run_server.sh", "src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/src/llama-expert-cache.cpp", "docs/REMAP_ROUNDTRIP_REMOVAL_PLAN_2026-09-15.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0010", "class": "measurement-hygiene", "rule": "Before attributing ANY single-shot throughput difference, measure the repeatability floor of the SAME binary on the same machine: run it twice and take the spread. If the effect you are attributing is smaller than that spread, the comparison has no discriminating power and the correct output is 'not resolvable', not 'direction unknown'. An instrument whose noise exceeds the signal is not a weak instrument, it is a non-instrument.", "because": "§3 of the whitepaper listed a 6-12% deficit of the rebuilt binary against an 18:00 run of an older one, honestly flagged as unquotable for want of an interleaved A/B -- a framing that implicitly assumed the gap needed explaining. Two runs of the IDENTICAL binary 34 minutes apart (22:30 and 23:04, no rebuild in between: libggml-metal and llama-server both timestamped 22:14) produced tg@0 8.67 -> 6.91 (-20.3%), pp@1024 79.88 -> 105.71 (+32.3%), pp@4096 81.52 -> 103.11 (+26.5%) and tg@512 6.72 -> 8.85 (+31.7%), with per-cell ± as large as the effect (pp@512 ±12.65 on 103.25). So the quantity §3 was comparing across builds moves by up to 32% with nothing changed at all, which makes the 6-12% not merely unquotable but unmeasurable by that method. The two runs also disagree on pool behaviour (hit 97.1%/519501 reads vs 97.3%/16163 misses), so it is not only the timing that drifts between launches.", "counterexample_observed": "The same binary, unmodified, two consecutive runs: tg@0 8.67 vs 6.91 t/s.", "applies_to": ["scripts/check/llama_bench_matrix.py", "docs/PREFILL250_DECODE25_WHITEPAPER_20260915_2240.html", "agent_harness/CONVENTIONS.md", "scripts/check/ab_interleave.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0009", "class": "gate-integrity", "rule": "A gate quantity computed at one call site and DISCARDED at another is not a gate quantity. Enforce it by funnelling every site through one counted entry point, not by adding a counter and hoping each caller reads it -- the caller that does not read it is exactly the caller the gate exists for.", "because": "llama_expert_cache_publish_slot_table returns the number of entries it had to clamp to 0 because the layer has no reserved ZERO slot, and §8.3 identified that count as the signal of a SILENT correctness failure: with zero_slot < 0 a non-resident selected expert makes the host leaf write -1 (loud -- the consumer reports an out-of-range id) while the table clamps it to 0 (index 0 is legal, so mul_mat_id reads a DIFFERENT expert's weights). There are exactly two call sites. The fast-path site checked the return value and printed it. The pool-path site discarded it outright -- and the pool path is the one EVERY MTP-off S1 arm actually takes, because CGC_SERVER_MTP=0 makes verify_fast and draft_fast both false so the `(verify_fast || draft_fast) && cgc_fast_eligible` guard is never entered. Every S1 measurement to date therefore computed the silent-corruption counter and threw it away. A second, independent divergence sat in the same pair: the two sites write the LEAF under different mappings (slot_table_safe vs the raw slot table) while publishing the TABLE through the same function, so their table/leaf equivalence is asserted, not shared.", "counterexample_observed": "A counter that is computed on every publish of the arm being measured, and read by nobody on that path.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/src/llama-expert-cache.h", "src/llama.cpp/src/llama-expert-cache.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0007", "class": "source-reading", "rule": "An edit tool's success message is not evidence the edit is in the tree, and edits issued in PARALLEL against one file silently lose writes. After every batch of edits to a single file, verify with `git diff -U0 -- <file> | grep '^@@'` plus a targeted grep for the removed strings; never stack a build on top of an unverified edit.", "because": "Three Edit calls were issued in a single message against llama-context.cpp (remove cgc_s1_tag() from the L4 identity map, remove it from the gather identity map, and drop the two guard terms that tested it). All three returned 'Successfully edited file'. Grepping the file afterwards found the first two strings still present at lines 5145 and 5966 while the third was gone -- one landed, two were silently dropped, and the reports were identical. Compounding it, the same run had removed the DEFINITIONS of cgc_s1_tag()/cgc_s1_ident() earlier, so the file was left referring to functions that no longer existed: a state that cannot compile, and that no success message would have revealed. This is the second occurrence of the class in one session -- an earlier attempt to add the ffn_moe_down capture and rewrite the fast-path call site also reported success without writing bytes, caught only because the string `CGC-S1: OUT` was found inside the built dylib while never appearing in any log. The distinctive signature is a probe that is compiled in and provably never fires.", "counterexample_observed": "Three 'Successfully edited' returns on one file: one landed, two absent from disk, and the file left calling deleted functions.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/src/llama-expert-cache.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0011", "class": "measurement-hygiene", "rule": "A capped diagnostic readback must be gated on the PHASE that contains the question, not merely on the knob that arms it. If the probe is counter-limited, the first N graphs of a process are warmup and prefill; numbering 'graph 3' across the whole process addresses a graph where the mechanism under test may not even be running. Gate on the phase (batched vs not) and print the ordinal inside that phase.", "because": "The ffn_moe_down output capture was first written with `static int cgc_s1_out_n = 0; if (cgc_s1_out_n < 6)` around a loop that dumps every captured layer. graph_compute is called for every graph, so that budget is consumed by the first six graph computes of the process -- warmup and prefill chunk processing -- before the first decoded token exists. Prefill takes a different branch of the expert-cache hook entirely (it builds no remap leaf; the hook itself checks n_tokens), so the arms would have been compared at a graph where the S1 mechanism under test is not engaged, and the comparison would have reported 'identical' for a reason that has nothing to do with the mechanism. The fix is one line of predicate: the second parameter of graph_compute is `batched` (= ubatch.n_tokens > 1), so `if (cgc_s1_outcap && !batched)` both spends the budget on decode graphs and makes the printed ordinal an honest address within the phase being discussed.", "counterexample_observed": "A 6-graph readback budget spent entirely on warmup and prefill, in a probe whose stated purpose was to compare one decode graph between two arms.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0012", "class": "measurement-hygiene", "rule": "A probe that must perturb the allocator to read its value has a cost measured in PINS PER GRAPH, not in bytes, and that cost can only be found by experiment. Do not derive the safe budget from tensor dimensions, and do not creep up on the limit: the failure mode is a fail-stop abort at load, not a slowdown.", "because": "CGC_S1_OUT_CAP pins ffn_moe_down with ggml_set_output so a post-synchronize read sees a live buffer. The first version pinned every layer in every graph and both arms died at the load-time warmup decode with kIOGPUCommandBufferCallbackErrorOutOfMemory -> CGC_METAL_FAIL_STOP abort (2/2), while the control arm without the knob started and decoded at 20.2 t/s in the same session. The obvious explanation -- bytes -- was written into the code comment as '2048 x 6144 x 4B = 50 MB per layer, ~2 GB for 40 layers' and it is WRONG in the direction that matters. Corrected, the boundary is: 40 layers on every graph dies; 11 layers on prefill only ALSO dies; 4 layers on prefill runs; 40 layers on decode runs. At the graph that dies the hook reports ntok=2, so ffn_moe_down is [2048, 8, 2] F32 and eleven pinned layers hold 11 x 131 KB = 1.4 MB. A megabyte and a half does not exhaust a Metal working set, so the controlling variable is the COUNT of tensors marked as graph outputs -- which changes the scheduler's allocation and split -- and it is non-monotone in the phase (prefill chunks are excluded, yet prefill-only pinning is the variant that dies). Two consequences worth keeping: the safe number is empirical (between 4 and 11 here), and a byte budget would have been wrong twice, once as 2 GB and once as 5 MB.", "counterexample_observed": "Eleven pinned tensors totalling 1.4 MB abort a 16 GB machine's Metal command buffer, while forty pinned tensors on the same tensors abort too -- and forty pinned tensors on a different phase do not.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/src/llama-context.h", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0019", "class": "diagnosis", "rule": "If a difference reproduces with the SAME structure at two DISJOINT slices of the axis you suspected, that axis is not the variable -- stop deepening it and look for a property of the container instead. Two windows that agree on 'which items differ' rather than 'the first item that differs' have already answered the question.", "because": "The S1 divergence had been chased through the layer axis: the decode capture reported 480/480 layers differing, including layer 0, so the natural next move was to walk deeper layer windows to find the first bad layer. That ladder was uninterpretable from the start -- decode step 0's layer-0 input descends from the prefill's KV and the two arms' prefills already disagree (M1 5/9), so every decode layer is downstream of an earlier divergence, and there is no 'identical input' origin at which a first divergence can be defined. Re-running it where the input IS identical by construction (prefill, layer 0 = embedding) produced the answer: probing layers 0-3 and then layers 4-7 (disjoint, 8 layers, 6 graphs, 2 arms = 96 pairings) returned the SAME structure both times. Crucially the two rungs did not agree on which layer first diverges -- they agreed that divergence tracks a property of the GRAPH, not of the layer: slab-served graphs (256/256 experts resident) were bit-identical across all 8 probed layers, pool-served graphs (143/256) diverged from the first pooled layer on. The predictor was verified against an independent runtime marker, `CGC-PREFILL-STREAM: il=0 kind=0 ntok=182`, which appears on exactly the two all-EQ graphs. The cost of the misdirection was real: the first capture attempt pinned all 40 layers in every graph and killed the process at load (see eng-mh-0012), and two layer windows were measured before the orthogonal property was noticed.", "counterexample_observed": "A layer ladder measured twice, 0-3 and 4-7, whose two windows agreed on the divergence structure rather than on a first-divergent index -- the signature that the layer index was never the variable, while the analysis was still framed as 'find the first bad layer'.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "scripts/check/decode_sweep.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0010", "class": "gate-integrity", "rule": "An env var whose READER tests presence (`getenv(...) != nullptr`) is a trap when its documented OFF value is the string \"0\": setting it to 0 turns the feature ON. Before citing any knob's value from a resolved-config record, grep the readers and check whether they test presence or value -- and never use a presence-gated knob's recorded value as evidence of behaviour.", "because": "The profile at run_server.sh:939-945 sets LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0=0 under a comment dating from 2026-09-09 whose entire purpose is to bring blk.0 BACK into the pool ('已證實 4/10 品質殺手 ... 甜蜜點 ... layer 0 在 pool 內) 無 skip0 = 90-100%'), and :1493 hands the array to the child as real environment variables via `env \"${SERVER_ENV[@]}\"`. All three readers of that variable test presence -- llama.cpp:396, llama-expert-cache.cpp:1708, llama-context.cpp:5222 -- so \"0\" is non-null, the gate is TRUE, and blk.0 has been excluded from the pool the entire time: llama-model-loader.cpp:1464 forces l4_kind = -1 for layer 0 and its FFN reads the full-width tensor with raw ids. The runtime confirms it independently: CGC-DECPROF reports layers=39 on all 19 steps of a log and layers=40 on none. The consequence is that a documented quality fix has been inert for six days, and -- separately -- that the resolved-config sidecars (gate caps, llama-bench json) all record `LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0: \"0\"`, i.e. they record an INTENT while the engine implements its opposite. This is the same disease as the stale oracle cap (dec-20260915-2233) and it is the third member of the presence-vs-value family in this project after CGC_OA_ASYNC (dec-20260915-2142, whose value-aware fix silently re-routed four profiles and forced a gate re-baseline). Presence-gating is not always wrong -- it is how this project spells 'on' for many diagnostics -- but it makes the pair (0, unset) mean the same thing, so any profile that writes `=0` expecting the feature off is writing a no-op.", "counterexample_observed": "LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0=0 in the production profile, with all three readers testing presence, running for six days with the feature ON and the profile comment asserting the opposite; the only signal that anything was wrong was CGC-DECPROF saying layers=39.", "applies_to": ["scripts/run_server.sh", "src/llama.cpp/src/llama.cpp", "src/llama.cpp/src/llama-expert-cache.cpp", "src/llama.cpp/src/llama-context.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0011", "class": "gate-integrity", "rule": "A fix that changes what a config value MEANS while leaving the string unchanged is invisible to a resolved-config comparability check, because that check is a string diff. It will report a plain FAIL that is a category error, not a regression. So: (a) re-baseline deliberately and retire the old reference, and (b) run the PRE-fix value as a control arm first -- it must reproduce the old reference bit-identically, which is simultaneously the proof that the old reference was dumped under the old behaviour and that the predicate change is numerically inert in every other dimension.", "because": "LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0's three readers were switched from a presence test to value semantics on 2026-09-16. Both the pre-fix and post-fix resolved configs contain the literal string \"0\" (the profile writes =0 either way), so m123_oracle_gate.py's comparability precondition -- which diffs the resolved ENV of reference and fresh dump -- sees ZERO differences and proceeds to a normal verdict. Measured: the control arm (CGC_SERVER_SKIP0=1, i.e. the pre-fix behaviour under the new binary) reproduced ref_iq3_pool8gb_M2_6144_bitident_v3.jsonl bit-identically (M1 9/9, M2 9/9, M3 9/9, full-row fnv1a64 9/9), while the new default (=0, layer 0 pooled) scored M1 4/9 / M2 9/9 / M3 6/9 with cross-tab {same/same: 4, diff/same: 5, diff/diff: 0}. Without the control arm the 4/9 would have been indistinguishable from 'the fix broke something'. This is the opposite failure mode to the CGC_OA_ASYNC round (dec-20260915-2215), where the resolved string itself moved (CGC_OA_ASYNC 0 -> 1) and the gate raised INVALID COMPARISON on its own, waking someone up. Value-semantics changes get no such alarm, so the re-baseline has to be a declared act rather than a detected one.", "counterexample_observed": "Two runs with byte-identical resolved env strings (LLAMA_EXPERT_CACHE_L4_SKIP_LAYER0=0 in both) that are nonetheless not comparable: one dumped 39 pooled layers, the other 40. The gate's own re-baseline hint ('A cross-config diff is NOT a regression') never fires, because there is no cross-config diff to report.", "applies_to": ["scripts/check/m123_oracle_gate.py", "Backup/knifeedge_matrix/ref_iq3_pool8gb_M2_6144_bitident_v4_skip0off.jsonl", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0012", "class": "measurement-hygiene", "rule": "A reading that is identical in every arm cannot be evidence about which arm you are in. Before using a log line to prove a state change, check that it actually varies with that state -- otherwise you are quoting a planned/target value as a measurement.", "because": "The 2026-09-09 sweet-spot log was read as '5976 slots = layer 0 in the pool', and 5976 was treated as a fingerprint of the pooled-40 configuration. Measured 2026-09-16: `llama_expert_cache: LAYER_CAPS per-layer caps: total 5976 slots (avg 145.8/layer, min 143/layer)` is printed IDENTICALLY in the skip0=1 arm (39 pooled layers) and the skip0=0 arm (40 pooled layers), because it is the planned sum of LAYER_CAPS (40 x 143 + 256 for the MTP layer) and does not consult the pooled-layer set at all. What actually discriminates the two arms is the teardown pool-integrity scan: owner-set slots 5793 -> 5935 (a delta of exactly +142, one layer's worth) and zero-regions 0 -> 4 (all four in layer 0, first at layer=0 slot=18 kind=0 owner_expert=252). Same disease as eng-gate-0010: a recorded value read as behaviour.", "counterexample_observed": "`total 5976 slots` identical across two arms that differ by exactly one pooled layer, with 2026-09-09 treating that same number as proof that layer 0 was pooled.", "applies_to": ["docs/PREFILL250_DECODE25_WHITEPAPER_20260915_2355.html", "agent_harness/CONVENTIONS.md", "docs/SKIP0_VALUE_SEMANTICS_20260916_0021.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0013", "class": "measurement-hygiene", "rule": "A launch configuration must be read by DUMPING it under the configuration you care about, not inferred from the default. Capture the resolved env as a normalised fingerprint so that 'the launch environment did not change' becomes a checkable claim -- and normalise correctly, or the instrument reports noise as change.", "because": "run_server.sh's CGC_LOOP_GUARD push sat inside the else-branch of the CGC_SOFT_POOL_L1 test (a missing `fi`), so setting CGC_SOFT_POOL_L1 -- exactly what the comment 6 lines above recommends ('opt back in with CGC_SOFT_POOL_L0=48 CGC_SOFT_POOL_L1=48') -- silently dropped CGC_LOOP_GUARD from the env handed to the server, and CGC_LOOP_GUARD=1 could not restore it because 'push the guard' lived in the branch that only runs when L1 is UNSET. Measured with CGC_DUMP_ENV=1 across four inputs: default -> pushed, L0=48 -> pushed, L1=48 -> DROPPED, L0=48 L1=48 -> DROPPED, plus L1=48 CGC_LOOP_GUARD=1 -> still DROPPED (the earlier 'L0+L1 keeps it' reading was a zsh quoting artifact of my own probe, not a property of the script). No default profile sets L1, so reading the default would never have exposed it. The fingerprint then carried the argument: the normalised dump md5 for prefill250 stabilised at e68a5dc537d626622a9af6c9c7ddf665 once the two volatile line classes were removed, and was IDENTICAL before and after the fix -- which justifies 'this change cannot move the gate numbers' without re-running the gate to find out. Raw (un-normalised) md5 is not stable: prefill250 differed on consecutive runs (8726fb39 vs f898bc94) while prod25 happened to match itself, i.e. a naive fingerprint is wrong in both directions.", "counterexample_observed": "The dump's own volatility: `CGCENV LOG .../llama_server_YYYYMMDD_HHMMSS.log` carries a timestamp, and the `[preflight] free=NN%` / `[guard] ... free=NN%` lines track memory pressure. Without filtering both classes, 'stable' and 'changed' are both unreliable verdicts -- and unfiltered, two profiles in the SAME run set disagreed about which one was stable.", "applies_to": ["scripts/run_server.sh", "agent_harness/CONVENTIONS.md", "docs/PRECOMMIT_HOOK_GUIDE_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0013", "class": "gate-integrity", "rule": "Before analysing whether a setting is missing from a resolved configuration, check the control flow that emits it. A setting can be absent because of an if/else nesting error rather than because a default is off -- and the two are indistinguishable from the resolved output alone, while requiring opposite fixes.", "because": "CGC_LOOP_GUARD was absent from the launch env whenever CGC_SOFT_POOL_L1 was set. That is NOT 'guarded by a default of off': the guard block had been swallowed into the L1 else-branch, so the knob was UNREACHABLE in that configuration, and CGC_LOOP_GUARD=1 could not turn it back on. Evidence: grep for LOOP_GUARD in run_server.sh returns exactly one push site (lines 1388-1390), which is textually inside the L1 else-branch, closed by a doubled `fi`; and the measured 4-input table (default / L0 / L1 / L0+L1) matches the nesting prediction rather than a default-value prediction. Why the distinction is expensive: the wrong reading ('it is just off by default') leads to 'pass the flag' -- which is precisely the action that does not work here. The same day's presence-gated defect is the mirror image: there the value lied while the structure was fine; here the structure lies while the value is ignored.", "counterexample_observed": "`CGC_SOFT_POOL_L1=48 CGC_LOOP_GUARD=1` -- a user explicitly requesting the guard ON -- produced no CGC_LOOP_GUARD in the env, because the request and the emission live in mutually exclusive branches of the same if. The knob was not defaulted off; it was unreachable.", "applies_to": ["scripts/run_server.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0008", "class": "source-reading", "rule": "A path returned by a directory-resolving tool is expressed relative to THAT tool's working directory, not yours. Anchor every cross-directory path to your own base before using it. A relative return that is perfectly correct inside the tool's own cwd silently mis-targets everywhere else -- and the failure can be a total no-op that still prints success.", "because": "`git -C <main-worktree> rev-parse --git-path hooks`, run from a DIFFERENT cwd, returns `.git/hooks`. That value is not wrong, it is incomplete: it omits its base. Substituting it for a previously-absolute value (--absolute-git-dir) in check_build_tracked.sh --install-hook therefore looked like a one-line fix while actually relocating the write target to whatever the shell's cwd happened to be. In this repo the shell cwd is a linked worktree, where `.git` is a FILE, so `mkdir -p` / `cat >` / `chmod +x` all failed and the script still printed \"installed pre-commit hook: <path>\". Measured: `ls -ld <linked-worktree>/.git/hooks` -> \"Not a directory\". This is a strictly worse failure mode than the bug being fixed: the original wrote to a real directory that git does not read (recoverable by moving one file), whereas the naive fix writes nothing at all while reporting success (nothing to move).", "counterexample_observed": "`HOOK_DIR=\"$(git -C \\\"$REPO_ROOT\\\" rev-parse --git-path hooks)\"` followed by `cat > \"$HOOK_DIR/pre-commit\"` -- the literal reading of the approved one-line change. From the main worktree the resolver returns the relative `.git/hooks`; from a linked worktree the shell cwd resolves it onto `<linked-worktree>/.git/hooks`, where `.git` is a regular file. mkdir/cat/chmod each fail, the script prints its success line, and exit code stays 0. Both the write target and the error are invisible.", "applies_to": ["scripts/check_build_tracked.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0014", "class": "measurement-hygiene", "rule": "A judge that re-uses the exact window width of the alarm it is judging can only confirm that alarm's premise -- it can never test it. When a confirmation step returns the same verdict as the alarm every single time, check the two are not reading the same bytes before treating the agreement as evidence.", "because": "Both zero-row instruments (ggml-metal-ops.cpp CGC-MMID-ASSERT `zero_row=`, and the teardown `pool integrity: ... zero-regions=`) decide 'the fill dropped this expert' by scanning the FIRST 4096 BYTES of an expert row for non-zero bytes. scripts/check/mmid_zero_row_triage.py -- the tool written to adjudicate them -- re-read the same expert row at the SAME 4096-byte width, so for any row whose first 4 KiB is zero it could only return MODEL-ZERO. Its recorded verdict '14 MODEL-ZERO / 0 ENGINE-ZERO' was structurally guaranteed, not measured. Reading the WHOLE row stride (335872 B for blk.*.ffn_{gate,up}_exps, confirmed equal to the engine's nb02 in the log) shows the file truth: 10 of 31488 gate/up/down rows have a zero leading run of 4592-13120 B (a whole number of 656-B quantised row lines) while being 17.8-47.0% non-zero overall -- layer 0 experts 25/195/205/252 and layer 1 expert 214, gate+up in each case -- and every one of the other 31478 rows has a leading zero run of exactly 0. So the pool region being zero there is REQUIRED by the checkpoint, and both alarms were false positives of the same shared blind spot.", "counterexample_observed": "After the probe was widened, the engine's own new counter agrees: `zero_row=1 (first_id=214) | zero_full=0` and `pool integrity: owner-set slots=5935 zero-regions=0 zero-prefix-only=4`. The same run that used to report 4 dropped experts now reports 0, with no behavioural change anywhere (gate M1/M2/M3/fnv1a64 = 9/9 vs v4, launch-env fingerprint unchanged at e68a5dc537d626622a9af6c9c7ddf665).", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "src/llama.cpp/src/llama-expert-cache.cpp", "scripts/check/mmid_zero_row_triage.py", "scripts/check/gguf_dead_expert_census.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0015", "class": "measurement-hygiene", "rule": "A memory-pressure counter is not a measurement of the resource the engine depends on. `Pages free` moves with page-cache size (large cache -> low free -> FAST) and with other-process footprint (large anon -> low free -> SLOW) in opposite directions, and reporting their sum averages the two into a number that predicts nothing.", "because": "Tested on prefill250 pp2048 @ -ub 6144: five independent processes gave 286.67 / 173.54 / 155.62 / 166.06 / 166.63 t/s with rho(usable%, t/s) = -0.70 -- the run with the LEAST free memory was the only one to clear 250. The instrument had recorded `free + inactive`; measured in the same session, free was 5.94 GiB while the actual page cache (File-backed pages) was only 3.33 GiB and anonymous was 3.82 GiB. Re-run with the page cache recorded explicitly and FORCED up 4.3x on three of six runs (2.32 -> 9.96 GiB, free driven to 0.06 GiB): rho(cache, t/s) = +0.09, and the whole batch landed on the same 155-184 band. So the correct variable was found and it also does not explain the spread.", "applies_to": ["scripts/check/prefill_certifiability.py", "scripts/check/llama_bench_matrix.py", "scripts/run_server.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0016", "class": "measurement-hygiene", "rule": "With one outlier in N runs, the honest output is the REPEATABLE BAND, not the range and not the target. The range only says an outlier exists; the target only says it was reached once. Quote the cluster, and count how many runs cleared it.", "because": "250 t/s was reached (286.67, and 254.74 / 264.78 / 276.59 on 09-14/15) and so was 122.68 -- 1/11 runs cleared 250 across two batches of identical commands. The informative reading is that ten of eleven runs sit in 155.33-184.09 (18.5%), i.e. the engine's operating point is a stable ~170 t/s band and 250 is the outlier, not the other way round. Reporting 'range 155-287' would have suggested a wide but centred distribution; reporting '1/11 cleared 250, band 155-184' names the operating point and prices the gap at 1.36x.", "applies_to": ["scripts/check/prefill_certifiability.py", "docs/PREFILL250_CERTIFIABILITY_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-bound-0002", "class": "honest-bounds", "rule": "Closing a defect in a MEASUREMENT INSTRUMENT is not the same as clearing an obstacle on a PERFORMANCE path, and must never be reported as 'the road is now clear'. An instrument defect being real does not make it load-bearing.", "because": "After the layer-0 zero-region work closed (the probe was 4096 B wide against a 4592-13120 B zero prefix, i.e. a probe-width bug in the triage tools), the summary said '250/25 has nothing blocking it now'. Three errors: (1) that work was never on the 250/25 path at all -- the road was equally 'clear' before it started, and 250/25 was still not done; (2) the road's declared #1 dependency M1/W3 is not unimplemented but 'implemented, measured, and rejected' (docs/M1_POOL_SPLIT_COST_2026-09-14.md), with two blockers each carrying an exact failure signature -- and its Blocker B lives in the very bookkeeping (slot_owner / slot_table / batch_owned) that the zero-region work had just been reading, so the session's work was ADJACENT to the obstacle, not past it; and (3) prefill250 and prod25 are different configs competing for the same 16 GB (ctx 8192 + ubatch 6144 vs ctx 4096 + model-default batch, shared 8 GiB pool), which was not mentioned at all.", "applies_to": ["docs/M1_POOL_SPLIT_COST_2026-09-14.md", "docs/ROADMAP_PREFILL250_DECODE25_2026-09-13.md", "docs/PREFILL250_CERTIFIABILITY_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0017", "class": "measurement-hygiene", "rule": "一個離群值，先問它是不是『這個 session 的第一次啟動』。啟動序與距上次載入的閒置秒數是必填的協變量，不是背景資訊。", "because": "11 次獨立啟動裡唯一越過 250 的那次（286.67）被判為抽獎，但換一次 session 就複現：三次『閒置後第一次啟動』= 286.67 / 293.11 / 299.35 t/s，而同一批次後續啟動 = 163–184；對照組同樣成立——緊接前一系列之後 5 秒啟動（exp2 run 1）落在慢帶（179.09）。判別量因此不是『第幾次』而是『距上次 GPU 負載結束多久』。把這個協變量漏掉，一個確定的熱暫態就會被讀成隨機抽獎。", "counterexample_observed": "前兩輪（exp1/exp2，共 11 次啟動）把 286.67 記成『1/11 的離群』，並據此把目標差距定價成 1.36×；實際上它是冷態的操作點。", "applies_to": ["scripts/check/prefill_certifiability.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0018", "class": "measurement-hygiene", "rule": "對『累積平均』型態的儀器輸出，只能取最後一行 × 它的 n；逐行加總沒有意義。", "because": "`CGC-SEG` 印的是 `w_us/n`（累積平均，數值會隨 n 上升而下降）。第一版 harvester 把 13 行印出來的平均值全部加起來，得到 0.54 s；同一批 log 的正確總量是 30.66 s（差 57×）。錯誤值的方向是『讓等待看起來不存在』，正好會把 GPU 瓶頸誤判成沒有瓶頸。", "counterexample_observed": "prefill_certifiability.py 的 harvest_segments 第一版：seg_wait_us=544570 （把累積平均加總），實際 seg_wait_s=30.66。", "applies_to": ["scripts/check/prefill_certifiability.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0019", "class": "measurement-hygiene", "rule": "實驗腳本若用固定的 arm 名當 log 檔名，N 次啟動會互相覆寫，證據在測完之前就消失；每次啟動必須有自己的 workdir。", "because": "llama_bench_matrix.py:295 寫 `llama_bench_<arm>.stderr.log`，而 tag 就是 arm 名。01:27-01:42 的 11 次啟動全部指向同一個檔名，所以 286.67 那次的逐段計數（slab pool/disk、seg wait、prefill_fills）在需要它的時候已經不存在——整個逐段分解被迫重跑一輪，而重跑要 6 分鐘且不保證再抓到快的那次。", "counterexample_observed": "exp1/exp2 的 11 次啟動只留下最後一次的 stderr；286.67 的 log 無法復原。", "applies_to": ["scripts/check/prefill_certifiability.py", "scripts/check/llama_bench_matrix.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0009", "class": "source-reading", "rule": "`CGC-PHASE`（env `CGC_PHASE_TIMING`）只在 `ubatch.n_tokens == 1` 累加，所以它在結構上無法量 prefill。（本條原本還斷言「`CGC-DECPROF` 這個字面在原始碼樹裡不存在」——**那是錯的**，見 `eng-src-0011`；成因是 grep 只掃了 `src/llama.cpp/src/`，漏掉 `ggml/src/`。）", "because": "llama-context.cpp:1723-1724 的註解自己寫 `batched (prefill) steps are skipped`，而累加區塊被 `if (ubatch.n_tokens == 1)` 包住（:1891）。任何要拆 prefill 的人如果照文件去找 `CGC-DECPROF`，會先找不到名字，再以為是環境沒設好。 本條的有效部分只有 CGC-PHASE 那半句；prefill 的替代儀器是 `CGC-GPUTIME`（`CGC_GPU_TIMING=1`）、常駐的 teardown 統計行，以及 `ioreg -c IOAccelerator` 的 Device Utilization %。", "counterexample_observed": "本輪一開始即被指派用 `CGC-DECPROF` 拆 prefill；CGC-PHASE 確實不吃 batched step，但 `CGC-DECPROF` 另有其人且存在。", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/ggml/src/ggml-backend.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": "eng-src-0011"}
{"type": "lesson", "lesson_id": "eng-src-0010", "class": "source-reading", "rule": "`read shape:` / `effective_rate` 不是這條路徑的 IO 量度：`n_read_bytes` 只在池路徑累加，而整層 slab 的 `pread`（llama-expert-cache.cpp:3351）一個計數器都不加。", "because": "`fill_job`（:41）有 n_reads+pread_usec 但沒有 n_read_bytes；池路徑（:2657,:2671）三者都有；`fill_slab` 的 contiguous `pread`（:3351）三者也許都沒有。後果實測：一份 178 GiB slab讀取的 log 印出 `bytes=0 (0.00 MiB/job) effective_rate=0 MiB/s`，而另一份印出`1 MiB/s`——這兩個數字都不是磁碟速率。`pread_usec` 同時是**跨執行緒總和**（header:463,473），不可對牆鐘；只有 `fill_wait_us` 在呼叫執行緒上。", "counterexample_observed": "llama_bench_prefill250.stderr.log：`jobs=107046 bytes=0 effective_rate=0 MiB/s`，同檔 `slab fills: disk=41134.3 MiB`。", "applies_to": ["src/llama.cpp/src/llama-expert-cache.cpp", "src/llama.cpp/src/llama-expert-cache.h", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0011", "class": "source-reading", "rule": "`CGC-DECPROF` 存在、且會發聲（`ggml-backend.cpp:2064/2088/2100`，閘門 env `CGC_DECODE_PROFILE`），但它有三個獨立閘門使它答不了 prefill：(1) `CGC_DECODE_PROFILE` 必須設；(2) 分段路徑必須開（`cgc_oa_async_enabled()`，:1621，`CGC_OA_ASYNC` 為 unset/空/非 0 才為真，`=0` 則關）；(3) 列印閘門 `(dp_step % 8) == 0`（:2061）且逐層累加器每步重置（:2109-2111），所以 `dp_step=1`（= 單一 graph_compute 的 prefill）永遠不列印，首次列印必在 step 8。要看見 prefill：把 :2061 改成 `((dp_step % 8) == 0 || dp_step == 1)`。", "because": "dp_step 每個 graph_compute 加一（:2048）。prefill250 的形狀是 `n_prompt=2048`、`ubatch=6144` ⇒ 整個 prefill 只是一個 graph_compute ⇒ dp_step=1，`1 % 8 ≠ 0`。而 :2110 在每一步結束就清零，所以第 1 步的逐層資料活不到第 8 步。實證指紋：掃過全部保留日誌中每一個含 `CGC-DECPROF` 的檔案（95 份、數萬行），**每一份的最小 step 都是 8**，`step<8` 出現 0 次。另外這一輪 22 次 prefill 啟動**沒有任何一次設過 `CGC_DECODE_PROFILE`**（設過它的 73 份 run dump 全屬 decode 家族）；獨立矩陣 `matrix_prefill250_20260915.json` 還是 `CGC_OA_ASYNC=0`，連分段路徑都沒進。", "counterexample_observed": "被指派「用 CGC-DECPROF 對拆 286.67 與 179.09」；第一次 grep 只掃 src/llama.cpp/src/ 就宣告它不存在，被日誌打臉——`llama_server_20260915_185704.log` 一個檔就有 2736 行 CGC-DECPROF。", "applies_to": ["src/llama.cpp/ggml/src/ggml-backend.cpp", "scripts/check/prefill_certifiability.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0020", "class": "measurement-hygiene", "rule": "報告裡的每一個資料欄都必須指得出一個 artifact；指不出來的，就標成「未記錄」或「導出」，不能填一個看起來精確的數字。", "because": "本報告初版的 session 表有一欄「首次啟動前閒置」（~10 分鐘 / 5 秒 / ~14 分鐘 / 12 分鐘 / 3 分鐘）。當時 `prefill_certifiability.py` 還沒有 `--idle-before`（本輪才加），日誌與 json 裡也沒有任何時間欄位，所以那一欄沒有來源。改成導出值之後結論反而更強：實際間隔是 67 / 247 / 848 / 923 s，而 run 1 的 t/s 是 179.09 / 286.57 / 293.11 / 299.35——單調，且把恢復常數從「≤3 分」夾到「67 s < τ ≤ 247 s」。", "counterexample_observed": "錯誤的「5 秒」被寫進報告的圖說、卡片、§2.1 的否證表，以及 dec-20260916-0225 的 reasoning；共四處。", "applies_to": ["docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "scripts/check/prefill_certifiability.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0021", "class": "log-forensics", "rule": "日誌檔存在，不等於那個指令跑成功了；一個「看起來像成功」的檔名可以掩蓋一次失敗的執行。落檔後必須檢查內容行數／第一行，而不是檢查檔名。", "because": "`Backup/cgc_logs/powermetrics_prefill_paired_20260916.log` 存在，所以「有這個檔」很容易被讀成『powermetrics 跑過了』；實際內容只有一行 `(eval):1: operation not permitted: sudo`——沙箱封鎖 sudo，重試與關沙箱都無效。同一輪真正可用的是同名的 `gpu_util_prefill_paired_20260916.log`（300 筆 1 Hz Device Utilization %），它反而沒被引用。兩者一併說明：失敗要落成「失敗」，可用但未用的資料要落成本報告。", "counterexample_observed": "報告初版把 powermetrics 列為「下一步（待人工）」，卻沒說它已經試過且失敗；同時漏用了磁碟上已有的 gpu_util 時序。", "applies_to": ["Backup/cgc_logs/", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0022", "class": "measurement-hygiene", "rule": "當一條時序沒有事件標籤（這裡是每行只有 `HH:MM:SS <pct>`）時，配對規則必須用「以事件完成時刻為邊界」的窗口，不能用「哪個連續區塊含有那個時刻」。前者對單筆離群取樣穩健，後者會被一筆低於門檻的取樣切斷而把事件配到它自己的尾巴。", "because": "`gpu_util_prefill_paired_20260916.log` 沒有 run 標籤，只能用 `cert_runs/*/summary.json` 的 mtime 對齊。第一版規則「區塊含有 mtime」在 run 2 上失敗：prefill 與短 decode 尾之間有一筆 util=59%（02:19:12）剛好低於門檻 60，把區塊切成 02:18:42–02:19:11（n=24, mean 97.3%）與 02:19:13–02:19:17（n=5, mean 74.2%）兩段，而 mtime 02:19:17 落在小碎片裡 ⇒ run 2 被配到 74.2% 的尾巴而不是 97.3% 的 prefill。改成「run k 擁有 (mtime(k-1), mtime(k)] 並在窗口內取最長 ≥60% 連續段」後三個 run 都正確配對。", "counterexample_observed": "`scripts/check/prefill_gputime_report.py --gpu-util` 第一版的輸出把 `20260916_020546_run02` 標成 (unmatched)、並把 small fragment 配給它；與報告 §3.1 的表不一致。", "applies_to": ["scripts/check/prefill_gputime_report.py", "Backup/cgc_logs/gpu_util_prefill_paired_20260916.log", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0012", "class": "source-reading", "rule": "`CGC-DECPROF` 的列印閘門現在放行 prefill：`ggml-backend.cpp:2080` 是 `if (dp_tot > 0 && ((dp_step % 8) == 0 || dp_step == 1 || dp_ntok > 1))`，且每一行尾帶 `ntok=%lld`。要開啟：`CGC_DECODE_PROFILE=1` + `CGC_DECODE_PROFILE_ALL=1` + `CGC_OA_ASYNC!=0`（`arm prefill250-decprof` 三者都設）。**判讀規則：用 `ntok` 判斷一個圖是不是 prefill，不要用 `step`。**", "because": "原本只有 `(dp_step % 8) == 0`，而 `n_prompt=2048` + `-ub 6144` 的 prefill 是單一 `graph_compute`（`dp_step=1`），逐層累加器又在每步結束就清零（`:2130`），所以 prefill 永遠不會列印（eng-src-0011）。改完之後第一次跑就印出 90 行，形狀是 `step=1..4 ntok=2048` 然後 `step=8.. ntok=1`。`dp_ntok` 讀的是 `as_topk[i]->ne[1]`（`ne = [n_expert_used, n_tokens]`），所以它讓每一行自證形狀——若某次 warmup 搶走 `dp_step=1`，輸出會直接顯示 `ntok=1` 而不是默默把 decode 當成 prefill 解讀。額外收穫：prefill 的三段拆分是 wait 85-98% / cb 1-13% / submit 0-2%，與 decode 的 wait 43-76% / cb 21-53% 形狀完全不同，所以先前用 decode 形狀推 prefill 是沒依據的。", "counterexample_observed": "本輪 22 次 prefill 啟動上 `CGC-DECPROF` 一次都沒輸出，而 95 份保留日誌的最小 step 恆為 8——那個「儀器答不了 prefill」的結論方向對，但原因是列印閘門，不是儀器不存在。", "applies_to": ["src/llama.cpp/ggml/src/ggml-backend.cpp", "scripts/check/prefill_certifiability.py", "scripts/check/llama_bench_matrix.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0023", "class": "measurement-hygiene", "rule": "當一個儀器同時輸出「全部」與「前 N 名」兩種視圖時，harvester 必須按**實體鍵**去重，不能把兩種行都 append 進一個 list——前 N 名是全部的子集，兩者相加等於把那些實體數了兩次。", "because": "`CGC-DECPROF` 會印 `top8:` 與（開 `_ALL` 時）`all:`，描述的是同一批層。第一版 harvester 把兩種都 append，於是一個 40 層的模型報成 `layers_seen=48`（40+8）。這不只是計數錯誤：它把重複層塞進 `max` 與變異數，而「該層是均勻變慢還是單層問題」正是用 `max/mean` 與 CV 判的——同一份資料因此被判成 LOCALISED，真值是 UNIFORM。改成以層號為鍵的 dict 之後 `layers_seen=40`，中位數/IQR 也才可信。", "counterexample_observed": "sweep 第一個 session 的 run 1 被印成 `layer wait over 48 layers: ... peak/mean=1.667 -> LOCALISED`；同一個圖改用去重後的穩健統計是 `median=363.13 ms, IQR/median=0.094`，即 uniform。", "applies_to": ["scripts/check/prefill_certifiability.py", "scripts/check/prefill_idle_sweep.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0024", "class": "measurement-hygiene", "rule": "判「逐層是均勻變慢還是局部變慢」時，用**中位數 + IQR/中位數 + 落在中位數 ±25% 內的層比例**，不要用 `max/mean`。管線化的逐段迴圈（`submit_ahead`）在前幾層天生等得少，那個爬升段會支配 `max/mean`，把均勻的圖判成局部。", "because": "第 i 層的 `wait` 是「到第 i 層為止累積的 GPU 工作量減掉 CPU 進度」，所以 L0 幾乎不用等。實測：285 t/s 的快跑裡最低的一層也是 L0（112 ms 對 155 ms 平台）；122 t/s 的慢跑逐層 wait 是 124,171,199,230,336,629,606,503,433,272,...——爬升到 L5 峰值後進入 342-404 ms 平台，`peak/mean=1.75` 但 `IQR/median=0.094`。用 `max/mean>1.5` 的門檻會判成 LOCALISED，而那是結構性暫態，與快慢狀態無關。", "counterexample_observed": "報告 §3.2 的第一次自動判定把慢跑印成 `LOCALISED in one layer (=> implementation)`，與同一份資料的 2.32× / CV 6.5% 逐層比值結論相反。", "applies_to": ["scripts/check/prefill_certifiability.py", "scripts/check/prefill_gputime_report.py", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0025", "class": "measurement-hygiene", "rule": "報告步驟裡的每一個路徑查找都要在**量測開始前**解析成絕對路徑，且回報時不得因裝飾性查找而中斷。一次成功的量測被一行 `print` 弄丟，是純粹可避免的資料損失。", "because": "`prefill_certifiability.py --logdir` 給相對路徑時，per-launch 日誌路徑是相對的而 `ROOT` 是絕對的，`segpath.relative_to(ROOT)` 直接 `ValueError` 結束行程——那行只是要印出路徑。實測：一次 285.58 t/s 的探針在印完 seg 統計後死掉，summary 表格完全沒印。修法是把 `--logdir`/`--json` 在 `parse_args` 之後一律解析成絕對路徑，並讓列印容忍 repo 外的路徑。同一個崩潰也直接催生了 `prefill_idle_sweep.py`（它需要逐一呼叫這個 harness 並讀它的 json）。", "counterexample_observed": "`python3 scripts/check/prefill_certifiability.py --arm prefill250-decprof --runs 1 --logdir Backup/llama_bench/decprof_probe ...` 印出 `pp=285.58 t/s` 之後以 `ValueError: ... is not in the subpath of ...` 結束。", "applies_to": ["scripts/check/prefill_certifiability.py", "scripts/check/prefill_idle_sweep.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0020", "class": "diagnosis", "rule": "M1 `CGC_POOL_SPLIT` 的 Blocker B（兩個 expert 共用一 slot ⇒ NaN ⇒ il=2 SIGSEGV）有兩個具體線索，但**都還沒被證成**：(1) `llama-expert-cache.cpp:1848` 把身分映射的每個 slot 的 `slot_last_use` 設為 0 ⇒ `pick_slot` 的 LRU 平手時以掃描序（slot 0）勝出 ⇒ **expert 0 是系統性第一個被逐出的**，這正好對上 `CGC-PRE: st[0]=-1` 而 `st[1..7]=1..7`；(2) 但 `docs/M1_POOL_SPLIT_COST_2026-09-14.md` §4 引用的那段日誌**自相矛盾**，必須先重新擷取才能當證據。", "because": "矛盾點：`CGC-POST` 與 `CGC-SLOT` 都在 `llama-context.cpp:5853` 取同一份 `st` 指標，而 `CGC-SLOT` 印的 `remap` 是在 `:5861-5866` 用同一份 `st` 逐格寫入的。所以「`st[1]=1`」與「`remap[1]=2`」不可能同時成立——引文裡的 `st[1]=1` 與 `remap=[1 2 3 4 5 6 7 8]` 至少有一個不是該行的原文。相反的假說也已排除：`CGC_DBUF2` 的交換（`:1348` 交換 `slot_table` 而**不**交換 `slot_owner`）預設關閉，且只在 verify-decode 的 `n_tokens==1` 觸發（`llama-context.cpp:4477`），所以它不是本次症狀的來源。zero slot 是**最後**一個 slot（`llama-context.cpp:5286`），不是 slot 0。", "counterexample_observed": "以 `CGC_POOL_SPLIT=1 CGC_POOL_SPLIT_DBG=1 CGC_IDENT_VERIFY=1` 重跑時，文件引用的 `CGC-PRE/CGC-POST/CGC-SLOT` 三行無法同時成立；重跑應加 `CGC_EXACT_PATH_DBG=1` （`llama-context.cpp:5793`，會完整印出每個 selected expert 的 slot/owner/loading/queued）與 `CGC_SLOT_DBG=1`（`:5875`），一次擷取完整行。", "applies_to": ["src/llama.cpp/src/llama-expert-cache.cpp", "src/llama.cpp/src/llama-context.cpp", "docs/M1_POOL_SPLIT_COST_2026-09-14.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0026", "class": "measurement-hygiene", "rule": "要把兩份日誌做「同一個量」的配對之前，先確認兩邊的**粒度相同**。若一邊只記了子集（top-8）、另一邊記了全集（all-40），共同項會退化成參考跑*自己選出來*的那個子集，而它通常正好是參考跑最貴的項目——不是隨機抽樣。先印出兩邊的覆蓋範圍，再算比值。", "because": "快探針日誌（`Backup/llama_bench/decprof_probe/20260916_025854_run01/`）是在 `CGC_DECODE_PROFILE_ALL` 生效前抓的，每個圖只印 `top1..top8`（全檔 80 行逐層）；sweep 日誌每個圖印 `all1..all40`（400 行逐層）。`llama_bench_matrix.py` 的 mtime 03:00:53 正好落在探針（02:58:54）與 sweep 第一個 session（03:01:36）之間，可證差異來自設定而非機器。於是共同層只剩 8 層，且全部落在 L28-L38（比值一致 ~2.3×），得出 2.32×/CV 6.5%；換一組同覆蓋的參考即變 1.64×/CV 30%。", "counterexample_observed": "`--decprof-pair` 第一次輸出 `common layers: 8 of 8 (the fast log's coverage is the limit)`、`mean=2.32x cv=6.5%`、判定 UNIFORM；同一工具改用兩份都含 `_ALL` 的日誌後，40 層共同覆蓋、`cv=30.1%`、判定 NOT UNIFORM。兩次都沒有報錯——粒度不匹配本身不會觸發任何警告。", "applies_to": ["scripts/check/prefill_gputime_report.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0027", "class": "measurement-hygiene", "rule": "逐層/逐段剖面要**跨圖平均**，不要只讀第一個圖。`llama-bench -r 3` 會在同一個行程裡留下多個圖，第一個圖帶著一次性的載入與首次觸碰成本；那個暫態會落在「當時正在跑的那幾段」身上，於是看起來像局部熱點。最便宜的驗證是印出每個圖的 `worst layer`：它在圖之間漂移就是暫態。", "because": "同一對日誌（preheat 198.64 t/s vs idle0 122.39 t/s，兩份都含 `_ALL`、各 4 個 prefill 圖）：只讀第一個圖 → `mean=1.64x cv=30.1%`、L5 高達 `3.50x`、判定 NOT UNIFORM；跨 4 圖平均 → `mean=1.60x cv=8.7%`、min 1.25 max 2.06、無任何層超過中位數的 1.5 倍、判定 UNIFORM。差別全部來自第一個圖。漂移指紋：快跑 4 個圖的最貴層是 L16 / L29 / L37 / L9（在圖之間移動），慢跑是 L5 / L4 / L32 / L4。", "counterexample_observed": "`--decprof-pair` 舊版固定讀 `pre[0]`，對同一份資料報 `cv=30.1%`、`NOT UNIFORM: ... look for a layer-localised cost`；加上 `--graph mean` 後同一份資料報 `cv=8.7%`、`UNIFORM`。**「層間 CV」這個量本身被讀法放大了約 3 倍。**", "applies_to": ["scripts/check/prefill_gputime_report.py", "scripts/check/prefill_certifiability.py", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0028", "class": "measurement-hygiene", "rule": "如果一個統計量的值取決於「怎麼讀」（讀哪個圖、取哪些樣本），那就在結論裡把讀法一起寫出來，不要只報數字。否則同一個量可以在 6.5% / 8.7% / 30.1% 之間移動，而讀者會以為那些差別來自機器。", "because": "本輪「層間 CV」出現了三個都算『同一件事』的值：(a) 8 層子集（快跑 top-8）→ 6.5%；(b) 40 層、跨 4 圖平均 → 8.7%；(c) 40 層、只讀第一個圖 → 30.1%。三者對應不同的樣本與不同的圖，卻都曾被寫成「層間 CV」。結論方向（全層等比）在 (a)(b) 相同、(c) 相反，所以讀法不只影響精度，還影響結論。", "counterexample_observed": "報告 §3.2 初版寫「2.32×（CV 6.5%）」而沒有標明那是 8 層子集，也沒有標明來源日誌缺 `_ALL`；同一節在改用全覆蓋後必須整段重寫，並新增陷阱 A/B 兩個框。", "applies_to": ["docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0029", "class": "measurement-hygiene", "rule": "長時間執行的掃描/彙總工具，會一直用它**啟動時載入**的那份程式碼。如果掃描期間改了被它呼叫的分析邏輯，產出的彙總表會與每個子步驟的輸出不同版本，而兩者都不會報錯。收尾時要用**當下的程式碼對保存下來的原始日誌統一重取**一次，並以重取的結果為準。", "because": "`prefill_idle_sweep.py` 對每個 session 是開子行程呼叫 `prefill_certifiability.py`（所以每個 session 的 `summary.json` 用的是當下的新 harvester），但它自己的彙總表在行程啟動時就載入了；本輪 sweep 跑到一半時 harvester 被修正（層號去重、`max/mean` → 中位數/IQR）。結果：前 2 個 session 的逐層統計是 `48 層`、後 5 個是 `40 層`，而最後那張彙總表仍印舊欄位（`peak/mean`）。15 份日誌都在磁碟上，所以重取是免費的，且是唯一能讓整份資料內部一致的做法。", "counterexample_observed": "sweep 的彙總表把 idle=0/60 兩個 session 印成 `r1 layers=48 peak/mean=1.667/1.738` 並判 LOCALISED，而同一批 session 的 `summary.json`（新程式碼）判 UNIFORM；事後統一重取 15 份日誌後，全部 15 次的 `IQR/中位數 ≤ 0.073`、`±25% 內 ≥ 90%`，一致為 UNIFORM。", "applies_to": ["scripts/check/prefill_idle_sweep.py", "scripts/check/prefill_certifiability.py", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0014", "class": "gate-integrity", "rule": "閘門的收尾摘要必須把「跑了但通過」與「根本沒跑」分開報。如果所有區段都跳過、摘要卻仍印一句無條件的 `OK`，那讀者（包括未來的自己）會把它讀成「全部驗證過」。每個跳過點都要計數，摘要要報出數目與理由。", "because": "`scripts/check_build_tracked.sh` 預設檢查 `build/bin`，但本 repo 的 build 產物在 `src/llama.cpp/build/bin`；於是它在這個 checkout 上會把追蹤/rpath/deadlock/binary-sync/replay 等區段**全部跳過**，最後仍印 `OK: build/bin 追蹤與 rpath 正常、main⊆dev 成立、……通過/未啟用。`——「未啟用」被折進同一句 OK 裡。實測：`RUN_REPLAY_BENCH=0 bash scripts/check_build_tracked.sh` 有 5 個區段跳過、卻以 OK 結束，我因此一度以為 檢查 8（原始碼↔binary 同步）已被驗證。", "counterexample_observed": "同一支 script 的輸出裡，`--- 檢查 dylib 被追蹤（洞 B） ---` 之後緊接 `SKIP build/bin 不存在 → 跳過 build/bin 追蹤 / rpath / deadlock / binary sync 檢查`，而最後一行仍是 `OK: ...`。修法是把 11 個跳過點改走 `skip()`（計數），摘要改為「沒有 FAIL，但有 N 個區段被 SKIP」。", "applies_to": ["scripts/check_build_tracked.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0015", "class": "gate-integrity", "rule": "完整性規則若只對「一種記錄型別」實作，就不要假設它對其他型別也成立——它通常只是被寫在那一型的規則旁邊，從未被泛化。id 唯一性就是例子：它有檢查，但只檢查 `episode_id`。", "because": "`validate.py` 在 episode 規則下方寫了 `duplicates episode_id` 檢查，於是 decision 與 lesson 的 id 唯一性**完全沒有強制**。實際後果：`lessons.jsonl` 出現兩筆 `eng-gate-0006`（一筆是既有的 presence-vs-value 閘門教訓、一筆是本輪新寫的閘門摘要教訓）與兩筆 `eng-bound-0001`，而 validator 一路綠燈、連續數個 commit 都沒發現。id 是**鍵**：重複會讓 `supersedes`／`superseded_by` 的解析失去意義（指向哪一筆？），也讓「關於 X 的那條教訓」變成歧義——正是 D4 要防的失效模式。修法是把檢查泛化成 `(type, id_key)` 表，並用 `--dir` 對 HEAD 已提交的 `lessons.jsonl` 驗一次：立即報 `ERROR duplicate lesson_id eng-bound-0001`。", "counterexample_observed": "`python3 traces/validate.py <(git show HEAD:.../lessons.jsonl)` 在加入檢查後報 `ERROR duplicate lesson_id eng-bound-0001`，而加入檢查前同一份檔案報 `-> OK`。selftest 也從 8/8 變成 10/10（新增 `duplicate_lesson_id`、`duplicate_decision_id` 兩個多記錄案例）。", "applies_to": ["agent_harness/engine_loop/traces/validate.py", "agent_harness/engine_loop/traces/selftest.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0016", "class": "gate-integrity", "rule": "「這個改動在結構上不可能影響數值」不是跳過 D5 數值閘門的理由。當閘門很便宜（本例實測 23 秒，且它自己透過 `run_server.sh` 起 server，受測配置就是生產配置）時，要跑它，而不是用一段推論去替代一次測量。判準是前幾個 commit 實際做了什麼，不是 CONVENTIONS.md 寫了什麼。", "because": "本輪 commit 的 message 初稿把引擎改動的數值惰性寫成「structural, not empirical」，並打算不跑 M1/M2/M3，理由是每個 hunk 都在 `if (dp_on)` 內、而 `dp_on` 在未設 `CGC_DECODE_PROFILE` 時為 false。論證本身正確，但它把一個**可執行的檢查**換成了一句推論。`scripts/check/m123_oracle_gate.py` 會自己起 server（profile／env allowlist／load path 都是生產那套，不是手寫 argv 而會漂移），送同一個決定性 probe，與最新 M2 oracle `ref_iq3_pool8gb_M2_6144_bitident_v4_skip0off.jsonl` 對比 logits。實測結果把惰性從「應該是」變成「是」：重建後的 `libggml-base.0.19.0.dylib`（md5 26d0312edac0d30f479d52e65bed510f，mtime 02:58:34 > 原始碼 02:58:17）產生**位元相同**的 logits。更關鍵的是：同 repo 前兩個動到 engine 的 commit（`53b7df693`『the M1/M2/M3 gate against the unchanged v4 reference is M1 9/9, M2 9/9, M3 9/9, full_fnv1a64 9/9, comparable=true』、`25d61278f`『M1 9/9, M2 9/9, M3 9/9, full_fnv1a64 9/9』）都在 message 裡報告了這組數字。所以跳過不是「慣例允許的省略」，而是打破一個正在被遵守的慣例——而慣例的實際內容只能從 commit body 讀出來。", "counterexample_observed": "`python3 scripts/check/m123_oracle_gate.py --tag decprof20260916` → 23 秒完成：`GATE decprof20260916: PASS  M1(bit-identical)=9/9  M2(argmax)=9/9  M3(topk)=9/9  n=9`，cross-tab 四格為 9/0/0/0（全部 num_eq_dec_eq），`comparable=true`、`config_diffs=[]`（兩端配置由 `run_server.sh CGC_DUMP_ENV=1` 解析，所以不是把跨配置比較誤讀成退化），probe 答 '42'，9 筆 dump 通過驗證。對照初稿 message 的結論句：『The engine change's numerical inertness is structural, not empirical … That is a stronger statement than an A/B would give, and it is why no benchmark was run for it.』——同一件事，一個用跑的，一個用推的。", "applies_to": ["scripts/check/m123_oracle_gate.py", "agent_harness/CONVENTIONS.md", "scripts/check_build_tracked.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0017", "class": "gate-integrity", "rule": "儀器「有在動」不等於「有量到要量的東西」。新增一個儀器前要問兩件事：(a) 它假設的輸入格式有沒有**真實樣本**佐證，(b) 它保留的欄位**足以分辨所有待測假設**嗎。一個會把決定性欄位丟掉、或格式不符時能**安靜印出空序列**的解析器，與「只能分辨 PASS/FAIL、不能分辨 PASS/SKIP」的閘門是同一類缺陷。", "because": "`scripts/check/powermetrics_gpu_freq.sh --parse` 原本用 `grep -o` 抓 `GPU HW active frequency` / `active residency` / `idle residency` / `GPU Power` 四個標籤再 `paste`。標籤本身是對的，但這個儀器同時犯了兩個錯：(1) 它**丟掉**標籤後面括號裡的 DVFS residency 分佈 `(396 MHz: .06% ... 1278 MHz: 99%)`——那才是「時脈限制 vs 功耗上限」唯一能分辨的地方。單一 `GPU HW active frequency` 把分佈壓成一個數，所以兩個狀態可以報**同一個** active frequency 卻佔用不同的檔位組合；時脈被壓的指紋是質量整片移出最高檔，只有分佈看得到。(2) 它把約 480 個樣本 × 4 條序列原樣印出，而問題本質是**冷熱對比**，等於叫讀者用眼睛做分段與平均。更根本的是，那些標籤字串**從未對真實輸出驗證過**——它們在整個 repo 內只出現在一句敘述性註解裡，所以若格式不符，`--parse` 會安靜地印出四條空序列，與「量到零」同形。", "counterexample_observed": "真實格式（2026-09-16 以三個獨立來源核對，含一個 production 的 Go 解析器）是 `GPU HW active residency: 99.35% (396 MHz: .06% 528 MHz: 0% ... 1278 MHz: 99%)`。新 `powermetrics_gpu_freq_parse.py` 在合成真實格式樣本上讀出最高檔移動 **−99.4pp**、判準 `CLOCK`（時脈 1398→1278 MHz、residency 99.80→99.65%、功率 40150→33415 mW），區塊 0/1/2 正確分段為 cold/hot/hot2；`--selftest` 4/4。相對地，改進前的輸出是 406 個樣本 × 4 條無冷熱對比的數字列。標籤故意改錯時 exit 2 並**列出實際看到的 GPU 行**，空檔則明說 '(none -- the capture looks empty or was truncated)'，兩種失敗都不再與「量到零」同形。", "applies_to": ["scripts/check/powermetrics_gpu_freq_parse.py", "scripts/check/powermetrics_gpu_freq.sh", "scripts/check/ioreport_gpu_pstate_probe.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0018", "class": "gate-integrity", "rule": "當一個儀器要說「什麼都沒有」時，它必須同時交出「它實際看到的原始輸入」。只印出**自己期待的標籤**的解析器，會把『輸入根本不是那個東西』與『輸入是那個東西、但值是零』壓成同一個輸出——而這兩者的處置完全不同。同理，改動 CLI 契約（argparse 的 nargs）時要同時跑一次該工具**不帶位置參數**的入口（`--selftest`），那是契約的另一半。", "because": "`scripts/check/powermetrics_gpu_freq_parse.py` 的無樣本分支原本只印 `GPU-looking lines actually present`，然後在沒有 GPU 行時印 `(none -- the capture looks empty or was truncated)`。它**已經開了那個檔**（就是為了找 GPU 行），所以那個檔的內容就在手上；它只是決定不印。2026-09-16 使用者跑 `--parse Backup/cgc_logs/powermetrics_prefill_*.log`，該 glob 只匹配到 02:17 那次 sudo 失敗留下的 40 bytes 檔，唯一內容是 `(eval):1: operation not permitted: sudo`——**整份診斷就在那一行裡**，而工具把它換成了「looks empty or was truncated」，於是一個「capture 從來沒執行」的狀態被回報成一個「擷取是空的」的狀態。這與 `eng-gate-0014` 的 PASS/FAIL vs PASS/SKIP 是同一類缺陷，只是作用在儀器的**輸入**上：它分得出『標籤變了』與『沒有標籤』，分不出『這個檔根本不是擷取』。順帶，同一輪把位置參數從 1 個改成多個時用了 `nargs=\"+\"`，`--selftest`（不帶 capture）立刻被 argparse 拒絕：`error: the following arguments are required: capture`——契約的另一半壞了，而它剛好就是 selftest 在跑的那條路。", "counterexample_observed": "修好後，同一個 40 bytes 檔的輸出變成：`samples : 0`，接著 `first non-blank lines actually in the file:` → `(eval):1: operation not permitted: sudo`，並以 exit 2 收尾加一句指明生法的話（`sudo -v` 然後跑**不帶** `--parse` 的 `.sh`）。多檔：`--parse good.log dead.log` → `=== powermetrics summary: 2 captures, 1 with data ===`，死檔列為 `SKIPPED -- no GPU samples`（不再靜默丟參數），好檔照常分析並給出 verdict `CLOCK (same work, lower clock)`（時脈 1398→1278 MHz、residency 99.80→99.70%、功率 40130→33565 mW、最高檔移動 −99.40 pp）。`nargs` 改回 `\"*\"` 後 `--selftest` 恢復，且 selftest 由 4/4 擴到 **6/6**（新增『死檔在前＋好檔在後』與『只有死檔』兩個案例，後者斷言輸出裡必須出現 `operation not permitted: sudo`）。附帶一個會誤導除錯的工具層陷阱：同一則訊息裡對同一個檔案送兩個 Edit，後寫的那筆會覆蓋先寫的那筆，且兩筆都回報 success——第一次多檔測試因此得到 `error: unrecognized arguments`，若不知道這件事會誤判成程式邏輯錯誤。", "applies_to": ["scripts/check/powermetrics_gpu_freq_parse.py", "scripts/check/powermetrics_gpu_freq.sh", "scripts/check/ioreport_gpu_pstate_probe.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0019", "class": "gate-integrity", "rule": "在同一個 dylib 兩側以指標傳遞的 struct，新增一個成員就是一次連結器看不見的 ABI 破壞：它不會報錯，只會讓舊 dylib 讀到錯的成員偏移，症狀是「崩在函式庫裡」而不是連結失敗。所以任何用 `git stash` 換臂的 A/B，都會讓 build tree 停在被 stash 的那一臂——pop 之後的第一件事是**重建**，不是跑測試；與該標頭連結的測試／工具必須和它所連的樹同狀態重建。", "because": "本輪為斷言 `ensure_batch` 的兩趟指派，在 `llama_expert_cache` 新增 `n_hit_adopted_queued`（8 bytes），把 `ever_loaded` 的偏移從 0x608 推到 0x610。A/B 用 `git stash` 換回 HEAD 建了舊 dylib（mtime 09:24:35），`stash pop`（09:27:22）之後**沒有重建**，於是「照新標頭編的測試」連上「照舊佈局編的庫」。執行結果是 `SIGSEGV / KERN_INVALID_ADDRESS at 0x0`，崩在 `libllama` 的 `llama_expert_cache_ensure_batch +1068`。把該指令反組譯回去（`ldr x9, [x10, x9]`，x10 來自 `[x8, #0x608]`）再對照 `offsetof` 印出的兩套偏移：0x608 在新佈局是 `n_slot_table_unchanged`（size_t，值 0，當成 data pointer 用 ⇒ 空指標索引），在舊佈局才是 `ever_loaded._M_start`。診斷靠四步在一分鐘內收斂：(a) `nm -gU` 確認符號有 exported；(b) `offsetof` 探針印出兩套偏移；(c) `strings` 顯示該 dylib **完全不含**新增的 `CGC-BATCH-INVARIANT` 字串 ⇒ 它是舊碼；(d) `stat` 兩個 mtime 一眼看完（庫 09:24 < 源碼 09:27）。", "counterexample_observed": "錯的處置是「測試寫錯了」——我的第一反應是回頭審測試的 `key_segs`/`slot_owner`/`ever_loaded` 初始化，那條路會一路改到看不出問題，因為測試本身沒有錯，錯的是它連到的庫。判準要換成「庫和標頭是不是同一棵樹」。留給下輪的產物：測試檔頭現在寫入完整編譯指令與這段 ABI 警告，並註明「動到這個標頭就要重建庫，再從同一棵樹重建測試」。", "applies_to": ["scripts/check/expert_cache_ensure_batch_order.cpp", "src/llama.cpp/src/llama-expert-cache.h", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0020", "class": "diagnosis", "rule": "當一個「受保護集合」是由**同一個迴圈**邊走邊建、而它正是用來防止該迴圈造成破壞時，這個保護必然有順序依賴：只有它**之前**走到的元素被保護。判準是「這個集合在迴圈結束後才等於它宣稱的集合嗎」；若是，就要拆成「先認領全部、再變更」兩趟。註解寫著 exact-set invariant 不構成實作。", "because": "`ensure_batch` 的 `batch_owned` 註解自稱「hit slots + assigned miss slots」，但它是和 miss 指派**同一個迴圈**增量填入的。當池填滿時（identity prepopulate 填滿池且每個 `slot_last_use` 都是 0），`pick_slot` 的 LRU 犧牲者就是「最低且未被跳過」那一格；而淘汰會清掉被犧牲者的 slot 表條目（`llama-expert-cache.cpp:~555`，`slot_table[layer*n_expert + evicted] = -1`）⇒ 一次 miss 會把**後面還沒走到的成員**變成 miss。結果：`ids=[0..7]` 配上**一個**冷 expert，整批 8/8 全 miss，且整層映射整體位移 +1（`st[e] = e+1`）。單元測試上 HEAD 是 `st[0]=1 … st[15]=16`、0 hit/16 miss；兩趟之後是 `st[0]=16, st[1..15]=1..15`、15 hit/1 miss。修法除了拆兩趟，還要處理「一個 expert 已經擁有 slot 但 slot 表尚未發布」（`slot_owner[s]=e` 且 `slot_queued/loading` 已設而 `slot_table[e]` 未發布時，舊的 hit 判定只看 `slot_table[e]` ⇒ 判成冷的 ⇒ 給它第二個 slot）。", "counterexample_observed": "「這個集合不可能漏」的說法要小心：本輪之前曾把 09-14 日誌的 `st[e]=e+1` 讀成「兩個 expert 共用一個 slot ⇒ NaN」，而那是**引文抄錯**（原文 `st[1]=2`，白皮書寫成 `st[1]=1`）。修好引文後三行逐格自洽（`remap[i] == st[ids[i]]`），真正的異常是整體位移，不是共用。⇒ 當一行「不可能」的指控成立時，先回頭核**引文是不是原文**（見 `eng-gate-0021`），再動機制。另外：`il=2` 的 NaN 是下游簽名不是因——位移讓每個 expert 讀到別人的權重，且把池的 residency 上緣推過最後一個可用 slot（池區 OOB 讀），那才是會產生 NaN 而不只是「算錯」的那一類。", "applies_to": ["src/llama.cpp/src/llama-expert-cache.cpp", "scripts/check/expert_cache_ensure_batch_order.cpp", "docs/M1_POOL_SPLIT_COST_2026-09-14.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0021", "class": "log-forensics", "rule": "當一份文件的關鍵引文「自己不可能成立」時，第一嫌疑人是**引文**，不是機制——尤其當原文還在。先回去讀原始日誌那一行的原文（`sed -n` 指定行號），再決定要修文件還是修程式；同時要查該行在程式中的**列印來源**，確認被一起引用的兩行是否共用同一份快照。", "because": "白皮書與 `docs/M1_POOL_SPLIT_COST_2026-09-14.md` §4 都引用 `CGC-POST: il=1 st[0]=1 st[1]=1`，並讀成「expert 0 與 expert 1 共用 slot 1 ⇒ NaN ⇒ il=2 SIGSEGV」。原始日誌 `Backup/cgc_logs/llama_server_20260914_094821.log:324` 寫的是 `st[1]=2`。而且 `CGC-POST` 與 `CGC-SLOT` 共用**同一份** `st` 快照、`remap` 是用那份快照逐格寫的，所以改成 `st[1]=2` 之後三行逐格自洽、`CGC-SLOT` 印的四個值就是 `st[0..3]`，矛盾完全消失。也就是說**一處抄寫錯誤被放大成「兩個 blocker 之一」**，並讓下一輪的工作計畫變成「先去重擷取」。", "counterexample_observed": "反例的成本是具體的：原始計畫是「需要一次乾淨的證據重拍才能動 blocker B」，而那次重拍在這台 16 GB 機器上**不可能發生**——split 配置用 10 GiB 與 4 GiB 兩種池預算都在 warmup 掛 `CGC-METAL-FAIL: ... status 5 (Insufficient Memory)`，服務不到任何請求。改成「先核引文」之後，同一份日誌就足以定位根因（機制全部在 `ensure_batch` / `pick_slot` 的原始碼裡），並能寫出一個毫秒級、不需模型、不需 IO 的鑑別性測試。⇒ 重擷取是最後手段，不是第一步；而在要求重擷取之前，要先確認那件事在這台機器上做得到。", "applies_to": ["docs/M1_POOL_SPLIT_COST_2026-09-14.md", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "Backup/cgc_logs/llama_server_20260914_094821.log"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0022", "class": "gate-integrity", "rule": "把一次 A/B 當成驗證之前，先確認**修法的前提在該配置下可達**。逐位元相同的 A/B 要分成兩句話讀：「沒壞的地方沒有變」（有價值的安全確認）與「修法被檢驗了」（沒有）。當現有入口取不到鑑別性訊號時，要去找**能把前提做到的那個最小實驗**，而不是把 no-op 的 A/B 寫成通過。", "because": "`ensure_batch` 的兩趟指派只在「池填滿、且 LRU 淘汰真的觸發」時才改變行為。出貨預設（非 `CGC_POOL_SPLIT`）的池在 warmup 期間**從不填滿**——miss 落在空槽 ⇒ 淘汰不觸發 ⇒ 兩趟是 no-op。本輪把修法 `git stash` 掉、同一 4 GiB 配置重跑，新舊二進位吐出的 `CGC-PRE`/`CGC-POST`/`CGC-SLOT` **逐位元相同**（例如兩份日誌都有 `CGC-POST: il=0 st[64]=64 st[161]=6 st[125]=5`）。若不追這一步，很容易把「兩臂一樣」寫成「修法已驗證」。", "counterexample_observed": "真正提供鑑別性的是 `scripts/check/expert_cache_ensure_batch_order.cpp`：不需模型、不需伺服器、不做 IO（`key_segs` 空 ⇒ 每個 fill 都是 no-op），把合成快取擺在 `prepopulate` 剛結束的狀態（池滿、`slot_last_use` 全 0、一個冷成員的 slot 被 in-flight fill 佔著），餵一個 16 成員 batch，並**直接連 `libllama` 裡真正的 `ensure_batch`**。HEAD 逐字重現 09-14 日誌的映射（`st[0]=1 st[1]=2 … st[7]=8`）且 0 hit/16 miss ⇒ FAIL；兩趟之後 15 hit/1 miss ⇒ PASS。⇒ 前提不可達時，把前提**做成**一個最小實驗，比要求生產配置去滿足它便宜得多。", "applies_to": ["scripts/check/expert_cache_ensure_batch_order.cpp", "scripts/check/m123_oracle_gate.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0023", "class": "gate-integrity", "rule": "用特權啟動的子行程不能用 `kill $!` 停，而且非互動 shell 的**背景**工作會忽略 `SIGINT`——兩個原因各自都足以讓 `kill` 回 0 卻什麼都沒發生。停止要委託給同層特權的守護行程（哨兵檔通訊）＋改送 `TERM`＋再加一個自我終止的數量上限；而**驗證要看「效果」（檔案有沒有凍結），不是看「pid 還活著沒」**。", "because": "2026-09-16 的 powermetrics 擷取：壓測 09:14 就結束，`powermetrics` 卻一直寫到 09:46 之後（多出約 4000 筆純閒置樣本），而腳本最後那行「跑完會自動 parse」**從未執行**——它卡在 `wait` 上。原碼是 `sudo powermetrics … & PM=$!; … kill -INT \"$PM\" 2>/dev/null`。(a) `$!` 是 **root 的 sudo** pid，非特權 `kill` 得 `EPERM`，而錯被 `2>/dev/null` 吞掉；(b) POSIX 讓非互動 shell 的背景工作把 `SIGINT/SIGQUIT` 設為 `SIG_IGN`——實測 `sh -c 'sleep 30 & P=$!; sleep 0.2; kill -INT $P; sleep 0.5; kill -0 $P && echo alive'` 印出 `alive`。所以即使權限沒問題也殺不掉。", "counterexample_observed": "第一個修法只處理了 (a)，而**只修一個會看起來像修好了卻仍然失敗**——(b) 是在用 stub `sudo`/`powermetrics` 實跑整支腳本時才暴露的（那就是那次 stub 值得寫的理由）。第二個錯誤是修完之後用 `kill -0 $(cat $PMFILE)` 驗證：它與訊號投遞競態，在**正常**的停止之後也誤報「仍在執行」。會誤報的警告等於沒有警告；改成量「檔案有沒有再長大」（停止的行程不會再寫）。第三個：`timeout` 在 macOS 上不存在，別把它寫進複現步驟。", "applies_to": ["scripts/check/powermetrics_gpu_freq.sh", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0024", "class": "measurement-hygiene", "rule": "門檻若是「整條序列的百分位」，它就會隨序列的**工作週期**漂移。當尾巴可能比工作量長一個數量級時，要用高到在低工作週期下仍留在活躍模態內的百分位，並用「把尾巴接長」檢驗它的不變性——不是只看手上這一份。", "because": "區塊分段門檻 `0.15 × p95(功率)`，而 p95 是**整份擷取**的百分位。閒置尾巴一長，p95 滑進閒置分佈、門檻塌到 200 mW 的地板，於是 240–300 mW 的閒置毛刺被當成啟動（本機閒置 p50 51 / p95 217 / max 398 mW）。同一份擷取因此報出 25–31 個活動區塊而不是 3 個，「冷」被配到 09:09:30 一筆 237 mW 的毛刺，整組判準失效。量測：把真實前綴接上重抽樣的閒置尾巴，尾巴長 0/10/32/90 分鐘時 `0.15×p95` 給 3/6/22/31 塊、`0.15×p99.5` 給 3/3/3/3 塊，且邊界逐位元穩定（±1 樣本）。", "counterexample_observed": "第一版回歸測試**無效**：它接的是一條「平的」300 mW 尾巴，而平尾巴與最後一次啟動**相鄰**、會被併進同一個區塊，所以舊規則也得 2 塊——測試因為錯誤的理由通過了。真實閒置是**尖刺狀**的，必須建成「4 樣本 300 mW 的爆叢 + 6 樣本真閒置」的交錯（爆叢 ≥ `min_len`、間隙 > `gap_merge`），舊規則才會露出 **401** 塊 vs 新規則 2 塊。⇒ 回歸測試的合成資料要模仿真實的**形狀**，不是模仿它的平均。", "applies_to": ["scripts/check/powermetrics_gpu_freq_parse.py", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0025", "class": "gate-integrity", "rule": "儀器要**在自己的資料上**跑過一次才算驗證過，而這包含「它讀了你付費取得的每一個欄位」。對外來樣本驗證通過、自測全綠，都不能取代這件事。", "because": "`powermetrics_gpu_freq_parse.py` 是為了取代一個「丟掉決定性欄位」的舊 `--parse` 而寫的，已對三個獨立來源的真實擷取核對過標籤、`--selftest` 4/4 全綠。第一次對本機自己的擷取跑，仍暴露兩件事：(a) 它**從未讀取 `Current pressure level`**——而那份擷取正是用 `--samplers gpu_power,thermal` 取的，理由就是「時脈限制 vs 功耗上限」分不開；也就是說**問題的另一半，寫它的那個解析器答不了**。(b) 它的分段門檻在真實的閒置分佈上塌掉（見 `eng-gate-0024`）。(a) 不是程式碼的 bug，是**覆蓋率缺口**，只有在資料真的到手時才會顯示。", "counterexample_observed": "「已對三個獨立來源的真實擷取核對無誤」這句話當時是真的，而且被寫進了白皮書 §8——但它與「這個解析器能回答那個問題」是兩句話。同理，「selftest 4/4」量的是合成樣本能走完規則，不量規則是否覆蓋了資料的所有欄位。⇒ 驗收清單要加一條：**擷取命令用到的每個 sampler，解析器都有讀，而且讀到的值有印出來**。", "applies_to": ["scripts/check/powermetrics_gpu_freq_parse.py", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0026", "class": "honest-bounds", "rule": "把吞吐的變化歸因給某個量（時脈、頻寬、頻率）之前，先驗**比例性**：若吞吐比與該量的比不一致，就存在第二個「不隨它縮放」的成分，而那個成分才是可動的目標。兩個數字都要報。", "because": "prefill 276.25 → 151.28 t/s（−45%）時，有效時脈是 1400 → 700 MHz（−50%）。純 1/f 模型預測應慢 2.000×，實測只慢 1.826×（差 9.5%）；run 2 同方向（預測 1.294×、實測 1.216×、差 6.5%）。⇒ 純時脈模型被**資料否證**（這個述句不需要任何擬合）。配 `t = 0.5575 ms + 4226/f` 得到一個不隨時脈縮放的地板 0.558 ms/token，佔冷態 3.620 ms 的 15.4%。這是「時脈掉 58% 而吞吐只掉 45%」的來源，也是目前唯一已知**不會**被散熱改善的部分。", "counterexample_observed": "兩種錯誤讀法。第一種是拿「最高檔 / 穩定檔」直接除：618/1470 = 42% ⇒ 說「吞吐應該只剩 42%」＝116 t/s —— 比實測 151 低了 23%，因為它同時丟掉了**駐留加權**（700 MHz 才是有效時脈）與**不縮放的地板**。第二種是相信 `R² = 0.9987`：那是**三個點配兩個參數、只剩 1 個自由度**，它證明「這個形式容納得下資料」，不證明 0.558 ms 是精確值。可引用的只有方向性的兩句：純 1/f 與資料不符、因此存在不縮放的成分；量級要更多時脈台階才敢定。", "applies_to": ["docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "scripts/check/powermetrics_gpu_freq_parse.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0027", "class": "gate-integrity", "rule": "殺不掉一個行程是關於「某個 pid」的述句，不是關於「整條行程鏈」的述句：一個 `sudo X &` 會產生兩個 pid，包裝層是自己的、子行程是 root 的。寫下「無法終止」之前要先 `pgrep -fl` 把整條鏈讀出來；而驗收要看效果（檔案是否凍結），不是看 `kill -0`。", "because": "2026-09-16 的 powermetrics 擷取在修正停止路徑之前留下孤兒行程。本輪先前的結論是「孤兒無法由本 session 終止，需要 `sudo kill -TERM <pid>`」——而這句是錯的。實際鏈是 `84847 sudo powermetrics -i 500 --samplers gpu_power,thermal --format text`（擁有者是自己）與 `84849 powermetrics -i 500 --samplers gpu_power thermal --format`（擁有者是 root，`sudo` 為了讓特權程式在它的工作階段裡跑而持有的那一層）。`kill -TERM 84849` 得 `operation not permitted`（那才是殺不動的一個）；`kill -TERM 84847` 回 0，兩個一起消失。停止後檔案凍結在 5 410 344 bytes，第二次量測確認不再增長。", "counterexample_observed": "錯的不是「哪個 pid 不能殺」這個事實，是**把它推廣成「整條鏈不能殺」**。這與本 repo 既有的 B7（可被跳過的閘門不是閘門）、B12（儀器要在自己的資料上跑過）、以及 `eng-gate-0023` 是同一家族：`kill` 的錯誤若被丟進 `/dev/null`，「我試過且失敗」與「我沒試」在證據上同形；這裡是那個 `/dev/null` 早已修好、卻仍在用一個殘缺的推論下結論。另一個同形的舊錯誤是 `kill -0` 與訊號投遞競態（`eng-gate-0023` 的第三個反例）——本輪沒有重犯：判定「已停」用的是檔案不再增長，而不是 pid。", "applies_to": ["scripts/check/powermetrics_gpu_freq.sh", "agent_harness/CONVENTIONS.md", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0028", "class": "gate-integrity", "rule": "修正一個已定調的根因時，要一起修掉**引用它的每一個地方**，包括原始碼註解與**出貨的 log 字串**。文件會被更正，字串不會：沒有任何測試讀註解，所以一個被撤回的診斷可以在 binary 裡活下來，並且是操作者會讀到的那一份。", "because": "2026-09-14 的 M1_POOL_SPLIT_COST 把『expert 0 與 expert 1 共用 slot 1』寫成兩個 blocker 之一，來源是擷取的轉錄錯誤（原始 log 是 st[1]=2）。2026-09-16 的 2b284667f 把文件、errata 與 §4 全部更正了，而且**同時**在 src/llama.cpp 加了新的兩趟式實作——但 llama.cpp:420-423 的註解與 424-426 的 LLAMA_LOG_WARN 仍寫著『degenerate routing + SIGSEGV』與『the remap maps two experts to one slot』。於是同一個 commit 的產物裡，文件說『那不是真的』、binary 說『那是原因』。本輪（271538f2f 之後）把它更正為 blocker A（wide 張量／Metal buffer is nil，且它是**靜默**失敗：0 次 abort、234 次 buffer-is-nil）。", "counterexample_observed": "可以被跳過的不只是閘門，還有**讀者**。`CGC_POOL_SPLIT` 的 WARN 只在一種配置下會印，而那種配置在這台機器上跑不起來（warmup OOM）⇒ 這個字串在正常路徑上永遠不會被任何人看到，也就永遠不會被發現是錯的。（同族：eng-gate-0025 的『儀器要在自己的資料上跑過』——那裡是沒讀到欄位，這裡是沒有讀者。）", "applies_to": ["src/llama.cpp/src/llama.cpp", "docs/M1_POOL_SPLIT_COST_2026-09-14.md", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0029", "class": "gate-integrity", "rule": "三個關於「可觀測性」的規則，同一個 commit 一起套用：(a) 為了可見性而新增的計數器，**同一個 commit 要指出它被誰讀**——在一棵樹裡只找得到宣告與 `++`，就是還沒接線；(b) 閘門必須有**正的覆蓋計數**（跑了幾次／幾層／幾個 batch），不要讓「log 裡沒有 VIOLATIONS」成為唯一的合格訊號，因為那與「閘門根本沒跑」在證據上同形；(c) 放閘門之前先問「這個分支排除掉的是哪一種輸入」，並確認被排除的那一種不是它要抓的那一種。", "because": "2026-09-16 的 blocker B 修正（2b284667f）一次加了三個東西，三個都以各自不同的方式無聲。(a) `n_hit_adopted_queued` 的註解自稱『makes that path visible instead of silent』，而全樹只有兩處提到它：`.h` 的宣告與 `.cpp` 的 `++`——它是**唯寫的**，收養路徑若真的觸發，沒有任何輸出看得見。(b) 不變量閘門 `LLAMA_EXPERT_CACHE_BATCH_INVARIANT` 的 OK 行只印 `il<=2`，所以「沒有 VIOLATIONS」與「`SERVER_ENV` allow-list 沒把它帶進去」長得一樣。(c) 同一段閘門巢狀在 `if (!miss_exps.empty())` 之內，於是**零 miss 的批次從來不被檢查**——而「一個冷 expert 把 hits-only 批次變成全 miss」正是它要守的那個失敗的前置形狀。本輪把閘門抽成 lambda 並移到 `ensure_batch` 尾端、加上三個計數（`n_batch_evict_batches` / `n_batch_inv_checks` / `n_batch_inv_violations`）、並在 destructor 印出，測試從 1 個案例擴為 2 個（case 2 就是收養，它的存在本身證明了 (a) 的缺口有多大）。", "counterexample_observed": "同一輪還暴露了第二種無聲，形狀不同但同源：**「在配置 X 下是 no-op」量錯了工作負載**。blocker B 的兩趟式在預設（非 split）配置下被判為 no-op，證據是『warmup 期間池子從不填滿，miss 落進空槽、LRU 從不觸發』⇒ 修與未修逐位元相同。該觀察為真，但它量的是 **warmup**。served request 的形狀不同：8 GiB 池遇上 2873-token 的 prefill chunk 會碰 256 個 expert，池子會滿，pass 2 會淘汰——實測 `batch_evict_batches=325`、`batch_invariant_checks=331`、`violations=0`。所以「在那個配置下是 no-op」是關於 warmup 的述句被當成了關於配置的述句；逐位元相同只證明『沒有東西被改壞』，不證明『修正被測過』。", "applies_to": ["src/llama.cpp/src/llama-expert-cache.h", "src/llama.cpp/src/llama-expert-cache.cpp", "scripts/check/expert_cache_ensure_batch_order.cpp", "docs/M1_POOL_SPLIT_COST_2026-09-14.md", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0030", "class": "gate-integrity", "rule": "兩個關於「量什麼」的規則。(a) 先量 blast radius，再問機制：一個缺陷擋住誰，獨立於它是怎麼壞的，而且便宜得多就能回答——對照臂要選「如果它壞了嚴重度就跳到另一級」的那個配置。(b) 實驗臂的標籤必須由它實際解析出來的設定產生，不可以由它的意圖產生；用 CGC_DUMP_ENV=1 當前置檢查，把 CGCENV PROFILE/BATCH/UBATCH 抄進報告，標籤才是證據而不是宣稱。", "because": "prefill250 第二個請求的 SIGSEGV 有兩個讀法，嚴重度差一個數量級：「MTP 壞了」（擋住整個出貨預設，因為 run_server.sh:88 的 SERVER_MTP:-1 預設就是 ON）與「這個為 prefill 吞吐而存在的 profile 壞了」。在知道任何機制之前，這個問題已經可以回答：三臂單變數階梯（同一份 ~2.9k token prompt、各連發 3 次請求、閘門全關、每臂先 CGC_DUMP_ENV=1 dump 解析後的環境）——OFF(profile off, BATCH -, MTP ON) 全過；P250_ub2048(prefill250, 2048/2048, PREFILL_STREAM=1, SLAB_CAP=256, 8GiB) 全過（160/149/155 t/s）；P250(prefill250, 6144/6144，其餘逐項相同) req1 過、req2 死（llama-server-2026-09-16-111232.ips，簽名與前兩份逐格相同）。⇒ 觸發條件是 6144 那一格，MTP 本身與 slab/stream 路徑都不足以觸發；blast radius 是一個 profile，不是一個功能。(b) 這支腳本第一版寫 run_arm prod25 卻沒傳任何環境變數，實際跑的是預設 profile off——數字有用但標籤是假的，而假標籤會被當成正確的證據引用；加上 dump 前置檢查後，CGCENV PROFILE   off 在第一秒就否決了 prod25。這是 eng-gate-0006 的同一個形狀，只是漂移的是證據的名字。", "counterexample_observed": "同一輪還撞到一個「原始碼有、二進位沒有」的例子，形狀與 B15/B16 不同但同源：出貨的 libllama-common.0.0.239.dylib 是用沒有 -DMTP_SUPPORT 的設定編出來的。三條獨立證據：(1) build/common/CMakeFiles/llama-common.dir/flags.make 的 CXX_DEFINES 不含它、build/CMakeCache.txt 的 CMAKE_CXX_FLAGS 是空的，而專案自己的 scripts/build_fork_llama.sh:34 預設 MTP_SUPPORT=ON；(2) strings -a 的正對照——未 guard 的字串（adding speculative implementation 'draft-mtp'）在、三個在 #ifdef MTP_SUPPORT 內部的字串全部不在；(3) 執行期堆疊第 4 格是 llama_get_embeddings_nextn，而 MTP 區塊裡唯一呼叫它的是 common/speculative.cpp:1562，位在 #else 分支裡。後果是那段標著 [CGC MTP fix] ... safely access each row 的逐列安全讀取不在執行中的二進位裡。這條與崩潰的因果關係本輪沒有建立，只建立了『它沒被編進去』這個事實。", "applies_to": ["Backup/run_mtp_crash_triage.sh", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md", "scripts/run_server.sh", "scripts/build_fork_llama.sh", "src/llama.cpp/ggml/src/ggml-metal/ggml-metal-context.m", "src/llama.cpp/common/speculative.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0031", "class": "diagnosis", "rule": "錯誤處理路徑不得「先釋放、後記錄」：任何用來填寫診斷資訊的物件，必須在任何 release 之前讀完。並且，錯誤路徑裡出現 EXC_BAD_ACCESS 時，先問「它遮蔽了什麼」，不要先問「什麼壞了」。", "because": "prefill250 的 req2 崩潰被讀成「MTP 路徑壞了」讀了三輪（§11.9 甚至把嫌疑點收斂到 cmd_bufs[] 某一格），真正的位置是 ggml-metal-context.m 的 cmd_bufs_ext 失敗路徑：舊碼順序是 印 log → release 迴圈 → removeAllObjects → 才呼叫 cgc_metal_record_error()，而該函式在 status == 5 時會讀 [cmd_buf error]。cmd_bufs_ext 對每個元素持有兩次 retain（addObject: 與 add 點的顯式 retain），所以走到 record 時 cmd_buf 已經被釋放兩次，接著被送 ObjC 訊息。後果是對稱的傷害：GPU 回報 kIOGPUCommandBufferCallbackErrorOutOfMemory(status 5) 時，行程不是照設計 GGML_ABORT(CGC-METAL-FAIL)，而是死在 objc_msgSend，於是唯一可診斷的事實（GPU out of memory）從未被印出。修正把記錄（含 ctx->has_error = true）移到釋放之前，同一份 6144 臂因此從 EXC_BAD_ACCESS SIGSEGV／KERN_INVALID_ADDRESS at 0x10 變成 EXC_CRASH SIGABRT 加上 ggml-metal-context.m:788 的 CGC-METAL-FAIL: extra command buffer 1 failed (status 5, Insufficient Memory (00000008:kIOGPUCommandBufferCallbackErrorOutOfMemory))。兩次 run 的失敗簽名（command buffer 1 / status 5 / Insufficient Memory …OutOfMemory）逐字相同，A11 配置指紋 749ce190c3d2f47727080efb7467fd49 相同，且 crash report 自己記錄的 image UUID 逐一比對後只有 libggml-metal 不同（a2188b93… → 4cc6f293…）⇒ 這是單變數對照，不是混淆對照。D5 閘門（--tag metalerrfix）M1/M2/M3 各 9/9、fnv1a64 9/9、cross-tab 9/0/0/0 ⇒ 對成功路徑是 no-op。", "counterexample_observed": "這一條的證據是機械的、不是原始碼推論，而且靠「保留當時的二進位」才做得到：113207.ips 的第 0/1 格是 objc_msgSend + 32 ← ggml_metal_synchronize + 612，imageOffset = 81412 = 0x13E04，正是 bl _objc_msgSend$error 的返回位址（cbz x0）。把備份下來的舊 libggml-metal（Backup/pre_mtp_rebuild_20260916/libggml-metal.0.19.0.dylib，md5 b28c011055fa2113ddce56453105c71a）反組譯，可以看到 0x13d7c 起是釋放迴圈、0x13db8 是 removeAllObjects、0x13e00 才是第二次 [cmd_buf error]——指令序本身就是證據。反例提醒：cmd_bufs[] 槽位（:1018-1076）與 slots 迴圈（:721-746）複查後在紙上是平衡的，原假設「某一格在釋放後沒歸零」不成立，所以不要把「崩在讀已釋放物件」直接等同於「某處多了一次 release」。仍未修、本輪刻意未動的一項：cmd_buf_last 的懸置窗口（graph_compute:985-988 的歸零被關在 if (use_capture) 內，而 synchronize:711-714 會 cgc_wait_cmd_buf 它；該函式是 static 會被內聯，堆疊上顯示 +0）。", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-context.m", "Backup/run_req2_retest.sh", "Backup/pre_mtp_rebuild_20260916/libggml-metal.0.19.0.dylib", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0032", "class": "gate-integrity", "rule": "稽核編譯期開關時，要量「載入器實際映射的那個檔案」的字串存在性——從 otool -L 反推 @rpath 載入集再逐一 realpath——並同時印出不受 guard 保護的 control 字串。不要把檔名寫死，也不要用 mtime 當一致性證據。", "because": "SONAME 內嵌 commit 數（libllama-common.0.0.<N>.dylib），所以每次全量重建都會換檔名，舊檔留在原地變成孤兒；而載入器走的是 symlink 鏈（libllama-common.dylib → libllama-common.0.dylib → libllama-common.0.0.275.dylib，用 otool -L 可確認）。重測腳本的證據頭把檔名寫死成 0.0.239，於是兩份報告都印「MTP_SUPPORT 字串 ABSENT」——與事實相反：兩次崩潰報告的 usedImages 都顯示實際載入的是 libllama-common.0.0.275.dylib（UUID 4ddc4e5d-ebb1-39e6-9e6a-1819404f1be3，3/3 受 guard 字串俱在）。錯誤方向剛好是「開關沒開」，會把整條 MTP 觀測鏈引到另一條路上（連帶讓「MTP_SUPPORT 有沒有編與崩潰的關係」這種問題問錯）。另一件事同時成立：增量 cmake --build 只重編 CMake 自己判定為 stale 的 TU（本輪 411 個 .o 全部最新、只重編了 test-quantize-stats.cpp.o），所以「libllama-common 的 .o 是 09-14、speculative.cpp 是 09-13」這種 mtime 比較可以同時成立於一個完全一致的 build；那個 build dir 的一致性要靠 flags.make 的內容比對，不是 mtime。修正後改為從 otool -L 反推 10 個載入 artifact 並逐一 realpath，輸出判準句 guarded 3/3 + control present => MTP_SUPPORT=ON (compiled in)。", "counterexample_observed": "第一次的正對照就測錯檔：拿 libllama-common.0.0.239.dylib（tracked、重建時沒有被覆寫）做 before/after，得到雜湊相同而得不出結論；改測重建產生的 0.0.275 才得到 0/3 → 3/3。另一面：run_server.sh:325-341 新增的 MTP 可見性守門兩面都量過——好 build 0 warning、舊 dylib 標記數 0（會 warning）、現行 3（不 warning）；control 字串缺失代表 artifact 壞掉，不是開關沒開，所以判準句必須把 control 一起印出來。順帶更正：這份寫死的證據頭不只「修後」那一份錯，修前那一份也錯（它當時就宣稱 MTP 沒編，而事實是已經編了）。 同族的第二個實例（2026-09-16 實測）：閘門的檔案比對樣式漏一種副檔名，等於對那個類別完全無效。scripts/check_build_tracked.sh 的檢查 8 用一個 case 樣式挑出「會被編進 binary 的 llama 原始碼」，原本列了 *src/*.cpp|*.h|*.c|*.mm|*.metal，獨漏 .m（只列了 Objective-C++ 的 .mm）。後果：改 ggml/src/ggml-metal/ggml-metal-context.m 會被判成「8 無 llama 原始碼變更（僅 doc/腳本/產物）」，一個假 PASS，而且它同時跳掉了「產物有沒有一起 staged」與「binary 是否比原始碼新」兩條。已補上 *src/*.m（該 repo 只有 2 個 .m，都在 ggml/src/ggml-metal/，都編進 libggml-metal，所以樣式是精確的）；陽性對照：餵一份含 .m 的假 staged 清單，staged_src 由 0 → 1，檢查 8 隨即改印 「build 產物隨原始碼 staged」+「binary 比 staged 原始碼新（已重建）」。", "applies_to": ["Backup/run_req2_retest.sh", "scripts/run_server.sh", "scripts/check_build_tracked.sh", "agent_harness/CONVENTIONS.md", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0021", "class": "diagnosis", "rule": "被交回來的「仍未解」清單要先分類再動手：缺陷／缺失的可見性／政策漂移／已過期的述句。只有第一類需要改行為；第二類要改的是訊息，第三類要對齊已裁決的那幾項（不是全部），第四類只能加時間範圍。", "because": "2026-09-16 一次交回四條。(a) req2 的 OOM 是真缺陷（只能繞道或改機制）。(b) 「--ngl 99 讓 common_fit_params 的 fit 被跳過」不是 bug：common/common.h:477 讓 params.fit_params 預設 true（等同 -fit on），但 common/fit.cpp:377-379 在使用者已給 n_gpu_layers 時 throw common_params_fit_exception(... \"abort\")，而腳本一律顯式帶 -ngl ⇒ fit 從來沒跑過，而且啟動訊息完全不提。修「可見性」只加一行啟動輸出；修「行為」（把 -ngl 拿掉讓 fit 決定）會改掉記憶體／效能剖面與既有 oracle 的可比性，是貴很多的動作。(c) build dir 與 scripts/build_fork_llama.sh 的 6 處不同裡只有 4 個 GGML_* 是已裁決的政策（BLAS/ACCELERATE 讓 IQ3_XXS 亂碼、CPU_REPACK 踩 tensor boundary、OPENMP 穩定性），LLAMA_BUILD_SERVER/TESTS 的差異是 devserver 刻意要的——全部對齊會把 server target 關掉。(d) §11.9.5 描述的是 11:30 之前的產物（libllama-common.0.0.239），那句話對現況不成立，只能改文字。把 (b) 這種當 bug 去修，等於用一個配置變更換掉一個可見性問題。", "counterexample_observed": "同一次裡的反例方向也值得記：§11.10.7(3) 原本說「不能說這個 build 等於專案自己的 Release 設定（GGML_BLAS/ACCELERATE/CPU_REPACK 未依腳本要求關掉）」，而對齊那四個之後重跑 D5（--tag flagalign2，在完整重建後的樹上）得到 M1/M2/M3 各 9/9、fnv1a64 9/9、cross-tab 9/0/0/0、config_diffs=[] —— 所以政策漂移可以是真的、而修它可以是數值 no-op；兩件事不衝突，但都不可以先驗地宣稱。", "applies_to": ["scripts/run_server.sh", "scripts/build_fork_llama.sh", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0033", "class": "gate-integrity", "rule": "相容性缺陷（唯讀映射被寫）的正確產物是硬閘門，而且閘門要四面都量：單獨拒跑、合法豁免放行、強制放行（+warning）、對照不受影響。閘門成立後要回頭把它從「建議槓桿」清單裡拿掉。", "because": "CGC_SERVER_LOAD_MODE=mmap 與 expert cache 不相容：L4 pool 是「regions adopted from expert tensors」，直接寫進模型張量儲存；none 模式下那是可寫堆積，mmap 下是唯讀 file-backed 映射。於是 fill_pool_direct（src/llama-expert-cache.cpp:693-710）的 zeroing 寫進唯讀頁 -> EXC_BAD_ACCESS / SIGBUS / KERN_PROTECTION_FAILURE，在載入階段就死（health 從未轉 ok，Backup/cgc_logs/req2retest_20260916_120057.txt 的 driver exited early；llama-server-2026-09-16-120239.ips 的 __bzero <- fill_pool_direct <- prewarm_hot <- process_ubatch <- load_model）。修它要動 expert cache 的儲存所有權（另一輪），所以本輪該交出來的是閘門。只量「單獨拒跑」不夠：你不會知道豁免路徑（EXPERT_CACHE_OFF=1，此時 pool 不寫張量儲存，mmap 其實安全）有沒有被誤擋。", "counterexample_observed": "第一版把 mmap 列進 [budget] 超額段的「建議槓桿」——與同一次新增的 [防護 2e] 結論直接矛盾（一個會 SIGBUS 的選項不是槓桿）。四面實測：CGC_SERVER_LOAD_MODE=mmap 單獨 rc=1 拒跑；+CGC_SERVER_EXPERT_CACHE_OFF=1 rc=0；+CGC_SERVER_ALLOW_MMAP_EXPERT_CACHE=1 rc=0 且印 warning；load_mode=none rc=0 照常走預算判讀（OVERSUBSCRIBED by 4838 MiB）。另注意閘門的觸發條件必須包含 EXPERT_CACHE_OFF 這一面，否則它會擋掉唯一一條 mmap 可用的路徑。", "applies_to": ["scripts/run_server.sh", "src/llama.cpp/src/llama-expert-cache.cpp", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0030", "class": "measurement-hygiene", "rule": "OOM 要寫成算式而不是形容詞，而且每一項都要標明是「算術」還是「實測」；「池的上限」不等於「實配」。算式印出來之前要複核單位換算。", "because": "-expert-cache 的 BUDGET 是上限：實測 LAYER_CAPS per-layer caps: total 5976 slots、每層 slab=110.00 MiB@256 experts ⇒ 約 2.5 GiB，不是 8192 MiB。把上限當實配會把 13030（model resident，load_mode=none = 匿名頁不可回收）+ 8192 的 21222 MiB 對 16384 MiB（OVERSUBSCRIBED 4838 MiB）算成別的數字。KV 同理：主 context 是 hybrid memory，filter 是 il < n_layer() && !is_recr(il)（llama-model.cpp:2292-2295 -> llama-memory-hybrid.cpp:48-50），full_attention_interval=4 ⇒ 只有 i=3,7,...,39 這 10 層帶 K/V ⇒ 每層每 token 2 kv-head x (256+256) x 2B = 2 KiB ⇒ fp16 @8192 = 160 MiB（q8_0 ≈ 85 MiB）；腳本舊註解的「saves ~0.56GB (1.25GB -> 0.69GB)」對應的是全 41 層 dense attention 的形狀（41 x 1024 x 8192 x 2B = 688 MB），不是這個模型。這一項與槓桿無關（75 MiB vs 超額 4838 MiB），但留一個沒量過的數字在腳本裡當事實會污染後續所有判讀。", "counterexample_observed": "自己踩的單位換算錯：第一版把 10 層算成 164 MiB（20 KiB/token x 8192 = 160 MiB，不是 164），已同時修正 [budget] 的 KV 行與 [kv] 註解。另一個假線索：6144 對照臂的報告顯示 new crash report: llama-server-2026-09-16-113649.ips，那是 11:36 的舊報告——Backup/run_req2_retest.sh 的 newest_ips() 是「取最新檔」而不是「取新檔」，於是把不屬於這次的 .ips 當成自己的。實情由 llama_server_20260916_120538.log 的 4 行自行證明（status 5, kIOGPUCommandBufferCallbackErrorOutOfMemory）。", "applies_to": ["scripts/run_server.sh", "Backup/run_req2_retest.sh", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0034", "class": "gate-integrity", "rule": "在 `set -o pipefail` 下，判定式不可寫成 `… | grep -q <needle>`：`grep -q` 一命中就關閉 pipe，上游（`strings`/`otool`/`nm`）收到 SIGPIPE 以 141 結束，`pipefail` 讓整條 pipeline 判成失敗，於是「條件成立」的那一面回報「不成立」。要寫成 `n=\"$(… | grep -c <needle> || true)\"` 再比對 `n`。閘門的兩面（含修復／不含修復）都要跑——只跑失敗的那一面永遠看不到這個 bug。", "because": "2026-09-16 12:3x，`scripts/run_server.sh` 的 `CGC_MMAP_POOL_FIX` 判定用 `strings -a \"$lib\" | grep -q '_Pool'`：在含修復的 `libggml-metal` 上 rc=1（stderr 是 `strings: failed to flush output`），閘門因此把**已修好**的 mmap 路徑判成不可用並 `exit 1`。同一台機器三面實測：`pipefail`+`grep -q` rc=1；`pipefail`+`grep -c` rc=0 且 n=1；無 `pipefail`+`grep -q` rc=0。這是 fail-closed 的假陰性：閘門在唯一該放行的那一面擋住。", "counterexample_observed": "scripts/run_server.sh（修復前的 CGC_MMAP_POOL_FIX 判定段）", "applies_to": ["scripts/run_server.sh", "Backup/run_req2_retest.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0031", "class": "measurement-hygiene", "rule": "實驗進行中不得編輯執行鏈上的任何腳本。bash 以位元組位移增量讀取腳本，改檔會讓它從錯位處繼續讀；而且 `run_server.sh` 會被每一臂重新叫起，中途改它等於在同一輪實驗裡換掉受測配置。改動前先 `pgrep -f <腳本名>` 確認沒有在跑；實驗跑完之前只讀不寫。", "because": "2026-09-16 12:53–12:57，`Backup/run_mmap_ab.sh` 正在跑 `none:5120` 這一臂時我對它連發四個 `Edit`。該臂**本身完整跑完並印出結果**（子行程 python 已把 summary 列寫入），但父行程接著從錯位的位移讀到 python 的 `\"common_md5\": common[:12],` 與 `(`，報 `line 156: n[:12],: command not found` 與 `line 157: syntax error near unexpected token '('`——錯誤行號指向語法完全正確的行。", "counterexample_observed": "Backup/cgc_logs/ub_probe/r1_none_ub5120.out（該臂輸出尾端的三行 shell 錯誤）", "applies_to": ["Backup/run_mmap_ab.sh", "Backup/run_req2_retest.sh", "scripts/run_server.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0022", "class": "diagnosis", "rule": "修好一個缺陷之後，必須重新量**它原本蓋住的限制**，並且把新的第一失敗點指名（訊號＋階段＋堆疊行）。修復的驗收條件是「舊訊號消失」**加上**「新的第一失敗點是什麼、落在哪個階段」——只寫前半句會把一個退步（更早死）記成一個進步。", "because": "2026-09-16，pool/mmap 修復（L4 pool tensor 改走自己的 `_Pool` buft）之後，同 profile、同 `ub=4096` 的兩次啟動：12:02 修復前死在**載入中**的 fill pool，SIGBUS / `KERN_PROTECTION_FAILURE`（`__bzero <- fill_pool_direct`）；12:46 修復後**載完 40 層**、在第一個 `ggml_metal_synchronize` 以 `status 5` `kIOGPUCommandBufferCallbackErrorOutOfMemory` 死。SIGBUS 消失（修復成立），但新限制隨之顯形：在 mmap 這一側，`ub=4096` 已經超出 Metal 的工作集。這條限制在修復前**不可能**被看見——SIGBUS 讓載入階段就先死。", "counterexample_observed": "~/Library/Logs/DiagnosticReports/llama-server-2026-09-16-124700.ips", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal.cpp", "src/llama.cpp/src/llama-model-loader.cpp", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0023", "class": "diagnosis", "rule": "macOS 的 BSD `grep` 不支援 `\\|` 交替。要交替必須用 `grep -E 'a|b'` 或 ripgrep；`grep 'a\\|b'` 會安靜地比對**字面上**的 `a|b`、以 rc=1 結束，與「真的沒有符合的行」在輸出上同形。「沒有符合的行」與「這個字串不存在」必須用不同的方式區分，否則會把一個查詢錯誤當成一個否定結論。", "because": "2026-09-16 13:0x，`grep -n 'id=\"s1111\"\\|id=\"s1110\"\\|id=\"s119\"' docs/PREFILL250_THERMAL_TRANSIENT_20260916.html` 回空輸出、rc=1；同一組 id 用 ripgrep 立刻命中 :2081 / :2128 / :2356 / :2372。同一輪稍早 `grep -rln 'a\\|b\\|c' --include=…` 也回空。單一固定字串的 `grep -q`/`grep -c` 不受影響（見 eng-gate-0034）。", "counterexample_observed": null, "applies_to": ["agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0035", "class": "gate-integrity", "rule": "建置產物的新鮮度，判準是**建置輸出有沒有編譯行**，不是 exit code，也不是產物存在或它的 mtime。`cmake --build` 在「已經最新」與「剛剛編好」兩種情況下都回 0，而磁碟上有一份產物這件事本身看起來就是權威的。改了會被編進二元檔的檔案之後、跑任何測試之前，必須先對該 target 跑一次建置並**讀它的輸出**；一旦印出 `Building …`，先前在同一棵樹上取得的所有數字全部失效——它們描述的是上一份產物。", "because": "2026-09-16：12:44:53 全量重建之後，13:00:34 編輯了 src/llama.cpp/ggml/src/ggml-metal/ggml-metal-context.m（把 cmd_buf_last 的所有權註解從「窗口很窄」改寫成查過的事實），沒有重建。於是 12:44–13:50 之間每一筆量測都屬於舊產物：13:45:10 的出廠驗收（268.09/273.55/256.61 t/s）與 13:50:16 的 D5 PASS 都是。發現方式是在收尾時跑 cmake --build 當 check 8 的證據，它印出 `Building C object ggml/src/ggml-metal/CMakeFiles/ggml-metal.dir/ggml-metal-context.m.o`；同批重建後 libggml-metal md5 968c36cf45742bb1667d5a02629c67be -> f2d1c96193939bd15404ba713a6fa85d，而 libggml-base / libggml-cpu 的 md5 不變（證明「有沒有編譯行」只關於變動的目標）。check 8 的 mtime 規則本來會擋下這一格（binary 12:44 < source 13:00），但它在 commit 前才跑，而量測更早發生——中間那段空窗裡只有建置輸出能擋。", "counterexample_observed": "Backup/cgc_logs/prod_accept/req2retest_20260916_134510.txt vs …_135131.txt（同配置、同 A11 指紋 a9c9dc104f057bf640e0132a83e01c12，libggml-metal 968c36cf -> f2d1c961）", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-context.m", "src/llama.cpp/build/bin/libggml-metal.0.19.0.dylib", "scripts/check_build_tracked.sh", "agent_harness/CONVENTIONS.md", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0036", "class": "gate-integrity", "rule": "一個 gate 的「參考身分」不能寄生在生產可調的預設值上。當 gate 宣稱「預設會重現參考的配置」，而那個性質是靠某個 profile 的生產預設值恰好等於參考值才成立時，任何一次生產調參都會無聲改寫這個 gate 的意義。做法：把參考檔的 numerics-determining 旋鈕寫成常數放在參考路徑旁邊，由 gate 在 resolve 之前併入；CLI 仍可覆蓋，但**有效集合必須在啟動前印出來**（「被靜默覆蓋」與「被靜默丟棄」在 transcript 上同形）。重新基線要顯式（--write-ref + --no-pin-oracle-env）。", "because": "2026-09-16：把 CGC_SERVER_PROFILE=prefill250 的 BATCH/UBATCH 生產預設由 6144 改成 5632（6144 在 req2 上 0/5 死、5632 4/4 活）之後，m123_oracle_gate.py 立刻對 CGCENV.BATCH / CGCENV.UBATCH / ARG[27] / ARG[29] 四列差異報 INVALID COMPARISON（comparable=False、ok=False），而同一份輸出裡 M1/M2/M3 都是 9/9 並被標為 'printed for information, NOT a verdict'。四列差異 100% 是自己的預設變動。修法是 ORACLE_PINNED_ENV = (CGC_SERVER_BATCH=6144, CGC_SERVER_UBATCH=6144)，放在 DEFAULT_REF 旁邊；14:02:04 的 gate 於是 comparable=true，cap 的 resolved BATCH/UBATCH = 6144/6144 證明釘子生效。殘留：gate 認證的是 oracle 配置的數值，出廠預設 5632 沒有自己的參考檔，只能量到跨配置的 9/9。", "counterexample_observed": "Backup/m123_oracle_gate/summary_poolfix5632.json（comparable=False、4 列 diff）vs summary_poolfix_final.json（comparable=True、diffs []）", "applies_to": ["scripts/check/m123_oracle_gate.py", "scripts/run_server.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0032", "class": "measurement-hygiene", "rule": "「兩次量測之間只有一個產物不同」不等於單變數實驗。單變數的前提是**交錯**，不是事後清點差異——因為機器狀態（頻率、swap 用量、壓縮器）不在任何一方的日誌裡，而它可以單獨造成同一份產物 2 倍以上的差距。兩個候選都無法排除時，正確的述句是「未歸因」，並指名能裁決它的那個交錯實驗；把「只有一個差異」寫成「就是它造成的」是製造一個假因果。", "because": "2026-09-16 14:0x：13:45:10（12:44 產物）的 prefill 是 268.09/273.55/256.61 t/s；13:51:31 之後（最終產物）四臂是 118.96/135.21/138.47、127.03/139.59/133.64、154.77/128.31/118.30、126.66/123.79/125.83——差距 2.25×，A11 指紋兩邊完全相同（a9c9dc104f057bf640e0132a83e01c12），唯一的內容差異是 libggml-metal（按記錄是註解改動）。兩份報告的 IO 統計逐位元相同（jobs=47991 bytes=2784305152，us/job 25043 vs 24510），所以慢的不是 streaming 路徑。但 13:58 的 sysctl vm.swapusage 是 used=5066.06M / total=6144M（82%），而本 repo 已量過同一份 binary 的 2.17× 散佈（none:5120 三次 req1 = 284.58/157.95/130.88，同一交錯輪次）。13:45 那臂的 swap 用量沒有被記錄，所以兩個候選都無法排除。", "counterexample_observed": "Backup/cgc_logs/prod_accept/req2retest_20260916_135720.txt（閒置 4 分鐘後仍 126.66/123.79/125.83，A11 指紋與 13:45 臂相同）", "applies_to": ["Backup/run_req2_retest.sh", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0033", "class": "measurement-hygiene", "rule": "要量「上一個事件結束到這一個事件開始」，就必須用**產生那些事件的檔案**的創建時間／mtime，不是包住它們的驅動腳本的戳記。驅動腳本的戳記標的是它自己何時開始，而它從開始到結束中間就是它造成的活動——用戳記相減，會把上一輪的執行時間算成兩輪之間的休息。", "because": "2026-09-16：Backup/run_req2_retest.sh 的報告檔名是腳本啟動的戳記（$OUTDIR/req2retest_<STAMP>.txt），內容是跑完才寫。用相鄰報告戳記相減，13:45-13:57 的四臂被記成「111 / 94 / 144 s 的間隔」，而每臂自己的執行約 90 s。改用伺服器 log 重算（下一臂 llama_server_<ts>.log 的創建時間 − 上一臂同類 log 的 mtime）得到真實安靜 **21 / 8 / 55 s**——差一個量級，而且兩個數字支持相反的讀法（前者像「有休息但沒用」，後者像「幾乎沒休息」）。同一個方法也用在 14:31-14:40 的冷態序列上，算得 32 分 13 秒 / 18 / 34 / 7 / 7 s。", "counterexample_observed": "Backup/cgc_logs/prod_accept/req2retest_20260916_{134510,135131,135322,135456,135720}.txt 的「# started」行 vs Backup/cgc_logs/llama_server_20260916_{135132,135323,135458,135722}.log 的創建時間與 mtime", "applies_to": ["Backup/run_req2_retest.sh", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0034", "class": "measurement-hygiene", "rule": "當一個候選變數是「距離上一次同類工作有多久」時，先問它的**配對預測**能不能被反例否證：找一組「間隔最短但最快」與「間隔最長但最慢」的配對，而不是找相關。如果反例出現，真正的自變數通常不是間隔長度，而是**累積**（同一序列裡這是第幾次），而累積與間隔在同一個序列裡是負相關的——這正是為什麼單看間隔會得到一個方向對、預測壞的規則。", "because": "2026-09-16 14:31-14:40，同一顆 binary、同一 A11 指紋 a9c9dc104f057bf640e0132a83e01c12、同一組 [perf]（batch=5632 ubatch=5632 oa_async=1 load_mode=none、MTP ON）：安靜 32 分 13 秒的臂 254.29/282.38/265.27，安靜 18 s 的臂 289.86/261.94/233.51（req1 比第一臂更快），安靜 34 s 的臂只有 188.35/200.58/166.95，安靜 7 s 的兩臂 167-188。**安靜最長的最慢、最短的最快**，所以白皮書 §10.10.4 第二層「距上次持續 prefill ≥150 s」作為充分條件被推翻；§3.3 的「0 s → 122.39 t/s」在冷卻過的機器上也無法複現（差別在前一個狀態：§3.3 的 0 s 樣本繼承的是已熱浸透的機器）。一致的模型是累積：一次啟動（約 40 s prefill）不足以耗盡熱預算，三次以上會；平台高≈185 t/s。", "counterexample_observed": "Backup/cgc_logs/cold_accept/req2retest_20260916_{143136,143550,143719,143835,143956}.txt", "applies_to": ["Backup/run_req2_retest.sh", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0037", "class": "gate-integrity", "rule": "條件式交付的合法性取決於**條件可不可量測**，而不是取決於它聽起來多合理。如果條件不可由可用的儀器觀測（例如需要 root 才能讀的 DVFS 檔位），那麼閘門只能**分類這一輪的宣稱**（COLD／HOT／UNKNOWN），**不能**印出「已滿足前提」——後者是宣稱讀到了一個它讀不到的東西，也就是假證據。同時：候選變數在時間上與真因耦合時（例：swap 在同一個安靜期間與熱一起復原），排除它要靠**順序相反**的配對，不是靠相關。", "because": "2026-09-16：pmset -g therm 只回 'No thermal warning level has been recorded'（非 root 讀不到等級），DVFS 駐留需要 root powermetrics。所以 prefill250 的熱前提不可觀測，只能靠協議（安靜期）近似；而該協議的常見形式『≥150 s』已被 eng-mh-0034 推翻。因此 Backup/run_req2_retest.sh 改成三態標籤：COLD-STATE（距上一臂結束 ≥ COLD_QUIET=1800 s，其 t/s 可被引用）／HOT-STATE（只能當熱態觀察，報告就地印出冷熱對照）／UNKNOWN-STATE（無狀態檔 ⇒ 兩邊都不宣稱）。swap 的排除靠反例順序：289.86 t/s 的臂 swap used=4975.06M、188.35 的臂是 4549.69M、13:51 慢臂是 5066.06M，且那 32 分 13 秒安靜期間 swap 自己從 5066.06M 回到 2743.00M ⇒ 它是同一個潛在變數的果。", "counterexample_observed": "Backup/cgc_logs/cold_accept/req2retest_20260916_143550.txt（289.86 t/s @ swap 4975.06M）vs …_143719.txt（188.35 t/s @ swap 4549.69M）vs Backup/cgc_logs/prod_accept/req2retest_20260916_135131.txt（118.96 t/s @ swap 5066.06M）", "applies_to": ["Backup/run_req2_retest.sh", "scripts/check/powermetrics_gpu_freq.sh", "scripts/check/prefill_certifiability.py", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0035", "class": "measurement-hygiene", "rule": "同一則訊息裡對同一個檔案送出兩個編輯會 race，其中一組靜默消失。危險的不是消失本身，是它留下的形狀：呼叫點落地而定義沒落地時，未定義函式在 `$( )` 裡只讓 stderr 多一行，stdout 是空字串，於是報告印出空白的讀數欄位，`grep '欄位名'` 仍然命中——它看起來像「儀器讀不到」，而不是「儀器不存在」。同一檔案的多處修改一律逐次送出；若已平行送出，收尾用 `grep -n` 確認定義與呼叫都在。", "because": "2026-09-16 15:15，對 Backup/run_req2_retest.sh 同時送出兩個 Edit（加更正註解＋加 _thermal_level() 定義／加機器狀態讀數），兩個都回報成功，磁碟上只有第二個。15:18:46、15:19:49、15:21:26 三臂因此印出 `[thermal pressure] before req1 = `（空白）。該腳本沒有 set -e，所以沒有崩潰、沒有非零退出。三臂的發射時等級由外層 shell 印出（0/2/2），所以那三臂的判準結論仍然有效；in-band 讀數則完全無效。15:26 補回定義後，15:31:04 的臂印出完整的 `before launch / before req1 / … / after req3` 序列。", "counterexample_observed": "Backup/cgc_logs/thermal_gate/req2retest_20260916_151846.txt:51（空白讀數）vs Backup/cgc_logs/thermal_gate2/req2retest_20260916_153104.txt:50-64（完整序列）", "applies_to": ["Backup/run_req2_retest.sh", "agent_harness/CONVENTIONS.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-bound-0003", "class": "honest-bounds", "rule": "寫下「不可觀測」之前，必須把介面列完再下結論，而且措辭要精確到「試過哪些介面、各自的結果」。因為「一個工具不可用」與「沒有工具可用」在結論的措辭上完全同形，而前者是真的、後者常常只是搜尋停得太早。反面同樣要記：找到一個介面不等於找到一個儀器——判準是它與被解釋的量之間的**實際分離度**，不是它的名字或刻度看起來對。", "because": "2026-09-16：白皮書 §11.14.7 寫下「沒有任何非 root 的即時儀器可以回答 governor 選了哪一階」，依據是 pmset -g therm 只回 'No thermal warning level has been recorded' 且 DVFS 駐留需要 root powermetrics——兩句都對，結論錯。真正的儀器是 notifyutil -g com.apple.system.thermalpressurelevel，正是 powermetrics 的 thermal sampler 讀的那條 notify(3) key，不需要 root、約 11 ms/次（實測 20 次 0.225 s）、可 2 Hz 取樣，滿載讀 2、停止後約 47 秒回 0。反面：NSProcessInfo.thermalState（非 root、Foundation、四級刻度，看起來就是那個缺失的儀器）在 367 個樣本裡跨越 ABBA 滿載與 4 分鐘閒置全部讀到 1/fair，沒有區辨力。認證一個儀器的方式是它產生的分離度：notify key 給出 Nominal 帶 6/6 ≥250（最低 253.42）與非 Nominal 帶 0/21 ≥250（最高 211.65），零重疊。", "counterexample_observed": "notifyutil -g com.apple.system.thermalpressurelevel（0↔2 會動）vs osascript -l JavaScript -e 'ObjC.import(\"Foundation\"); $.NSProcessInfo.processInfo.thermalState'（367/367 = 1）；Backup/cgc_logs/thermal_libab.tsv", "applies_to": ["docs/PREFILL250_THERMAL_TRANSIENT_20260916.html", "agent_harness/CONVENTIONS.md", "Backup/thermal_pressure_probe.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0038", "class": "gate-integrity", "rule": "一個條件要在**哪一刻**讀，本身是條件的一部分，必須由資料決定而不是由直覺決定。若某個讀數在事件中途會改變，就要問「改變之後那個事件有沒有被撤回」；如果沒有，閘門讀的是事件開始前那一刻，而不是要求全程成立。而且閘門的讀數與它要授權的數字要**同時同地**取得（in-band），不能事後重建——否則條件的時間戳與數字的時間戳無法對齊。", "because": "2026-09-16：以 notify key 為閘門時，直覺會要求 Level 全程為 0。實測否證：15:18:46 那一臂發射時 0/NOMINAL，外掛 2 Hz 探針顯示 15:19:13 升 1、15:19:24 升 2（皆在臂中途），而該臂三個請求仍是 257.41/253.42/262.64，全部 ≥250。15:31:04 那一臂則全程 0/NOMINAL（after req3 才翻 1），讀數 266.81/269.35/271.64。所以閘門讀『發射前那一刻』：發射 0 → 6/6 ≥250（最低 253.42）；發射 1 或 2 → 0/21 ≥250（最高 211.65）。落後的原因（governor 對熱壓訊號反應有延遲）是假設，HTTP 路徑上沒有 per-request 的 DVFS 證據，所以不寫成機制結論。", "counterexample_observed": "Backup/cgc_logs/thermal_gate/req2retest_20260916_151846.txt（中途升 1→2，仍全部 ≥250）vs Backup/cgc_logs/thermal_gate2/req2retest_20260916_153104.txt（全程 0，全部 ≥250）", "applies_to": ["Backup/run_thermal_gate.sh", "Backup/run_req2_retest.sh", "docs/PREFILL250_CONDITIONAL_DELIVERY_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0036", "class": "measurement-hygiene", "rule": "交錯 A/B 得到「無差異」時，正確的述句是「測不到差異」，而且組內散佈必須與組間差一起報——否則讀者會把雜訊當成效果。但一個 null 只能**否證**一個假設，不能自行**證立**替代假設：替代假設需要另一條獨立的分離證據，否則「不是 A 所以是 B」只是把未歸因換成一個更順耳的未歸因。", "because": "2026-09-16：為裁決白皮書 §11.13 的 2.25x（12:44 產物 268.09/273.55/256.61 vs 最終產物 118.30-154.77，唯一內容差異是 libggml-metal），用 Backup/run_lib_ab.sh 做檔案互換的 ABBA 交錯（一臂暖機丟棄，共 5 臂）。req1 A=121.50/B=122.47（+0.8%）、req2 A=159.68/B=136.69（-14.4%）、req3 A=154.83/B=153.87（-0.6%）：方向不一致，且 A 的組內 span 51.56 > 組間均差 7.66 的 6.7 倍 ⇒ 庫的假設被否證。替代解釋（機器狀態）不是由這個 null 證立的，而是由閘門帶來的第二條獨立證據：268.09/273.55/256.61 落在 Nominal 帶（≥253.42），118.30-154.77 落在 Heavy 帶（≤211.65），分屬分離的兩帶。這是一致，不是直接觀測——那些臂當時沒有等級讀數。", "counterexample_observed": "Backup/cgc_logs/libab/summary_20260916_150641.tsv", "applies_to": ["Backup/run_lib_ab.sh", "docs/PREFILL250_THERMAL_TRANSIENT_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-bound-0004", "class": "honest-bounds", "rule": "一個只發行**指標**（path + hash）的索引，在「把內容送到另一台機器」這個用途下是無效的——推上去的 bytes 是指標，不是內容。判斷一份索引資產夠不夠，要問**消費者在哪裡、它需要指標還是內容**，不是問「索引是否可驗證」。可驗證性與充分性是兩條獨立的軸：索引可以兩者都對，卻仍然服務不到那個消費者。", "because": "2026-09-16：`.workbuddy/` 在 `.gitignore` 裡（工作樹乾淨時 `git status --ignored` 顯示 `!! .workbuddy/`）⇒ 記憶與 skill 從未進過版控。遠端 `cgcengine0907/fusionroutemot` 的 `agent_harness/scripts/auto_git_push.ps1`（commit `1f3b0a78d`，112 行）會定時 `git add agent_harness` 後 push，所以「內容在不在 `agent_harness/` 底下」直接決定它有沒有機會被同步。而 `agent_harness/engine_loop/memory/` 的既有慣例刻意只放 `INDEX.jsonl`（理由：複製一份會製造第二個權威來源，且失效方式是安靜的），該索引逐列只有 `path/sha256(16)/lines/mtime` 與每個 `##` 節的 `line_start/line_end` ⇒ 另一台機器拿到索引也讀不到本文。索引本身沒有錯，`--check` 也真的會抓 drift（本輪 `memory index OK: 3 file(s), 53 section(s), no drift`）；但它服務的消費者（**同一台機器上要知道去讀哪一段**）與「要同步內容的機器」不是同一個 ⇒ 補上 dated 實體快照 `agent_harness/memory/`（3 檔）與 `agent_harness/skills/`（3 skill），每個檔開頭插 banner 標明權威位置，並改寫 `engine_loop/memory/README.md` 那句被這次決定推翻的「若要把內容實體鏡像進 repo…目前刻意不做」。", "counterexample_observed": "`agent_harness/engine_loop/memory/INDEX.jsonl`（指標；`--check` 綠，卻到不了推送線） vs `agent_harness/memory/SNAPSHOT.jsonl`（內容 ＋ `source_sha256`，推送線讀得到）", "applies_to": ["agent_harness/engine_loop/memory/README.md", "agent_harness/memory/README.md", "agent_harness/skills/README.md", "agent_harness/scripts/auto_git_push.ps1"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0037", "class": "measurement-hygiene", "rule": "一個刻意便宜的戳記（取樣視窗、人工列舉清單、抽樣 digest）的**判別力**只能用陽性對照決定，不能由「它涵蓋了什麼」推論——因為真正讓它動起來的位元組，可能不在它涵蓋的範圍內。推論出的洞要先量再修；量出來若是否證，那個否證本身有價值：它會指名偵測力**來自誰**。若來源是別人的實作細節（連結器、編譯器、檔案系統），就沒有任何測試釘住它 ⇒ 應把它變成一條可重跑的陽性對照，而不是一條註解。", "because": "2026-09-16 陽性對照。假設（僅由讀原始碼得出）：`knifeedge_matrix.binary_stamp()` 對每個產物只記 `{size, sha256(前 64 KiB)[:16]}`（`:491`），而 Metal kernel 的位元組在 `libggml-metal.0.19.0.dylib` 的 **offset 169,445 / 177,128**（實測 `b.find(b'kernel_mul_mv_id')`），總長 954,920 B ⇒ 同長度的 kernel 改動應該完全隱形，而 `record_geometry_key()` 用的正是這個弱戳記。做法：把 `ggml-metal.metal:3100` 的 `1.0f` 改成 `1.1f`（**同長度，4 字元**）→ `cmake --build build --target llama-server -j8`（實測印 `Building ASM object …/autogenerated/ggml-metal-embed.s.o` → `Built target ggml-metal`）→ 量。\n結果 **否證**：size 954,920 → 954,920（不變，如預期），但全檔 md5 `f2d1c961…` → `4de03712…`、`binary_stamp()` 的 head `d09373fd93553c6a` → `34722cea73abf044`，`build_fingerprint()` 的 `libggml-metal` `f2d1c9619393` → `4de0371267f8`。兩個戳記都動了。\n機制（`cmp -l` ＋ `otool -l`，見 `Backup/positive_control/`）：兩份產物差 **80 個位元組**，其中 **15 個落在前 64 KiB（最小 offset 1,881）**；`otool -l` 顯示 `LC_UUID` 為 `53F7B392-0230-38B7-BD16-DD6EE690CCC3` vs `639AA7E3-05C1-3AD8-800E-E56E14203B53` ⇒ **ld64 寫的是內容衍生的 UUID**，於是任何內容改動都會動到標頭區的位元組，而標頭區就在視窗內。\n順帶確立的兩件事：(a) 改回原始碼重建後 md5 回到 `f2d1c96193939bd15404ba713a6fa85d`、size 954,920，**byte-identical** ⇒ 這個建置對此產物是可重現的，且「relink 會換 UUID」不成立（UUID 由內容決定）；(b) 重建時 **mtime 變了而位元組沒變**，正好印證 docstring 選擇不用 mtime 的理由。\n殘留（這才是要釘住的部分）：這條偵測力**不是設計出來的**——它依賴 ld64 把內容衍生 UUID 放進前 64 KiB。若某個工具鏈改成固定 UUID，或改動只發生在 64 KiB 之後且不經連結器（例如事後 strip／codesign 造成同尺寸的尾端變動），這個戳記就會對同尺寸改動變瞎。今天這個性質為真，但沒有任何測試在看著它。", "counterexample_observed": "Backup/positive_control/A_samelength_edited.dylib（改動後）、Backup/positive_control/cmp_offsets.txt（80 個差異位元組的 offset 清單，15 個 ≤ 65536）、Backup/positive_control/A_embed.metal", "applies_to": ["scripts/check/knifeedge_matrix.py", "scripts/check/decode_sweep.py", "Backup/positive_control/"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-smoke-0003", "class": "smoke", "rule": "当一个路径变量「恰好」等于另一个时，任何挂在它底下的派生都是定时炸弹：它在被移动之前无法被区分（两个名字指向同一个目录，所以两种写法都对），移动之后则静默指错。锚点要按「它代表什么」显式命名，不是按「它现在等于什么」；而 import 的验证必须带一组对照组，否则「通过」可能只是因为当前工作目录刚好也在 sys.path 上。", "because": "2026-09-16 E1（agent_harness 重组成 tb_loop/）。三支入口脚本写 PYTHONPATH=\"$TB_LOOP_DIR\" 而 import 字串是 tb_loop.agents.*；因为 TB_LOOP_DIR 当时就是 agent_harness/ 本身（tb_loop/ 这个目录还不存在），那一行同时是对的又是错的，而在搬动之前没有任何观测能把两者分开。同样形状的还有 config.env 里的四条路径：它们写 \"${TB_LOOP_DIR}/../app/...\"（仓库根）、\"${TB_LOOP_DIR}/loopmoe/...\"（agent_harness/loopmoe）、\"${TB_LOOP_DIR}/pd_data\"——其中 loopmoe/ 与 pd_data/ 并不随 tb_loop 移动，所以搬完之后它们会被指到 agent_harness/tb_loop/loopmoe 去。处置：config.env 显式拆成三个锚点（TB_LOOP_DIR=tb_loop 自己、TB_HARNESS_ROOT=agent_harness/、TB_REPO_ROOT=仓库根），三支入口的 PYTHONPATH 改指 TB_HARNESS_ROOT，并补 tb_loop/__init__.py 让它是正規套件而不是靠 PEP 420 的 namespace package（namespace package 下 tb_loop.__file__ 是 None，解析依赖 sys.path 上的隐式扫描）。验证方式必须是判别性的：正确 PYTHONPATH 下 tb_loop.agents.{prime_agent_adapter,codebuff_api_agent,loopmoe_agent_adapter} 与 tb_loop.learning.attribution 四个模块都要解析到新位置，且对照组（PYTHONPATH 指回 tb_loop 自己）必须仍然报 No module named 'tb_loop'。", "counterexample_observed": "对照组 PYTHONPATH=agent_harness/tb_loop python3 -c \"import tb_loop\" -> ModuleNotFoundError: No module named 'tb_loop'（证明测试能区分，而不是偶然通过）；以及 local_rehearsal.py / batch_run.sh / refine_harness.sh 用 $(dirname ${BASH_SOURCE[0]})/.. 这类「自带锚点」的写法在搬动后自动保持正确——同一次搬动里，自带锚点的脚本零改动，而共享变量的脚本全部要改。", "applies_to": ["agent_harness/tb_loop/config.env", "agent_harness/tb_loop/run_round.sh", "agent_harness/tb_loop/gen_sft.sh", "agent_harness/tb_loop/finetune/eval_round.sh", "agent_harness/engine_loop/traces/validate.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0038", "class": "measurement-hygiene", "rule": "在把 decode 吞吐的散佈歸因給散熱之前，必須先排除兩個**非散熱**來源，否則同一個數字會同時被兩種東西解釋：(a) **生成長度**——同一臂、同一 server session 內，短生成是**短爆**，長生成才是可持續值，兩者可以差 1.6×；(b) **暖機輪被計分**——`--warmup 0` 讓第一個請求的一次性成本落進一個**評分輪**，於是 `decode_tps_min` 變成「製程暖機」而不是「機器狀態」的訊號。⇒ 報 decode t/s 必須同時報**生成長度**與**該輪的散熱等級**，且要指明那個數字是 median 還是某輪：median 會把首輪吞掉，讓一個 17× 的首輪成本對中位數完全隱形。", "because": "2026-09-16，把 `notifyutil -g com.apple.system.thermalpressurelevel` 接進 `decode_sweep.py`（發射前）與 `decode_bench.py`（每輪兩側）之後，同一支 `p25-gputime` 臂（prod25、MTP=0）跑了三次，**全程 NOMINAL、零個非 0 讀數**：\n  * n=24、`--warmup 0`、兩跑相隔 1 分鐘：median **20.32 / 20.31**，但每跑的 `decode_tps_min` 是 **8.48 / 7.98**。逐輪配對顯示那是**第 0 輪**：wall **23.83 s** vs 兩個 steady 輪的 **1.44 / 1.35 s**（同 n、同 session、同 NOMINAL）。\n  * n=160、`--warmup 1`（丟棄暖機）：median **12.95**，逐輪 12.39 / 12.95 / 13.67，**單調上升**（還在暖機，沒有熱衰退），`predicted_n`=124。\n⇒ 短爆 20.3 與可持續 12.95 差了 **1.6×**，而**等級讀數從頭到尾都是 0**。所以「基線 2.4× 散佈是散熱造成的」這個假設**被這個儀器自己否證**了——它把「一次性成本」與「機器狀態」分開，而兩者在舊資料裡長得一樣。", "counterexample_observed": "Backup/phase_decomp/decode_thermal_hot_20260916.json（round 0 wall 23.83 s / 7.98 t/s vs round 1-2 1.44 s / 20.31-20.76 t/s，`thermal_hist` 6/6 NOMINAL）、Backup/phase_decomp/decode_thermal_sustain_20260916.json（n=160、丟棄暖機、12.39-13.67）", "applies_to": ["scripts/check/decode_sweep.py", "scripts/check/decode_bench.py", "scripts/check/thermal_pressure.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0039", "class": "gate-integrity", "rule": "一個探針對「**輸入不存在**」的回答，必須與它的真實讀數**可區分**；不可區分的探針無法用自己的輸出被驗證，而它的失效方向是把「我們沒有在量」講成「我們量到了一個合理值」。設計或採用探針時要先問：餵它一個不存在的東西，它回什麼？那個回答與「一切正常」長得一樣嗎？若一樣，就只能**(a)** 只接受已驗證過的輸入（allowlist）、**(b)** 把「無法偵測缺席」寫進文件、並**(c)** 用**負載**而非解析器去驗儀器是活的——不要用解析器假裝偵測到它偵測不到的東西。", "because": "2026-09-16 寫 `scripts/check/thermal_pressure.py` 時，它的 `--selftest` 當場抓到自己：`notifyutil -g <不存在的 key>` **回 `0` 且 exit 0**，輸出形狀與真 key 逐字相同：\n    $ notifyutil -g com.apple.system.thermalpressurelevel\n    com.apple.system.thermalpressurelevel 0        # exit 0\n    $ notifyutil -g com.apple.system.this.key.does.not.exist\n    com.apple.system.this.key.does.not.exist 0     # exit 0，同形\n加 `-v` 與 `-q` 都一樣（實測）。⇒ exit code 與輸出形狀**都**不能區分「存在且為 0」與「不存在」。一個信任它們的解析器會在**儀器整個缺席**的機器上回報 `NOMINAL`，而那是「我們沒量到」穿著「一切正常」的衣服——與靜默的 0 同族。\n處置：模組只接受 `SUPPORTED_KEYS`（已實測過的 key）並拒絕其他，把 typo 那一類擋掉；把**無法偵測缺席**寫進 docstring，並指出殘留的緩解在別處——那個 key 的活性是由它對負載的**實測反應**建立的（靜止 0、持續負載 2、停後約 47 s 回 0），所以「一條永遠是 0 的序列」是該起疑的簽名，而檢驗它是**負載測試**，不是解析器。", "counterexample_observed": "`notifyutil -g com.apple.system.this.key.does.not.exist` -> `com.apple.system.this.key.does.not.exist 0`, exit 0（`-v` / `-q` 亦同）", "applies_to": ["scripts/check/thermal_pressure.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0039", "class": "measurement-hygiene", "rule": "Never put a llama-bench tg number next to a decode_bench number without saying which rep/round the number averages over. llama-bench's `avg_ts` averages the COLD first rep in; `decode_bench --warmup 1` discards it. At n=128 that single rep is the whole apparent gap: reported 9.38-9.42 vs plateau 11.3 t/s, against decode_bench's warm 12.36 t/s (1.10x, not 1.32x).", "because": "Same env (prod25 + CGC_SERVER_MTP=0;CGC_GPU_TIMING=1;CGC_DECODE_PROFILE=1, resolved through `run_server.sh CGC_DUMP_ENV=1` for BOTH), same model (the MTP=0 path loads Qwen3.6-35B-A3B-UD-IQ3_XXS.gguf, not the denseIQ4X carrier), same n, interleaved in one session, thermal level recorded on every arm: llama-bench read 7.21 (n=24) / 9.42, 9.32, 9.38 (n=128, three runs 1.1% apart) where decode_bench read 18.74 (n=24) / 12.36 (n=128). Reading the per-rep DECPROF series instead of the average shows rep 1 at 120 ms/token and rep 2-3 at 86-93 ms, i.e. `avg_ts` = 9.4 while the engine's own steady state is 87-89 ms = 11.3 t/s. The cause is visible in the source: llama-bench's tg warmup is `test_gen(ctx, 1, ...)` -- ONE token -- and the context (hence the expert pool) is created per instance, so the pool starts empty and the first rep pays the compulsory-fill cost (its own miss attribution: compulsory 4437/5195 = 85.4%).", "counterexample_observed": "Backup/phase_decomp/lb_n128_match_20260916_1807.json (9.42) + lb_n128_match2 (9.32) + p2-phase anchor (9.38) vs db_n128_match_20260916_1807.json (12.36); per-rep DECPROF means n=128 r=3 -> r1=120 r2=86 r3=93 ms, n=256 r=3 -> r1=116 r2=87 r3=87 ms", "applies_to": ["scripts/check/llama_bench_matrix.py", "scripts/check/decode_bench.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0040", "class": "measurement-hygiene", "rule": "Do not use llama-bench as the instrument of record for this engine's decode target. It has no sampler, so it cannot execute the speculative/MTP path at all, and it advances the sequence with `std::rand() % n_vocab`, so it does not measure the token stream a served request produces.", "because": "`grep -c 'sampler|speculat|draft|MTP' tools/llama-bench/llama-bench.cpp` is 0. `test_gen` is `llama_decode(1 token); llama_synchronize(ctx); token = rand()%n_vocab;` (llama-bench.cpp:2168-2183) -- no sampler, no draft, no verify, no accept. Every remaining lever for the 25 t/s decode target is MTP, and MTP needs a sampler to decide acceptance, so llama-bench can only ever report the MTP *model* running without speculation (measured: 8.75 +- 0.95, i.e. no benefit visible). The random stream is not a cosmetic detail either: it changes what the expert pool is asked to do -- random ids gave 96.0% hit with 85.4% COMPULSORY misses and only 7 of 40 layers over-subscribed, where the same engine serving coherent text gave 92.6% with 51.0% compulsory / 49.0% capacity and ALL 40 layers over-subscribed, reading 3.35x the bytes (18.35 GiB vs 5.48 GiB). A stream whose expert union is unpredictable is also the stream a prefetcher cannot help, which is a plausible reason llama-bench's higher hit rate coexists with its lower t/s.", "counterexample_observed": "BACKUP phase_decomp/lb_n128_mtp_20260916_1807.json (8.75 +- 0.95, prod25 profile, MTP=1 -> denseIQ4X + bit-identical pillars, thermal launch HEAVY) vs the MTP=0 arms at 9.32-9.42; and the two miss-attribution lines: llama-bench 'compulsory=4437 capacity=758 ... layers_distinct_over_slots=7' vs server 'compulsory=8874 capacity=8522 ... layers_distinct_over_slots=40'", "applies_to": ["scripts/check/llama_bench_matrix.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0041", "class": "measurement-hygiene", "rule": "Never compare the expert cache's hit%/misses/reads between two harnesses. Those counters are process-lifetime accumulators, and the two processes accumulate over different work: llama-bench's cover its own warmup + reps of tg only, the server's cover model load + warmup request + prefill chunks of a chat prompt + every round. Only ratios that are defined inside one process (miss attribution split, over-subscribed layer count) survive the comparison.", "because": "The same engine, same pool, same env reported hit 96.0% in the llama-bench process and 92.6% in the server -- and the higher-hit side was the SLOWER one (9.4 vs 12.4 t/s). That inversion is what makes the naive reading wrong, and it is not a defect in either number: 128,920 requests over ~400 tokens in one process vs 234,840 over a different mix (including ~100 prefill tokens at n_batch=8) in the other. Comparing them is the same failure as comparing t/s across instruments, one level down.", "counterexample_observed": "llama-bench: 'final stats: runtime requests=128920 hits=123725 misses=5195 (hit rate 96.0%) file_reads=15516 ... effective_rate=309 MiB/s' vs server: 'requests=234840 hits=217444 misses=17396 (92.6%) file_reads=49320 ... effective_rate=217 MiB/s'", "applies_to": ["scripts/check/decode_sweep.py", "scripts/check/llama_bench_matrix.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0013", "class": "source-reading", "rule": "Two windows that both look like 'the generation' are not: llama-bench's tg warmup is one token (`test_gen(ctx, 1, ...)`), and the server's `predicted_ms` starts at the first SAMPLED token, so a request is scored as N tokens over N-1 steps.", "because": "Both were established by reading, then confirmed by measurement. (1) warmup=1 token: predicted '--no-warmup will change nothing', measured 9.40 +- 1.19 with it off vs 9.38 +- 1.02 with it on -- and the off arm ran at HEAVY where llama-bench is slower, so the null result is if anything conservative. The same read also refutes the harness's own --no-warmup help text ('warmup is what leaves the expert pool hot, so the default is the closer analogue of a served request'); one token leaves nothing hot. It is also consistent with the print accounting: 1 warmup graph + 3x128 rep graphs = 385, and the gate prints every 8th plus step 1 -> 49 lines, which is exactly what the log has. (2) `t_start_generation = t_now` is set inside `if (slot.n_decoded == 1)`, AFTER the sampler has already synchronized -- server-context.cpp:4085-4099 -- so `predicted_ms` spans N-1 decode steps while `predicted_n` = N, an N/(N-1) optimistic bias (+4.3% at n=24, +0.8% at n=124).", "counterexample_observed": "llama-bench.cpp:2392 `test_gen(ctx, 1, t.n_threads)` and server-context.cpp:4092 `slot.t_start_generation = t_now;`; measured 49 printed DECPROF graphs for a 1+384-graph run, and BACKUP phase_decomp/lb_p2_n128_nowarm_*.json (9.40) vs lb_p0_anchor_n128_*.json (9.38)", "applies_to": ["scripts/check/llama_bench_matrix.py", "scripts/check/decode_bench.py", "src/llama.cpp/tools/server/server-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0024", "class": "diagnosis", "rule": "Before any cross-arm diff from a NEW instrument, run the SAME arm twice and diff it against itself. A readout that is not reproducible under that control cannot support any cross-arm conclusion, no matter how plausible the result looks.", "because": "The first version of the tensor-output capture read the node's dst without a memory barrier. Same-arm control (p25-gputime captured twice, diffed against itself): ids SAME 120/120 on every graph, while `ffn_moe_down-1.dst` DIFFERED on EVERY graph. The cross-arm run then showed exactly the pattern the hypothesis predicted -- 'identical ids, different output, therefore the ids point at different weights' -- i.e. an unreproducible readout produced a CONFIDENT FALSE POSITIVE in the direction of the thing under test. With the barrier added, both same-arm controls (baseline arm and S1 arm) came out SAME on every graph, and the cross-arm answer flipped.", "counterexample_observed": "same-arm, no barrier: 'ffn_moe_down-1.dst DIFF' on graphs 1..30; same-arm, with ggml_metal_encoder_memory_barrier: 'ffn_moe_down-1.dst SAME' on graphs 1..35. Same binary otherwise, same prompt, same arm.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "Backup/run_ids_dst_capture.sh", "Backup/analyze_dst_capture.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0025", "class": "diagnosis", "rule": "A second capture stream that must be paired with an existing one has to be emitted in SUBMISSION order across both streams, not appended after the first. And when a capture is placed after a kernel whose output it reads, it needs an explicit memory barrier if the fork allows kernel concurrency.", "because": "Two separate traps, both silent. (1) ids_capture_diff.py segments graphs at `ffn_moe_gate-1`, an ids row: had the dst rows been printed after all ids rows, every dst row would land in one final pseudo-graph, get compared against nothing, and be reported as IDENTICAL -- a false negative in the one direction this instrument exists to rule out. Solved with a shared submission sequence number and a merge in the dump. (2) The ids operand is produced MANY nodes upstream of mul_mat_id and has long settled; the dst is produced by the IMMEDIATELY PRECEDING kernel. That asymmetry is the diagnostic signature in the control: ids reproducible, dst not. It points straight at ordering, not at values.", "counterexample_observed": "no barrier: ids 120 SAME / dst DIFF every graph, same arm. With barrier: both SAME. The asymmetry was the tell that identified the mechanism.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "scripts/check/ids_capture_diff.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0014", "class": "source-reading", "rule": "kernel_cgc_ids_capture is a generic 'copy up to `stride` int32 words from buffer 1 into slot N of buffer 2' kernel -- it has nothing ids-specific in it. Pointing the instrument at a tensor OUTPUT therefore needs no new kernel, no new kargs and no new pipeline: only a second destination buffer with a wider stride and a second submission site.", "because": "Reading the kernel instead of assuming an ids-capture kernel must be special-cased saved .metal, ggml-metal-impl.h, ggml-metal-device.{h,cpp} and ggml-metal-context.m entirely -- the whole extension is one file (ggml-metal-ops.cpp) plus a three-line env allowlist. It also kept the existing comparator usable unchanged, because the row format is the same.", "counterexample_observed": "extension touched: ggml-metal-ops.cpp (new cgc_submit/cgc_dst_capture/capture_init + seq merge), ggml-metal-ops.h (comment), run_server.sh (allowlist). Unchanged: ggml-metal.metal, ggml-metal-impl.h, ggml-metal-device.h/.cpp/.m, ggml-metal-context.m.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0042", "class": "measurement-hygiene", "rule": "The node-name filter for a targeted capture must match the name EXACTLY. Substring matching looks harmless and captures ten layers instead of one.", "because": "`ffn_moe_down-1` is a substring of `ffn_moe_down-10` .. `ffn_moe_down-19`, so a substring filter for layer 1 would also capture layers 10-19. The comparator pairs nodes BY NAME and keeps the FIRST occurrence, so the pairing would then depend on which layer's node happened to be encoded first -- a silently wrong comparison rather than an error.", "counterexample_observed": "run_server.sh's allowlist drops any CGC_* it does not list, so a wrong or dropped filter value is indistinguishable from 'the node never ran' -- the value is a node NAME, which makes the silent failure mode worse than for a boolean knob.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "scripts/run_server.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0026", "class": "diagnosis", "rule": "S1's carrier is NOT layer 1's MoE weights. With the output capture working, layer 1's MoE output (`ffn_moe_down-1.dst`) is bit-identical between the baseline and the S1 arm on every graph where the two arms still process the same token, and layer 0's and layer 1's ids are identical while layers 2..39 differ. The divergence therefore enters after layer 1's MoE output and at or before layer 2's router. Treat REMAP plan 9.18.4 and 9.18.7 as REFUTED / unsupported.", "because": "9.18.4 was an elimination argument, and its premise was wrong. It reasoned: on graph 3, layer 1's gate/up/down ids are identical and layer 2's router differs, so under identical input and identical ids layer 1's MoE output must differ, so the carrier is the weight content the ids point at. Direct measurement of that output says it does NOT differ. Since layer 1's MoE output being bit-identical also implies layer 1's attention output is identical (a different attention output would not produce a bit-identical MoE output), the remaining candidates are layer 2's attention path (q/k/v, rope, KV read) or the norm/residual between layers 1 and 2 -- not the expert pool. 9.18.7's 'the thing to fix is residency' rested on 9.18.4, so it loses its only direct evidence.", "counterexample_observed": "graph 4 and 5: SAME = [ffn_moe_down-0, ffn_moe_down-1, ffn_moe_gate-0, ffn_moe_gate-1, ffn_moe_up-0, ffn_moe_up-1]; DIFF = layers 2..39 (114 nodes). ffn_moe_down-1.dst = SAME on graphs 1..30 in two independent repeats. Layer 0's router ids being SAME is also what rules out 'the two arms are simply processing different tokens'.", "applies_to": ["docs/REMAP_ROUNDTRIP_REMOVAL_PLAN_2026-09-15.md", "src/llama.cpp/src/llama-graph.cpp", "scripts/check/ids_capture_diff.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0040", "class": "gate-integrity", "rule": "一個比較器必須先被證明**能給出「沒有差異」這個答案**，才有資格說它給出了「有差異」。只有陽性檢查的比較器，與一個「永遠回答不同」的裝置，在輸出上不可區分——而後者的失效方向正是把雜訊講成效果。判準是成對的：同一個儀器、同一組輸入，只改那個自變數（這裡是注入範圍），一次必須回「相同」、一次必須回「不同」，且兩次都要有可回歸的斷言。配套的欄位設計：當某臂自己前後不一致（reps 之間），「不能比」必須與「比過了，相同」長得不同——`null` ≠ `false`。", "because": "2026-09-16 E3（`agent_harness/engine_loop/distill/closed_loop.py` 的四臂閉環對照）。第一版只有陽性斷言：A vs B 與 C vs D 各自被觀察到「不同」。它從未被證明能在該說「相同」時說「相同」，於是「lesson 有影響力」與「這個比較器對任何輸入都報不同」這兩件事在輸出上同形。修法是把 C/D 那一對做成成對斷言：`--memories-scope none` 讓 D 臂的 prompt 與 C 臂位元組完全相同 ⇒ `C_vs_D_identical` 必須是 `true`；換成 `first:8` ⇒ 必須是 `false`；同一個假模型、同一組題目、只換 scope。第二個配套是 `compare.json` 的 `comparable` 欄：某臂的 reps 之間不一致時，跨臂欄位寫 `null` 而不是 `false`——把沒有資訊的題目記成 `false`，就是在無資訊的地方產生一個看起來有資訊的欄位。", "counterexample_observed": "`closed_loop_selftest.py` 的 C 組（`scope=none` ⇒ `C_vs_D_identical=true`）就是這條的可執行反例：把它拿掉，`closed_loop.py` 的所有陽性斷言在一個「無條件報不同」的實作上照樣全綠。同源的既有條目（互補而非重複）：`eng-gate-0002`（閘門必須先證明會拒絕）、`eng-mh-0037`（判別力只能靠陽性對照決定）、`eng-gate-0012`（每臂都相同的讀數不能當證據）。", "applies_to": ["agent_harness/engine_loop/distill/closed_loop_selftest.py", "agent_harness/engine_loop/distill/closed_loop.py", "scripts/check/m123_oracle_gate.py", "scripts/check/ab_interleave.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0027", "class": "diagnosis", "rule": "The first divergence between the baseline arm and the S1 arm is INSIDE layer 2's gated delta-net core. Layer 0's and layer 1's outputs (`l_out-0`, `l_out-1`) are bit-identical on every properly delimited pass; layer 2's attention INPUT (`attn_norm-2`) and BOTH of its input projections (`z-2`, `gate-2`) are bit-identical; layer 2's attention OUTPUT projection (`linear_attn_out-2`) differs from graph 4 on. So no dense matmul in layer 2 is the divergence point, and neither is anything before it. Narrow eng-diag-0026's window from 'at or before layer 2's router' to 'between z-2/gate-2 and the ssm_out projection' -- i.e. conv1d / delta-rule scan / state.", "because": "Measured 2026-09-16 with CGC_TENSOR_CAPTURE extended to five dispatchers (mul_mat, mul_mat_id, flash_attn_ext, bin, norm), two independent cross-arm pairs, after both same-arm controls came back clean (2 arms x 2 processes x 23 graphs x 11 nodes = zero differences). Restricted to graphs 1..23 because the IDS destination caps at 4096 slots and the tail chunks therefore merge several forward passes. What this refutes: eng-diag-0026 left two candidates standing, 'layer 2's attention (q/k/v, rope, KV)' and 'the norm/residual between layers 1 and 2'. The second is now excluded outright (attn_norm-2 is identical, and so is l_out-1), and the first is narrowed to the recurrent core: the layer's dense projections are all identical. Note the deliberate limit: this localises the FIRST divergence, it does not exonerate the pool anywhere else -- layer 2's MoE does differ, but only downstream of this, so it carries no information.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "docs/REMAP_ROUNDTRIP_REMOVAL_PLAN_2026-09-15.md", "Backup/run_ids_dst_capture.sh"], "superseded_by": "eng-diag-0028", "counterexample_observed": null}
{"type": "lesson", "lesson_id": "eng-src-0015", "class": "source-reading", "rule": "A capture placed at the tail of a Metal dispatcher must name `ctx->node(idx + n_fuse - 1)`, not `ctx->node(idx)`. Every dispatcher that can fuse ends by REPOINTING bid_dst at the last node it covered (`ggml-metal-ops.cpp`: `if (n_fuse > 1) bid_dst = ggml_metal_get_buffer_id(ctx->node(idx + n_fuse - 1));`), and the dispatchers that cannot fuse end in a constant `return 1;` -- so the same expression is correct in both cases. Getting this wrong silently mislabels a fused tensor as its first node.", "because": "This is what makes it safe to instrument a dispatcher with one line at its end instead of chasing each of its internal paths: for mul_mat alone there are eight `set_buffer(..., bid_dst, ...)` sites and not all of them are on any given call's path. The rule also has a visible consequence worth knowing before trusting a name: ggml gives every tensor a name, so an unnamed node arrives as an auto-generated `node_NNN`. When a fused group therefore ends at such a node, the row is emitted under THAT name and an explicit filter never matches it -- the row for the node you asked about simply does not appear. Measured: `norm-2` appears 41/41 in one arm and 0/41 in the other purely because the norm dispatcher fused its following MUL in one arm and not the other. So a node's presence in the stream depends on the fusion state, which depends on node order -- see the coupled lesson on ABSENT not meaning SAME.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp"], "superseded_by": null, "counterexample_observed": null}
{"type": "lesson", "lesson_id": "eng-mh-0043", "class": "measurement-hygiene", "rule": "Row emission order is NOT computation order, and it is not even stable: the same arm, captured twice, emits the same nodes in a different order (measured: `linear_attn_out-2` before `attn_residual-2` in one graph and after it in another, within one process). Therefore any inference of the form 'this row came first, so this node computed first' is reading noise. Localise by the SOURCE chain (the builder's order), which is knowledge, not by the stream, which is not.", "because": "The encode loop itself is strictly ascending and deterministic (`for idx < n_nodes: ggml_metal_op_encode(ctx, idx)`), so the instability has to come from the node list the scheduler hands the backend, not from the encode loop -- which also means it is not something an instrument can fix. It was found while trying to explain an apparent reordering, and it retroactively explains a puzzling enumeration result in the same round. The values, by contrast, ARE reproducible (both same-arm controls were clean over 23 graphs x 11 nodes), so the readout is sound; only its ORDER is not. Consequence for tooling: segmenting a stream at a name marker (as ids_capture_diff.py does at `ffn_moe_gate-1`) is robust to this; pairing rows by position would not be.", "applies_to": ["Backup/analyze_capture_nodes.py", "scripts/check/ids_capture_diff.py"], "superseded_by": null, "counterexample_observed": null}
{"type": "lesson", "lesson_id": "eng-mh-0044", "class": "measurement-hygiene", "rule": "Two ways this capture stream can look like agreement when it is not, and both are now printed rather than inferred. (1) ABSENT is not SAME: a node is captured only when it happens to be the last node of its fused group, and the fusion state depends on node order, so `norm-2` appears 41/41 in the baseline arm and 0/41 in the S1 arm -- a name missing from one log must be reported as not-comparable, never as identical. (2) The last graph chunks are not delimited: the IDS destination is capped at 4096 slots and a pass costs ~114, so the tail merges several forward passes and pairing by name there compares rows from different passes -- bound the analysis (--upto 24 for these runs) before believing any tail divergence.", "because": "Both were caught by controls that existed for other reasons: (1) by a per-name row count, after a same-arm control flagged a node at graph 27; (2) by noticing that a node appears 41 times while the segmentation reports only 36 chunks, which means chunk 36 holds ~5 passes. The failure mode they share is the one this project treats as worst: a silent false negative, or its twin, a divergence that is an artifact of the analysis rather than the engine. The instrument was also made to fail loudly here: DST slot exhaustion now emits one WARN instead of truncating quietly.", "applies_to": ["Backup/analyze_capture_nodes.py", "Backup/run_ids_dst_capture.sh", "src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp"], "superseded_by": null, "counterexample_observed": null}
{"type": "lesson", "lesson_id": "eng-diag-0028", "class": "diagnosis", "rule": "The first divergence between the baseline arm and the S1 arm does NOT sit at a fixed layer number -- it sits at the first layer served by the GPU slot table, i.e. it tracks CGC_S1_MIN_IL. Measured with the gate moved and nothing else changed: MIN_IL=1 gives first_diff at `l_out-1`, MIN_IL=2 gives first_diff at `ffn_moe_out-2` while `ffn_moe_out-1` and `l_out-1` both become bit-identical. So 'layer N is broken' and 'the divergence propagates numerically from layer 0' are both dead; every earlier sentence of the form 'the first divergence is at layer N' has to be rewritten as 'the first GPU-table-served layer'.", "because": "This also retires the previous localisation (`eng-diag-0027`, which named layer 2's gated delta-net core), because two of its premises did not survive: (a) its window started at element 0 of every tensor, and a ggml tensor is ne0-fastest, so on a (2048,T) output that window IS token 0 -- every 'SAME' in that round meant 'same at token 0', not 'same'; (b) the same layer 1 is bit-identical when the host leaf serves it and differs when the GPU table does, so the carrier is the served path and not the layer. The cleanest form of the result is a single sentence with the gate as the independent variable.", "counterexample_observed": "`CGC_S1_MIN_IL=2` run (two arms x two rounds, same-arm controls clean on both): first_diff moved from `l_out-1` to `ffn_moe_out-2`, and `l_out-1` flipped from DIFF to SAME. On that same pass layer 0's 15 captured nodes, the `mul_mat_id` ids (120/120) and the pool layout / consumed set (40/40) are all identical -- so nothing about layer 0 or the input to the gated layer differs.", "applies_to": ["src/llama.cpp/src/llama-context.cpp", "src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0041", "class": "gate-integrity", "rule": "A name filter with a fixed-size buffer silently drops its own tail, and the drop is invisible from both ends. `CGC_TENSOR_CAPTURE` is copied into a fixed `dst_filter[N]`, so a long comma-separated list is cut MID-NAME: a 23-name request (312 chars) fitted only 19 names against a 256-byte buffer, truncating inside the 20th, and the four lost names included the one node that round existed to read. The second half of the defect is on the consumer side: the analyser listed only the names that appeared, so a name that was dropped by the filter printed as 'nobody asked for this' -- the same shape as a name the producing op never emitted.", "because": "Capability and request are two different facts, and both failure modes look like silence: a truncated filter and an op that was never hooked are indistinguishable unless the instrument prints the EFFECTIVE list it accepted and the consumer prints the names it was asked for but never captured. Fix is two-sided by construction: raise the buffer and warn on truncation at init, print the effective list at startup, and have the analyser emit a 'requested but never captured' block with the three possible causes (filter truncated / name belongs to a VIEW with no kernel / op not hooked).", "counterexample_observed": "The round was internally consistent and looked like a clean negative result: every node reported `never`, and the same-arm control agreed. It was committed as evidence before the 256-byte buffer was found; the truncation point sat inside `attn_residu`. After raising the buffer to 2048 the same request produced the missing nodes and the verdict changed.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "Backup/analyze_capture_nodes.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-bound-0005", "class": "honest-bounds", "rule": "A delivery mechanism that a document states to be periodic is not thereby running; it has to be re-measured before it is cited. 'The convention says the pusher pushes' is a claim about a machine, and the machine can be a different one. The cheap discriminator is the artifact the mechanism creates on its first successful run (a log directory, a stamp file): if that artifact is absent, the mechanism has never run HERE, whatever the document says.", "because": "`CONVENTIONS.md` D6's 2026-09-16 amendment reads as a standing fact -- `auto_git_push.ps1` periodically `git add agent_harness` then pushes -- and it is the stated reason the dated snapshots exist. The script is Windows-only and hard-coded to `D:\\alex\\flashkv0516\\cgcengine_full`, remote `cgc0907`, branch `fusionroutemot`; this machine has no `pwsh`/`powershell`, no crontab entry, no LaunchAgents entry, and no `agent_harness/scripts/logs/` (which the script creates on its first run). So the memory body could not have been reaching the remote from this machine at any point. Any sentence of the form 'the snapshot is how the memory reaches the push line' inherits that assumption.", "counterexample_observed": "`git ls-remote` on the real remote returns `1f3b0a78d` for `fusionroutemot`, whose message is `auto(agent_harness): periodic backup 2026-09-16 14:04` -- the script's own format, so it did run somewhere, once; nothing after it, and none of the three later snapshot commits are ancestors of it. The current work is on a different branch (`demo/sweet-spot-windows-fix`) that the script does not target.", "applies_to": ["agent_harness/scripts/auto_git_push.ps1", "agent_harness/engine_loop/memory/README.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-bound-0006", "class": "honest-bounds", "rule": "A durable record's effective capacity is set by its weakest consumer, not by the disk, and exceeding it fails silently by losing the TAIL. A canonical file that is read whole by an injection step with a fixed limit therefore degrades from the back end, where the most recently added (and often most actionable) material lives -- and the file keeps reporting its true size, so nothing looks broken. Split the record by topic into an index plus topic files, and before splitting verify that every consumer of it enumerates rather than whitelists, or the new files will be silently missed -- which is the same defect in the other direction.", "because": "Found when the project's cross-day memory file reached 19,240 bytes and began arriving truncated at session start: the tail was simply not there, and the tail is where the live status of the current line sits. The tooling turned out to be glob-based on all three sides (the memory indexer, the asset manifest, and the snapshot importer), so an extra `.md` in the memory directory is a supported shape rather than a schema violation -- but that had to be read out of the three scripts first, because 3 new files would otherwise have been picked up by none of them.", "counterexample_observed": "After the split the canonical file is 5,660 bytes (was 19,240) while the four files together are 25,581, i.e. the record got larger and the injected part got 3.4x smaller. The query interface still resolves material that moved: `--query SEL-DRIFT` returns `MEMORY_S1.md:8-22` and `:23-44`, and `--query OVERSUBSCRIBED` returns `MEMORY_FACTS.md:6-26`.", "applies_to": [".workbuddy/memory/MEMORY.md", "agent_harness/engine_loop/memory/build_memory_index.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0016", "class": "source-reading", "rule": "A comment that explains WHY a mechanism exists reads exactly like a description of the current state, so it cannot be used to conclude 'X is still the case'. Before drawing a status conclusion from a comment, resolve the FLAG the comment names and read that flag's own state -- and look for a loud warning emitted alongside it. A comment about a decoupling describes what it decouples FROM, which is the past, not the present.", "because": "Fell for it on the CGC_POOL_SPLIT comment in llama-model-loader.h: it says the split keeps the expert tensors at full width and then explains '... which is exactly the coupling that forces n_batch down to cgc_pool_max_tokens() when the tensor is shrunk'. Reading that second half as the present tense produced a wrong status report -- 'M1 has not started' about a work item that was in fact implemented and separately judged unusable. The truth was in a different file, two lines of a WARN: llama.cpp emits 'CGC_POOL_SPLIT is an EXPERIMENTAL, NOT USABLE configuration' with its measured failure (M1 2/42, 234 buffer-is-nil, warmup OOM at both 10 GiB and 4 GiB on a 16 GB box). The same nominal status also exists in the roadmap's own record, which says the numeric gate was ACHIEVED -- the two sources only look contradictory until you notice they describe different work items.", "counterexample_observed": "Corrected within the hour against docs/roadmap-2026-09-14/ROADMAP_PREFILL250_DECODE25_2026-09-14.html, which records M1's numeric gate as reached (M1=M2=M3=117/117 at 2/4/6/8/10 GiB, including the union>slots case at 2 GiB) while listing two exit conditions still open (decode must not regress -- measured 0.72x; prefill chunk 2048 -- needs work item 2, which does not exist). Related trap, same repo: 'M1/M2/M3' names both roadmap milestones and the D5 gate's three verdict metrics, so a status question about one reads as a status question about the other.", "applies_to": ["src/llama.cpp/src/llama-model-loader.h", "src/llama.cpp/src/llama.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0029", "class": "diagnosis", "rule": "An instrument that exists to decide 'is the SAME thing really the same' must record the INDEX the consumer used in the same record as the digest of the data behind it, and the comparator must read the index field FIRST. A digest alone cannot separate two different defects: 'the two arms selected different indices' and 'the same index holds different data' both print as one word, DIFF. Print them side by side, write the branching decision rule into the comparator, and make the 'no difference' verdict state what it does and does not license. Corollary for the probe width: clamp the probe to the row stride, then report the clamped width -- if the width equals the stride, 'identical' is a statement about the whole row rather than about a prefix, and that is a strictly stronger claim for the same code.", "because": "The S1 localisation had been reduced to one sentence, 'the two arms consume the same ids and the same pool layout, yet the gathered bytes differ', and the pre-existing probe for it reads the weights from the HOST during graph ENCODING (op->src[0]->data + id*nb02). On the arm under investigation the ids are a graph-computed node, so at encode time that pointer is unfilled and the probe reports 16/16 out-of-range ids made of float bit patterns -- it tracks who last wrote the buffer, not what the consumer received. Moving the same read into the Metal kernel, submitted into the same encoder right after the consuming kernel, produced the answer on the first run: over 30 graphs, including the first single-token step, the selected slots hold byte-identical rows (probe = nb02 = 335872/450560 B), and on the 5 graphs where the rows differ the IDS differ. So the hypothesis the probe was built to test was refuted rather than confirmed -- and a digest-only instrument would have printed DIFF there and read exactly like a confirmation. Recording the id in each row is what prevented that false positive.", "counterexample_observed": "Two interleaved-free A/B runs of 36 graphs each, probe 4096 B and probe = whole row, both gave SAME=90 DIFF=15 NOT-READ=0 with FIRST DIFF at graph 31 (ffn_moe_down-1.pool) and a different id in row 0 of every differing pair; the same-arm control (one arm, twice) was 99/99 SAME at both probe widths. At the first single-token step the layer-1 ids are still identical while layer 0's attn_norm output already differs, so the divergence is introduced in layer 0 and the routing follows one step later.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal.metal", "Backup/compare_pool_row.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0042", "class": "gate-integrity", "rule": "A driver's closing step must be able to fail the driver. A `python3 - <<'PY'` heredoc inside a bash script inherits no `set -e`, so a traceback in the final analysis step leaves the run's exit status at 0 and the run reads as complete -- and the step that gets skipped is exactly the one the next reader reaches for. Check the status of every heredoc/analysis step explicitly, and treat a traceback printed between two normal lines as a silent failure rather than as noise. Related and easy to miss: a path join written `p / \"a\" + name` parses as `(p / \"a\") + name`, so a Path-plus-string TypeError hides inside a line that looks like a path join.", "because": "Adding one counter to the closing heredoc of the capture driver exposed that its last line had been raising `TypeError: unsupported operand type(s) for +: 'PosixPath' and 'str'` on every run since it was written, while `arms.json` (one line above it) was written fine -- so the per-arm copies named `latest_*` never existed and nothing downstream complained. The failure was visible only to someone reading the script's own stderr, and the surrounding output looked complete. A repo-wide grep then showed no consumer of `latest_*`, which is why the impact was a missing artifact rather than a wrong number -- a distinction that could only be made after finding the bug.", "counterexample_observed": "Run 2026-09-17 09:11: the driver printed both arms' row counts, then the TypeError, then continued through every analysis section and printed its final '=== done ===' banner with exit status 0; the reference to the missing `latest_*` copies is the only thing that was silently absent.", "applies_to": ["Backup/run_ids_dst_capture.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0030", "class": "diagnosis", "rule": "When a verdict is about an OPERAND rather than a tensor, the instrument's record budget must cover the operand's full size, and the verdict must name the prefix it actually covered. For a 2-D ids operand the first N rows are NOT a sample -- a ggml tensor is ne0-fastest, so they are exactly TOKEN 0, and the remaining tokens of the chunk are simply not in the reading. 'The ids are identical' then silently means 'token 0's ids are identical', and the difference it hides can be the whole divergence. Widen the budget and re-run before quoting the verdict; if the budget cannot reach the operand's size, state the covered prefix in the conclusion itself.", "because": "The device-side pool-row digest reads the first `rows` ids (default 8 = n_expert_used) and digests the row each selects. At T=1 that is the whole operand, so the 6 decode graphs and the first 2-token graph were fully covered and everything agreed -- and the verdict went out as 'same ids, same rows, on every graph'. At T=2 the operand holds 16 ids and at T=8 it holds 64, so every multi-token graph of the run was a token-0-only reading. Widening to rows=12 exposed it on the next run: at the SECOND graph of the run, token 0's 8 ids are still identical across arms while token 1's differ (the S1 arm returns repeats of slot 0 where the anchor returns distinct slots), which is the real first divergence of the run -- one graph earlier than the localisation had placed it, and it explains a whole-tensor difference at a node (layer 1's MoE output) whose input activations, router logits, routing weights and token-0 rows were all byte-identical. The earlier 'first divergence' reading was not wrong about what it compared; it was wrong about how much it covered.", "counterexample_observed": "Run 2026-09-17 10:22 (rows=12, probe = whole row, whole-tensor digests): graph 1 token 0 ids A=[2,105,7,9,5,106,1,84] B=[2,105,7,9,5,106,1,84] (identical), token 1 ids A=[8,0,50,4] B=[0,103,0,74] with slot 0 appearing twice in B and carrying the same row content both times; graph 2 (T=8) the same shape at token 1. At rows=8 (the default) those four positions were never read, and the comparator reported SAME for the whole graph.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal.metal", "Backup/compare_pool_row.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0045", "class": "measurement-hygiene", "rule": "A diagnostic whose PRINT is capped is not a whole-run reading: the cap silently turns 'the first N instances' into 'the answer', and a negative conclusion drawn from it is a conclusion about the cap, not about the run. When an instrument prints both per-instance lines and an aggregate counter, the aggregate is the reading and the per-instance lines are samples -- so a zero in the samples can coexist with a non-zero total, and the total is what a claim must quote. Check the print cap before quoting a zero.", "because": "Two independent instances in one round. (1) The slot-table churn instrument prints per-graph lines only for the first 10 graphs (graphs 0-9 were all `SEL-DRIFT entries=0`), and the earlier 'timing/race is refuted' conclusion was read off those lines -- while the same run's whole-run counter says `consumed_changed=192`, i.e. the consumed subset moved 192 times, necessarily after graph 9. (2) The slot-table clamp warning carries an explicit cap (`cgc_clamp_lines++ < 16`) and prints 16 identical lines, which reads like '16 layers are affected' when it is 'the print stopped at 16'. Both were in the same log as the measurement that used them.", "counterexample_observed": "Run 2026-09-17 10:29 (p25-slotgpu-churn, CGC_S1_TABLE_CHURN=1): 10 graph-level `TABLE-CHURN` lines, all with `SEL-DRIFT ... entries=0`, and the aggregate line `S1 slot-table: publishes=1599 clamped_selected=0 clamped_table=180687 changed_entries=572 consumed_changed=192 consumed_unchanged_publishes=198`. The per-`il` clamp warning printed exactly 16 lines for il=1..16 on every layer that was checked.", "applies_to": ["src/llama.cpp/src/llama-context.cpp"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0031", "class": "diagnosis", "rule": "An annotation added to a reading must be sampled at the SAME instant as the reading, and if that instant is not the end of the step, the instrument must be built to run at the earlier one. Concretely: when a row records what a consumer SAW, anything that labels that row (which expert a slot holds, which owner a page belongs to, which generation a pointer came from) must be captured before or at the consumer, never at the synchronize point where the row is printed. The end-of-step state is a valid reading of its own -- but it is a different question, and it must not be used to label a within-step one, because the specific hypothesis such an instrument exists to test is usually 'did the thing that should have been in place before the read actually arrive before the read'.", "because": "The pool-row digest (a POOL row records the id a MoE consumer used and a digest of the bytes that id selected) was extended with the slot->expert map so the comparator could separate 'same slot, different expert' from 'same slot, same expert, different bytes'. The obvious implementation -- look the map up in the dump path, where the ids have just been read -- is wrong here for a reason particular to the investigation: the dump runs at synchronize, i.e. after the whole pass, so it would have read the map as it stood AFTER any background fill that landed late. 'A fill landed after the consumer had already read' is exactly the hypothesis the row was being used to test, so the label would have been produced by the phenomenon under test. The map itself does not depend on the ids, so it can be snapshotted at encode time (immediately before the consuming kernel is submitted) and stored per capture; the ids are consulted later, when they finally exist.", "counterexample_observed": "Instrument written 2026-09-17 (§9.18.7), not yet run. The contrast is measurable in the design itself: the same run's engine-side counter reports `consumed_changed=192` over one pass -- i.e. the consumed slot->expert map DOES move within a run -- while a hardcoded dump-time lookup would have shown one final value for every row and no way to see the movement at all.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.h"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0043", "class": "gate-integrity", "rule": "When a comparison gains a column, every sentence the tool prints about a verdict must be re-derived from the new cause, not left reading the old proxy field -- and any 'these agree' verdict must be re-checked at the granularity of the NEW comparison, not only at the level the old guard was written for. Two concrete traps: (a) a summary line whose explanatory text keys off a field that the new column has just made non-diagnostic will contradict the tool's own headline, and a reader who skims the last line gets the opposite conclusion; (b) a guard against comparing sentinel-with-sentinel that is written per KEY ('every row was unread') does not cover a key where only SOME rows were unread, and those rows will still compare equal and be counted as agreement. Add the row-level verdict explicitly (as its own name, not folded into the existing one) so the total can never silently include an unmeasured row.", "because": "Adding the slot->expert column to the pool-row comparator changed what 'ids differ' licenses: it no longer implies 'the mapping is at fault' (it can be a benign relayout of the same expert) and 'ids same' no longer implies 'the bytes are at fault' (the slot can hold different experts). The pre-existing closing sentence chose between 'mapping, not bytes' and 'residency delivered different content' purely on whether the ids matched, so on a row that was a genuine content difference with different ids the tool printed a CONTENT headline and then, three lines later, the opposite conclusion. Separately, the pre-existing all-sentinel guard was written at key level because that is where the original incident was; a fixture with one unread row out of two still reported the key as SAME, because an unread row's group tuple is (sentinel, sentinel) on both sides and compares equal.", "counterexample_observed": "Both found by fixtures, not by reasoning, in the same edit (2026-09-17): the 'routing' fixture with ids differing and owners differing printed `*** CONTENT` followed by `FIRST DIFF ... ids differ. The index differs: mapping, not bytes.`; and the 'unread' fixture printed `verdict rows: SAME=1` while its own cause tally said `SAME=1 UNREAD=1`. After the fix the latter is `PART-READ=1` and withholds the verdict.", "applies_to": ["Backup/compare_pool_row.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0046", "class": "measurement-hygiene", "rule": "When an instrument snapshots a shared structure once per capture, store one copy per capture -- do not share one copy between captures that are being compared. A shared buffer makes the LAST capture's state the label for every earlier one, which silently deletes exactly the drift the captures were collected to expose, and it does so without any error: every row still prints a plausible value. The cost of per-capture storage is bounded and stateable (here: slot count x captures, arena grown on demand and capped with a printed warning when exhausted); the cost of a shared buffer is an unsupported conclusion. Corollary: if the snapshot cannot be taken (channel absent, arena full, key unparsable), write a distinct 'not annotated' token and make the reader treat it as absence of evidence -- never as an empty snapshot.", "because": "The per-capture design was chosen over the cheaper one-array-per-layer design while writing §9.18.7. The three MoE nodes of one layer (gate/up/down) are captured within microseconds of each other and would all have read the same shared array, so a fill landing between two of them -- drift inside a single layer inside a single pass -- would have printed identically on all three rows. The whole point of capturing at encode time rather than at dump time was to be able to see movement within the step; a shared array would have undone that while looking correct.", "counterexample_observed": "Design-level, verified only by the fixtures of the time (2026-09-17): the comparator distinguishes `own=none` (not annotated) from `own=[]` (annotated and empty), and the three-state token is what lets a run printed before the column existed ('OWNER CHANNEL ABSENT', every row UNCLASSIFIED) still produce the identical legacy verdict (`SAME=90 DIFF=15 NOT-READ=0`, `FIRST DIFF = graph 31 ffn_moe_down-1.pool`) as the historical run it is compared against.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "Backup/compare_pool_row.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0044", "class": "gate-integrity", "rule": "A printed field's delimiter is part of its contract, not cosmetic: if a reader is documented to recognise `key=[...]`, the writer must print the brackets, and a liberal reader that accepts the bare form as well is a compatibility convenience rather than a licence to print either. The failure mode is specific and nasty -- the reader reports 'field absent', which is indistinguishable from 'the producer was never enabled', while the producer's own banner a few lines earlier says it is ACTIVE. Whenever a tool can report 'the instrument is missing', that report must be distinguishable from 'the instrument ran and I could not read it', or every absence verdict becomes unfalsifiable.", "because": "The slot->expert annotation was added to the pool-row capture and documented as `own=[...]`, with `own=none` reserved for 'not annotated'. The dumper printed a bare comma list. The comparator, written against the documentation, therefore found no owner on any row and printed its loud 'OWNER CHANNEL ABSENT -- no CONTENT rows must not be read as the content axis was cleared' warning, on two runs whose ggml-metal banner said `owner channel ACTIVE` in the same log. The first reading of a brand-new instrument was a false alarm about the instrument itself, and the run had to be repeated after a rebuild.", "counterexample_observed": "Run 2026-09-17 11:0x: the comparator printed OWNER CHANNEL ABSENT for /tmp/r13_own1.log, whose line 38 reads `CGC-POOL-CAP: owner channel ACTIVE (CGC-IDS-CAP rows will carry own=[...])`. After printing brackets, the same logs give `owner column: 210 of 210 POOL rows carry own=`. The comparator now also accepts the bare form, and a truncated `own=[5,9` (no closing bracket) is deliberately still treated as NOT annotated rather than as a short list.", "applies_to": ["src/llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp", "Backup/compare_pool_row.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0047", "class": "measurement-hygiene", "rule": "The number of rows a comparator may compare is bounded by the OPERAND's size on both sides, not by the depth the capture was ASKED for. A capture that digests 'the first N ids the consumer used' writes fewer rows than N whenever the operand holds fewer ids, and the unwritten rows are pure sentinel -- so a comparison that iterates to N manufactures a 'measurement gap' verdict for a region that never existed. Distinguish three things that all look like a short reading: rows BEYOND the operand (exclude; not part of the question), rows the operand has but the reader did not cover (report as a gap, with the covered prefix named), and rows covered but never written (a genuine instrumentation failure). Record the operand's size in the row so the reader can tell them apart -- a capture depth that is a knob cannot double as the operand's size.", "because": "Running the pool-row capture at depth 12 (needed because a 2-token chunk has 16 ids and the anomaly sits at the second token) produced 4 rows of sentinel on every single-token graph, because a decode step's operand holds only 8 ids. The comparator flagged 15 of 105 keys as PART-READ -- three of the three node names on each of the five decode graphs -- and a PART-READ key has no verdict, so the first A/B of the new instrument appeared to be missing a fifth of its evidence. The capture already printed the operand size (`nsel`), which is what makes min(depth, nsel) the correct row count rather than a guess.", "counterexample_observed": "Run 2026-09-17 11:1x: `verdict rows: SAME=90 DIFF=0 PART-READ=15 NOT-READ=0` before the fix, with `causes: UNCLASSIFIED-SAME-ID=1200 UNREAD=60`; after bounding the row count by `nsel` the same pair of logs gives `SAME=105 DIFF=0 PART-READ=0 NOT-READ=0` and `causes: SAME=1200`. The per-graph decomposition then shows the decode graphs with exactly 8 compared rows and the prompt chunks with 12, which is what the operands actually hold.", "applies_to": ["Backup/compare_pool_row.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0048", "class": "measurement-hygiene", "rule": "A tracked build artifact is a SHARED resource: rebuilding writes to every experiment currently running on that machine, not just to your own tree. A pre-flight check that merely PRINTS the conflict is not a gate -- the check and the act must be in one branch, so that a non-empty result aborts instead of being observed and then ignored. Before any build, gate on BOTH (a) a listener on the harness port and (b) the measurement processes of every other line (the runner script and the sweep), and abort if either is present; and treat 'the binaries are tracked' as the reason this matters -- a build is a commit-shaped act on the machine's shared state even when nothing is written to git.", "because": "Both the llama-server binaries and the library dylibs live under the tracked build tree, and two sessions were driving the same GPU in the same half hour. The pre-flight `lsof -iTCP:8080` was written into the same shell line as the `cmake --build`, printed the other session's listening server, and the build ran anyway -- replacing libggml-metal and libllama underneath a run that had started 54 seconds earlier. The visible consequence is that the other line's A/B ended up spanning two builds, which silently breaks the single-build premise every comparison in this project relies on; the invisible consequence is that the wrong conclusion is indistinguishable from the right one.", "counterexample_observed": "2026-09-17 11:29:13 (this machine): `lsof -nP -iTCP:8080 -sTCP:LISTEN` printed `llama-ser 25649 ... TCP *:8080 (LISTEN)` and `pgrep -fl llama-server` printed the same process, in the same command whose next statement relinked `libggml-metal.0.19.0.dylib` and `libllama.0.0.279.dylib`. The other line's runner had started at 11:28:19 and its next arm began at 11:29:22 on the new library. Its two earlier attempts had already died with `Insufficient Memory (kIOGPUCommandBufferCallbackErrorOutOfMemory)` at load_model, i.e. under GPU memory pressure from concurrent work rather than from any assertion.", "applies_to": ["src/llama.cpp/build/bin/libggml-metal.0.19.0.dylib", "src/llama.cpp/build/bin/libllama.0.0.279.dylib", "scripts/check/decode_sweep.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0049", "class": "measurement-hygiene", "rule": "A build fingerprint assembled by reading several files one at a time is not atomic: if a relink is in progress, it can stamp a combination of versions that never ran together, and it does so silently -- the dict looks complete and every key is a real hash. Take the fingerprint TWICE (at launch and at teardown) and mark the row not-comparable when they differ, or else detect that any of the files changed while the fingerprint was being assembled and refuse it. The reason this matters more than a normal race: the fingerprint is the thing that decides which rows may be compared, so a torn one does not produce a wrong number, it produces a wrong permission -- it licenses a comparison that must not happen.", "because": "The pool-row A/B run at 11:28 on this machine had its binaries replaced underneath it: two libraries were relinked 3 seconds apart (libggml-metal at 11:29:13, libllama at 11:29:16), and the first arm's fingerprint was computed inside that 3-second window. It therefore recorded the NEW libggml-metal hash together with the OLD libllama hash -- a build that never existed as a running configuration -- while the second arm recorded the new pair. Read naively, the two arms 'share' the metal key and differ on llama, which invites the conclusion that only one library changed; in fact the whole run straddled two builds and neither arm is quotable.", "counterexample_observed": "Run 2026-09-17 11:28 (Backup/phase_decomp/ids_dst_capture_20260917_112819.json): arm p25-gputime-churn -> {libggml-metal: 7ac0ac2cdedd, libllama: 1010579c5b33}; arm p25-slotgpu-churn -> {libggml-metal: 7ac0ac2cdedd, libllama: 24ca3917d5cf}. The on-disk pair at the time of reading is {7ac0ac2cdedd, 24ca3917d5cf} and 1010579c5b33 exists nowhere on disk, so the first arm's dict is a mixture. The rerun at 11:31 gives {7ac0ac2cdedd, 24ca3917d5cf} for both arms, matching the disk.", "applies_to": ["scripts/check/decode_sweep.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0045", "class": "gate-integrity", "rule": "一個 check 的綠燈只證明「它管得到的那些條目」彼此一致，不證明「你要的東西在它的管轄範圍內」。所以新增任何衍生物時，同一個 commit 要做兩件事：讓它進入某個 check 的管轄，並讓「這份產物由誰檢查」可以被回答。特別地：把產物登記進一個以「自己的 bytes/mtime」為準的索引**不算**被檢查——那只證明它沒被手改，不證明它跟得上它的輸入；後者只能重生一次再逐位元組比。", "because": "2026-09-17，agent_harness engine loop。`sft_prime` 落後 47,478 B（輸入 lessons 112→132），而當時**每一條閘門都是綠的**：`index_assets.py --check` 報 `80 assets, existence + bytes + mtime all agree`、`build_memory_index.py --check` 報 no drift、`traces/validate.py` 122/38/112 全 OK。原因是 MANIFEST 的 80 筆全部是 `scripts/check/*` 與兩個索引檔，而 `harness_engine/`、`sft_pi/`、`sft_prime/`、`distill/` 四個目錄**一筆都不在裡面**——那份綠燈從來沒有對它們做出任何主張。同一個輸入源（lessons.jsonl）的另一份投影 `harness_engine/memories/` **有** `--check`，而且它當時**正確地紅著**（缺 19 檔）：訊號一直在，只是只有一半的衍生物接上了它。", "counterexample_observed": "判別性測試：把兩個 builder 重生到 /tmp 再逐檔 sha256 比對——`sft_pi/{train,valid}.jsonl` 逐位元組相同（它的輸入 episodes 沒動），`sft_prime/train.jsonl` 296,108 → 343,586 B ⇒ **兩個投影，一個滯後一個沒有，而 repo 沒有辦法分辨它們**。第二個反例是 bare run：兩個 builder 的預設都是 `--system full`，而版控裡的產物分別是 `head:8000` 與 `none` ⇒ 不加參數重跑會產生一份**語意不同**的資料集（sft_prime 從 343 KB 變成 ~15 MB），而輸出看起來完全正常；修法是記下 invocation 並讓裸跑重現它。", "applies_to": ["agent_harness/engine_loop/sft_prime/build_sft_prime.py", "agent_harness/engine_loop/sft_pi/build_sft_pi.py", "agent_harness/engine_loop/sft_common.py", "agent_harness/engine_loop/index_assets.py", "agent_harness/engine_loop/README.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0046", "class": "gate-integrity", "rule": "把一條早就寫在文件裡、卻從未被執行的驗收變成檢查時，成本**通常在量具不在規則**：一個「每一條都要有 X」的掃描器，它的**假陽性率**才是設計內容。所以這種檢查的第一個交付物不是它抓到什麼，而是**它自己的假陽性清單**；第二個是**陽性對照**（注入一個該抓的，證明它會抓）。反過來：一個在乾淨輸入上也報一堆問題的掃描器，它產生的不是待辦清單而是噪音——而噪音會讓人去『修』沒壞的東西。", "because": "2026-09-17，`CONVENTIONS.md` 第 7 行早就寫著「每一條都必須能指到今天的具體證據（檔名／log 行／欄位值）。指不到的條文不是憲章，是感想」，而 E1 的驗收欄引用了它，卻**從來沒有被執行過**。寫 `agent_harness/shared/check_citations.py` 時，第一版在**乾淨的**憲章上報 **84 個問題，全部是假的**，分六類：①只在 repo 根解析（80 個假懸空；`validate.py` 其實在 `engine_loop/traces/`）②行文斜線被當路徑（`gpu_union/wait`、`peak/mean`、`IQR/中位數`）③decision id 用精確比對（id 是 `dec-YYYYMMDD-HHMM-<slug>`，憲章只引到 `dec-…-HHMM`，要**前綴**比對）④副檔名本身（`.m`／`.sh`——行文在講「獨漏 .m」）⑤承前省略（`..._202725.log`）⑥**同一格程式碼裡路徑後面跟著參數**（`prefill_gputime_report.py --decprof-pair …`）。第 ⑥ 類最貴：它讓 A18 被報成「完全沒有指針」，於是人會去補一條本來就合格的條文。修完六類後：懸空 0，61/61 條都有交代（59 可解析指針 ＋ 2 顯式標記）。", "counterexample_observed": "陽性對照：把 `agent_harness/fake/nope.jsonl` 注入憲章副本的 A1 → 懸空必須由 0 變 1（實測 0→1）；沒有這一格，「0 懸空」與「檢查器根本沒在跑」是同一個輸出。第二個反例是**判準的位移**：`- 證據形態：…` 這個標記之所以必要，是因為憲章自己的括號裡就寫了「欄位值」——B25 的證據是一次 `grep` 的輸出，本來就沒有檔案可指；把它算成不合格，就是把驗收條件從「有交代」偷偷換成「有檔案」。", "applies_to": ["agent_harness/shared/check_citations.py", "agent_harness/shared/README.md", "agent_harness/CONVENTIONS.md", "agent_harness/engine_loop/traces/validate.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-diag-0032", "class": "diagnosis", "rule": "When a tool reimplements an engine's formula, validate it on at least two artifacts that differ in exactly the dimension the formula's conditionals touch -- and make the tool print which branch it took. A formula that is only ever checked against one artifact encodes that artifact's accident as if it were the rule. Concretely: any reimplementation must read the SAME discriminators the engine reads (here, the GGUF's own nextn count), not infer them from the artifact in hand, because the artifact in hand may happen to make the branch irrelevant.", "because": "A GGUF header parser reproduced the loader's pool-capacity formula as `per_slot = MAX over all layers`. The loader computes that MAX over TRUNK layers only (it skips the NextN/MTP layer explicitly, so the trunk's pool cannot depend on how the head happens to be stored) while still counting every layer in the denominator. On the shipped model the two agree, because its MTP layer is THINNER than the trunk's binding layer -- so the tool matched the engine on every check that had been run (107/143/179 verified against two engine runs). The moment a second model was tried whose MTP layer is q8_0 and thus thicker, the tool reported 15 slots where the engine computes 148. Nothing about the first model's numbers looked wrong, and the tool had already been quoted in a plan document.", "counterexample_observed": "2026-09-17: `scripts/check/gguf_pool_geometry.py` vs the engine on `Ornith-1.5-35B-...gguf`. Uncorrected formula: per_slot = blk.40 (q8_0) = 3,342,336 B -> clamp(2 GiB/(41*3,342,336),8,256) = 15. Engine, same 2 GiB budget: `n_slots=29` with `LAYER_CAPS per-layer caps: total 1416 slots (avg 34.5/layer, min 29/layer)`. Corrected (MAX over trunk only): per_slot = blk.0 = 1,769,472 B -> 29, and the census prediction matches the engine's line exactly at both 2 GiB (1416) and 4 GiB (2616, engine `n_slots=59`).", "applies_to": ["scripts/check/gguf_pool_geometry.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0050", "class": "measurement-hygiene", "rule": "Before designing a change to the engine, grep for the seam that already exists: if a concept already appears per-layer in the engine's own diagnostic output, the plumbing for it is already there and the change may be configuration rather than code. Ask what the engine ALREADY prints about the quantity, and treat a per-item census line as proof that per-item values are supported end to end. The payoff is not just less work: a config-only change needs no rebuild, which in this repo means it cannot clobber another line's mapped dylibs, and it needs no numerical gate because no source changed.", "because": "M6 had been scoped as an engine change ('allow per-layer quotas instead of one MAX for the whole class'). Reading the loader found that per-layer slot counts already existed behind `LLAMA_EXPERT_CACHE_LAYER_CAPS`, read by BOTH sides (the loader's ne[2] shrink and the cache's per-layer slot vectors), and -- decisively -- that `run_server.sh` already sets it on every launch with a default of \"40-40:256\". The engine even prints a census of it: `LAYER_CAPS per-layer caps: total 5976 slots (avg 145.8/layer, min 143/layer)`. A census line whose total is exactly 40*143+256 is hard evidence that per-layer values flow through the whole path. The intended change therefore reduced to one environment string, with the same budget giving the 37 typical layers 179 -> 232 slots.", "counterexample_observed": "2026-09-17: the plan document had proposed a source change plus a rebuild plus a domain-specific gate run. The config route instead needs no rebuild (so it cannot interfere with the concurrently-running measurement line, the failure that lesson eng-mh-0048 records) and no D5, because no file under src/ changes. Verified read-only: 41-layer census totals match the formula to the slot at 143/179 (Nail) and 29/59 (Ornith).", "applies_to": ["src/llama.cpp/src/llama-model-loader.cpp", "src/llama.cpp/src/llama-expert-cache.h", "scripts/run_server.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-bound-0007", "class": "honest-bounds", "rule": "「這支腳本自己算錨點」**不是**它不會因為搬遷而壞的證據；證據是「它算出來的路徑還存在」。自帶錨點只保證相對於自己，不保證相對於世界：腳本往下搬一層，`$(dirname \"$SCRIPT_DIR\")` 的語意就跟著改一層，只有當那個目標**也**跟著搬時兩層誤差才互相抵消。所以搬動任何一支腳本之後，要驗的是**每個派生路徑的存在性**，不是「它有沒有一行寫死的路徑」。最惡的一種是**一半僥倖正確**：同一個變數底下有一些派生仍然解析得到（因為它們的目標也搬了），於是第一個錯不會以「錨點錯了」的形式出現，而以「這個檔案有問題」的形式出現。", "because": "2026-09-16 E1 把 642 個檔搬進 agent_harness/tb_loop/。我在白皮書裡下了推論「自帶錨點的腳本一行都不用改；共享變數的全部要改，0 比 9」——**那個推論選錯了判準**：檢查的是「有沒有自己算錨點」，而那是無關的性質。一天後掃描發現兩支腳本壞掉：① `tb_loop/finetune/finetune_loopmoe.sh` 的 `AGENT_HARNESS_DIR=\"$(dirname \"$SCRIPT_DIR\")\"` 從 `agent_harness/` 變成 `tb_loop/`，於是它需要的一半東西（`config.env`、`sft_data_merged/`）仍然找得到、另一半（`loopmoe/`）找不到 —— **失敗之所以靜默，是因為其中一半僥倖正確**（同一支腳本裡的 `loopmoe/` 與 `sft_data_merged/` 在搬遷後分屬兩層，原本恰好同一層）。順帶發現同一支腳本裡 `LOOPMOE_CONFIG` 讀的是 `config.env` **沒有定義**的名字（那裡叫 `LOOPMOE_CONFIG_PATH`）⇒ 它的 `${…:-fallback}` 一定走 fallback，於是 fallback 一壞就整個壞。② `tb_loop/scripts/start_local_model.sh` 的 `$ROOT/../models/gguf/…` 從 repo 根變成 `agent_harness/models/gguf/`（不存在）。修法：`finetune_loopmoe.sh` 把錨點**拆成兩個名字**（`TB_LOOP_DIR_SELF` 對 config.env、`TB_HARNESS_ROOT` 對 loopmoe/）並在兩者都算得出來時互相對照，最後斷言 `LOOPMOE_CONFIG` 存在（那一行就是本來該有而沒有的斷言）；`start_local_model.sh` 補一個 `REPO_ROOT`。驗證方式是**用腳本自己的定義實際求值**再檢查每個目標存在，不是重算推論。", "counterexample_observed": "同一批腳本裡的對照組：`tb_loop/finetune/eval_round.sh`、`finetune_gemma4.sh`、`compare_finetune.sh`、`learning/refine_harness.sh`、`scripts/batch_run.sh`、`scripts/setup_env.sh` 用的也是 `$(dirname \"${BASH_SOURCE[0]}\")/..`，而它們**是對的** —— 因為那些錨點的目標（`TB_LOOP_DIR` 底下的東西）跟腳本一起搬了。差別不在寫法，在**目標有沒有跟著搬**。第二個對照是同一天的另一類：`scripts/check/*` 的 562 條引用裡 134 條來自已定稿白皮書 ⇒ 那裡的 `git mv` 會讓引用懸空；兩件事的判準是同一句話（目標還在不在），只是量測方式不同（一個用路徑求值，一個用引用計數）。", "applies_to": ["agent_harness/tb_loop/finetune/finetune_loopmoe.sh", "agent_harness/tb_loop/scripts/start_local_model.sh", "agent_harness/tb_loop/config.env", "agent_harness/PLAN_ENGINE_LOOP_2026-09-15.md", "docs/AGENT_HARNESS_E1_RESTRUCTURE_20260916.html"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0017", "class": "source-reading", "rule": "**把單體拆成套件會讓「外部以屬性替換注入替身」靜默失效**，而 AST 等價、runtime 指紋、CLI 面**三者都比對不到它**（三者都不呼叫函式）。單體時代「只有一個命名空間」讓 `km.X = 替身` 必然生效 —— 那是一個 trivially true、所以從來沒有被寫下來過的契約。拆檔之後 entry 的 `X` 只是 re-export 產生的一個獨立綁定，而**讀者讀的是子模組的全域**。偵測它的唯一方法是把契約寫成一條檢查：對每個住在子模組的名字，`entry.X = sentinel`（或 `entry.<module>.X = sentinel`）之後，`F.__globals__[X]` 必須就是那個 sentinel。★ 還有一半沒有解：用 `spec_from_file_location` 載入的模組**不在 `sys.modules` 裡**，Python 沒有給任何物件回指的辦法，所以攔不到 `__setattr__` ⇒ 那個情境的唯一可行入口是把子模組綁在 entry 上、改打 `entry.<module>.X`。**不要把「正常 import 時可以轉發」講成「契約保住了」。**", "because": "2026-09-17 E4 item 2 把 3310 行的 `scripts/check/knifeedge_matrix.py` 拆成 `scripts/check/knifeedge/`。`feasibility_gate_selftest.py` 用 `km.pool_geometry = lambda kind: dict(FAKE_GEOM)` 注入合成幾何（三處以上），而拆檔後 `feasibility_cell` 讀的是 `knifeedge/feasibility.py` 的全域 ⇒ 注入無聲無息地沒生效。處置分兩層：正常 import 的 entry 由 shim 換掉自己的類別攔 `__setattr__` 並轉發到定義它的模組；路徑載入的情境改用 `km.feasibility.pool_geometry`（呼叫端 5 處 ＋ `oracle_truth_gate_selftest.py` 1 處），並要求改前後**測試輸出逐字元相同**。", "counterexample_observed": "同一支測試改之前與改之後：搬遷前在等價環境（`git archive HEAD scripts/check` ＋ `models`／`src` 符號連結）跑 → `ALL PASS`；搬遷後未修 → `ModuleNotFoundError: No module named 'numpy'`（注入失效使它掉到真的 GGUF 路徑）；修好注入點後 → `ALL PASS`，且與搬遷前的輸出逐行相同。第二個對照更貴：`check_module_split.py contracts` 的陰性對照（把 shim 的轉發層那兩行拿掉）→ 4 項裡 3 項變紅並列出 **101 個具體失敗**（`BINARY_DIR -> identity`、`CAP_INVARIANCE_DEFAULT_CAPS -> constants`…）—— 這證明那個檢查真的在測東西，而不是在印一行字。", "applies_to": ["scripts/check/knifeedge_matrix.py", "scripts/check/knifeedge/feasibility.py", "scripts/check/feasibility_gate_selftest.py", "scripts/check/oracle_truth_gate_selftest.py", "agent_harness/shared/check_module_split.py", "agent_harness/shared/split_module.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0018", "class": "source-reading", "rule": "**`global` 宣告出來的名字，必須與它的讀者住在同一個模組。** 這種名字在「誰定義它」這個靜態問題下**不存在**（它沒有 module-level 的賦值敘述，只有某個函式裡的 `global X` 與 `X = …`），所以任何以「頂層節點 → 名字」為基礎的搬家工具都會**看不到它**，於是可以把讀者搬到別的模組而讓它讀到一個不存在的名字 —— 語法檢查不會反應，import 也不會失敗，只有在執行到那一行時才 `NameError`。寫搬家工具時要把這條做成**硬性拒絕**（我的第一版沒有，是在乾跑時發現的）：算出每個 `global` 名字的寫者與讀者，要求它們被分到同一模組。", "because": "`scripts/check/knifeedge_matrix.py` 有兩個 `global` 宣告。其中 `args_greedy_global` 只在 `main()`（L2998 `global` ＋ L3169 賦值）建立、只在 `print_matrix()`（L2807）被讀。按「能力」分模組時 `main` 屬於 `cli`、`print_matrix` 屬於 `matrix` —— 完全自然的一刀，卻會讓 `print_matrix` 在自己的模組裡找一個從來沒被建立的名字。", "counterexample_observed": "AST／依賴分析的第一版**沒有**報出這一條：因為 `args_greedy_global` 沒有任何 module-level 賦值，它不在「頂層名字」集合裡，於是 `print_matrix` 對它的引用被歸類成「不是模組級引用」而消失。對照組是另一個 `global`：`_feasibility()` 的 `_FEAS_MOD`（L2170 `_FEAS_MOD = None`）**有** module-level 賦值，所以它以普通全域的身分被看見、被歸到 `feasibility`；同一份程式裡兩個 `global` 只有一個看得見這件事，就是這條 lesson 的來源。", "applies_to": ["scripts/check/knifeedge_matrix.py", "scripts/check/knifeedge/cli.py", "agent_harness/shared/split_module.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-src-0019", "class": "source-reading", "rule": "**以「綁定名」為鍵建立 import 表，會讓綁定同名的那第二個 import 靜默覆蓋第一個。** `import urllib.error` 與 `import urllib.request` 綁定的是**同一個**名字 `urllib`，所以 `{綁定名: import 敘述}` 形式的字典只留得下最後一個；之後任何「這個模組用到 urllib ⇒ 就 import 那一條」的重建都會漏掉另一條。危險的地方是它**仍然會跑** —— 因為 `urllib.request` 自己會 `import urllib.error`，`urllib.error.HTTPError` 照樣解析得到。**僥倖不是保證。** 正確的做法是讓值成為**列表**（綁定名 → 所有相關 import 敘述），並讓「重建出來的樹是否含有原檔每一個 import」變成檢查項。", "because": "E4 item 2 寫 `split_module.py` 的 import 重建時，第一版就是 `table[(a.asname or a.name).split('.')[0]] = text`。AST 等價檢查（`check_module_split.py ast`）第一次跑就報 `MISSING Import  import urllib.error` —— 那是這個工具唯一一次在**沒有先看過症狀**的情況下抓到一條真缺陷。", "counterexample_observed": "同一份原檔裡還有 `import hashlib`／`import json`／`import os` 等 7 條單綁定 import，它們不會被覆蓋 —— 所以「用字典」這件事在 7／9 的情況下是對的，錯誤只在 2 條上出現。這個 7 比 2 正是它容易過關的原因。修好之後（值改列表）AST 檢查的 `MISSING` 由 2 項降到 0。", "applies_to": ["agent_harness/shared/split_module.py", "scripts/check/knifeedge/oracle.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0047", "class": "gate-integrity", "rule": "**「沒有改行為」是一個要分層主張的 claim，而每一層都要有自己的陰性對照。** 層與層的可見範圍不同：AST 只看結構（看不到 `__file__` 的指涉、看不到 `global` 的歸屬）、runtime 指紋只看「不呼叫函式就量得到的性質」（`dir()`／簽名／docstring／常數值／錨點路徑）、CLI 只看參數面與 `--help` 的文字。**三者都看不到「外部注入的替身失效」**（那要真的把值塞進去再看誰讀到）。所以「多一層檢查」不是為了更嚴格，是為了**換一個可見範圍**。而每一層的陰性對照要**故意弄壞**：改一個常數、拿掉一段轉發 —— 並要求檢查器變紅。**沒有陰性對照的檢查，它的綠燈與「它根本沒在跑」是同一個輸出。**", "because": "拆分一個 3310 行的檔案之後，我需要一個判準來回答「有沒有改行為」。一層不夠：AST 通過了、指紋通過了、`--help` 逐字元相同 —— 而 `feasibility_gate_selftest` 已經因為替身失效而崩潰。四層各自抓到不同的東西，其中第四層（替換契約）是唯一抓到那個真缺陷的。", "counterexample_observed": "① 把 `knifeedge/caps.py` 裡的一個 `= 6` 改成 `= 7` → AST 檢查 rc=2（紅）並印出 `MISSING FunctionDef …`；② 把 anchor 手寫模組裡的 `= 6` 改成 `= 7` → 同樣紅；③ 把 shim 的轉發層拿掉 → `contracts` 由 4/4 變成 1/4（101 個具體失敗）；④ 未改的複本 → 全部通過（rc=0／0 個 MISSING）。四個方向都量過，所以「通過」不是因為檢查器沉默。", "applies_to": ["agent_harness/shared/check_module_split.py", "agent_harness/shared/knifeedge_split_map.json", "scripts/check/knifeedge_matrix.py", "agent_harness/shared/split_module.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0048", "class": "gate-integrity", "rule": "**一條只寫在散文與 `.gitignore` 裡的政策沒有執行者。** `.gitignore` 只影響**未追蹤**的檔案 —— `git add -f` 直接繞過它，而散文不會對任何事做出反應。把它變成斷言的第一件事，是**列出既有的違反**（通常有一批，而且通常不是你的）；而正解既不是「移出」（那可能等於刪掉別人的證據，而且他們的白皮書可能正引用著它）也不是「沉默放過」，而是**登記成看得見的例外**：逐筆印出、附 owner／大小／已查過的欄位，並且**多一筆就紅**。`grandfathered` 不是豁免權，它的功能是把「沉默的違規」換成「看得見的例外」；配套的一條是**每一筆例外都必須仍在版控裡**，否則過期的例外會開始掩蓋未來的新違反。", "because": "PLAN §8 的 log 政策（「`Backup/cgc_logs` 原文不進 repo」）寫在三個地方：PLAN 的段落、`.gitignore:419-425` 的 dated 註解、以及幾支腳本的行為。它從未被機檢過。2026-09-17 把政策抽成 `agent_harness/shared/log_policy.json` 並寫了 `check_log_policy.py`，**第一次執行就抓到** `Backup/cgc_logs` 底下有 12 個 `.log` 原文在版控裡。", "counterexample_observed": "那 12 筆共 5,831,279 bytes（10 個 ~670 KB 的 llama-server 原文 ＋ 2 個小檔），而 `git grep -l <檔名> -- docs/ '*.md'` 的引用數是 **0** —— 也就是說它們既違反政策、也沒有被任何文件引用；處置仍是「登記而不移出」，因為它們是另一條線的量測證據。同一輪的反向對照：`Backup/cgc_logs` 底下另外 22 個檔案（118 KB 的 `arms.json`／`*.tsv`／`req2retest*.txt`）是**衍生摘要不是原文**，政策不禁它們 —— 若檢查器只會「檔數變多就紅」，它會把這 22 筆報成違反，而那時「12 筆真違反」就被 22 筆假陽性稀釋掉了。", "applies_to": ["agent_harness/shared/log_policy.json", "agent_harness/shared/check_log_policy.py", ".gitignore", "agent_harness/PLAN_ENGINE_LOOP_2026-09-15.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0051", "class": "measurement-hygiene", "rule": "**編譯期產生的診斷只出現一次** —— `SyntaxWarning` 是在**編譯**原始碼時發出的，載入既有 `.pyc` 時不會重現。所以同一支程式兩次執行的 stderr 不同，可能只是快取狀態不同，而不是行為不同。要比較兩支程式的 stderr（或任何以「有沒有那幾行」為判準的東西），**先把兩邊的 `__pycache__` 都清掉**，否則你比到的是快取。**推論：一個以「新舊輸出逐字元相同」為證據的檢查，必須控制快取。**", "because": "E4 item 2 的 CLI 層檢查比對拆分前後的 `--help`：第一次跑出 183 bytes 的差異，形狀是舊 entry（在 `/tmp`，沒有快取）噴了一行 `SyntaxWarning: invalid escape sequence '\\S'` 而新 entry 沒噴。看起來像「CLI 面變了」，實際上新的那棵樹只是命中了我幾分鐘前 import 時留下的 `.pyc`。", "counterexample_observed": "清掉 `scripts/check/knifeedge/__pycache__` 之後，新 entry 一樣噴同一個警告（行號由 `knifeedge_matrix.py:2134` 變成 `knifeedge/host.py:223`，訊息與引文一字不差）⇒ 兩邊的 stdout 逐字元相同（11168 bytes）、stderr 只差在警告裡的檔案路徑。這也讓那條檢查的判準被改成「stdout 逐字元比對 ＋ stderr 比對前先把 warning 區塊換成佔位符」——因為警告內容天生包含路徑與行號，而搬動程式碼必然改變它。", "applies_to": ["agent_harness/shared/check_module_split.py", "scripts/check/knifeedge/host.py"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0049", "class": "gate-integrity", "rule": "**一個檢查的預設參數，可能在它被提交的那一刻失效。** 拆分的等價檢查要讀「原檔」，而在拆分還沒提交時 `--rev HEAD` 指的就是原檔；一提交，同一個路徑就變成 shim ⇒ 檢查開始拿 shim 當原檔比。症狀極具誤導性：`declared_edits 指名了原檔裡沒有的名字`、`原樣的複本 -> fail` —— 讀起來像「拆分壞了」，其實是**量具指錯了指涉**。一般化：任何檢查只要用「當下的 HEAD／cwd／最新檔」當預設輸入，就要問「它在我提交之後還指著同一個東西嗎」；不變的輸入（釘住的修訂、寫進對照表的 rev）才是指涉。★ 而且它會**掩蓋陰性對照**：我的 self-test 兩項都回 rc=2，於是「未改的複本」失敗、「改一個字元的複本」也失敗 —— 陰性對照看起來是 ok 的，因為它與陽性對照同歸於盡。", "because": "2026-09-17 E4 item 2 拆分 `scripts/check/knifeedge_matrix.py`。`check_module_split.py` 的 `--rev` 預設 `HEAD`、`check_stale.py` 的反向檢查用 `git grep`。兩者在**提交之前**都是綠的，提交 `854e9a5fd` 之後雙雙變紅（跑閘門時才發現，不是在寫的時候）。", "counterexample_observed": "修法是把預設改成讀 `shared/knifeedge_split_map.json` 的 `source_rev`（＝拆分前那個修訂 e3e2f1576）。修完之後 `--self-test` 回復 2/2（未改的複本 rc=0、改一個字元 rc=2），而**不給 `--rev` 直接跑 ast 也正確**（113 個節點、111 逐 AST 相同）。對照組：`git grep -l '\"_stale\"'` 的版本在提交後把 `stale_registry.json`（它自己有 `banner_key: \"_stale\"`）與日誌快照都報成「未登記的標記」。", "applies_to": ["agent_harness/shared/check_module_split.py", "agent_harness/shared/check_stale.py", "agent_harness/shared/knifeedge_split_map.json"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0050", "class": "gate-integrity", "rule": "**反向檢查的判準要是「資料結構裡有那個鍵」，不是「文字裡提到那個字串」。** 「有沒有人偷偷貼了一個標記」這個問題，用 grep 字面字串去答時，會把**提到**那個字串的一切都算成**標記**：註冊表自己的欄位值、日誌裡的一句話、白皮書的一段說明。假陽性在這裡特別貴，因為反向檢查的目的正是「避免未登記的標記」，而它一旦開始誤報，真正的那一筆就淹沒在噪音裡 —— 與 `eng-gate-0048` 的「22 筆假陽性稀釋 12 筆真違反」同一個形狀。", "because": "`check_stale.py` 的反向檢查第一版是 `git grep -l '\"_stale\"'`。它在寫的時候（日誌還沒有提到 `_stale`、註冊表也還沒被納入視野）是綠的；提交之後 `stale_registry.json` 被自己的 `banner_key: \"_stale\"` 命中，於是 `--check` 報「stale_registry.json 帶著 _stale 記號但不在登記表裡」。", "counterexample_observed": "改成「解析版控中每一個 `*.json`，看它的**頂層 dict** 有沒有 `_stale` 鍵」之後：`--check` 恢復綠燈，而 `--self-test` 的第 6 項（把一個帶頂層 `_stale` 的 JSON `git add --intent-to-add` 之後必須被點名）**仍然會紅** ⇒ 修正沒有把檢查弄鈍。這一對（修假陽性 ＋ 證明真陽性還在）是這一類修正的必要條件。", "applies_to": ["agent_harness/shared/check_stale.py", "agent_harness/shared/stale_registry.json"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0052", "class": "measurement-hygiene", "rule": "**在共用機器上，`run_server.sh` 的 preflight 會把「別條線正在跑的 server」當殘留行程 SIGTERM 掉 —— 所以「起跑前檢查港埠」保護的是起跑那一瞬間，不是整個 run。** 一個 20–30 分鐘的協定（48 題品質、交錯 A/B）用單次 pre-launch 檢查是**不安全**的：它在第 5 分鐘被別人的 preflight 殺掉，而工具只會把那份結果標成 invalid，於是你拿到一個「看起來像分數、其實是未完成樣本」的東西。長跑要的是**互斥**（雙方都認的 lockfile、或請對方設 `CGC_PREFLIGHT_KILL=0`），或切成有界的片段（`--profiles`／`--max-per-profile`）。而且：**判死因要讀 server 自己的 log**，因為「被 SIGTERM」與「Metal OOM」在客戶端看起來一模一樣（連續空回應）。", "because": "2026-09-17 14:01，本線在 13:56:54 過了一次合法的閘門（8080 確實沒有 listener、沒有任何量測行程）並起了控制臂；14:01:53 那個 server 收到 `[CGC] Received SIGTERM`，而 3 秒後別條線的 `cap_r37_pool4.json` 出現 —— 是他們的 `run_server.sh:638-740`（`[防護 1] 清殘留`，目的正是防「行程疊加」造成的 GPU OOM ret=-3 與 kernel panic）把本線的 server 清掉了。控制臂因此死在 longform-zh #6（14/48），而 `bare_48.py` 只在事後把它標成 `invalid`。我先前的判讀是「控制臂活著、零 OOM ⇒ 指向 +238 MiB 的池」，那是**錯的**：控制臂不是自己死的，所以它對「池的記憶體代價」**不提供任何證據**，兩臂都沒跑完 ⇒ 閘門 2 既未通過也未否證。", "counterexample_observed": "控制臂的 log 結尾是 `Received SIGTERM — initiating graceful shutdown`，不是 `Insufficient Memory`；而兩臂的 `invalid_reason` 都是同一句模板（連續 5 題瞬間空回應），客戶端分不出內部崩潰與外部被殺。", "superseded_by": null, "applies_to": ["scripts/run_server.sh", "scripts/check/bare_48.py", "scripts/check/m123_oracle_gate.py", "scripts/check/decode_sweep.py"]}
{"type": "lesson", "lesson_id": "eng-gate-0051", "class": "gate-integrity", "rule": "**對比工具必須容忍「有一側早停」——否則它會偏偏在「該說這臂無效」的那一刻崩掉。** 一個會產生 `invalid` 結果的守護（死伺服器防護、timeout、中斷）之後，產物是**部分**的：profile 數少於完整跑。若對比函式逐「A 的 key 集合」去索引 B，第一個缺失的 key 就 `KeyError` ⇒ 使用者拿到 traceback 而不是「這份無效」。正確形狀：走**兩側的聯集**、缺的一側印 `n/a`、**把 `invalid` 在表頭大聲標出來並且不讓它的通過數被當成分數**、總計列在題數不同時**拒絕相減**。判準是：*任何會標記「此結果無效」的路徑，都必須有對應的「以無效身分被讀取」的路徑。*", "because": "2026-09-17，`bare_48.py --compare` 對兩份**都**被標成 `invalid` 的結果檔崩潰：`KeyError: 'longform-zh'`（A 有 {qa-zh, longform-zh}、B 只有 {qa-zh}）。崩潰點正好是它唯一能提供價值的地方 —— 它該說的是「這 14 題不是分數」，實際說的是 traceback。同一個形狀在本 repo 已經出現過一次（比對器在 `UNREAD` 守衛上只看 key 層、漏 row 層），所以這是複發而非偶發。", "counterexample_observed": "修好前：`m6-control`(14 題) vs `m6-slack256`(6 題) ⇒ `KeyError: 'longform-zh'` 並中止，`qa-zh 5/8 vs 0/8` 的差距印出來但沒有「這不是分數」的警告。修好後：兩臂的 `invalid` 先大聲印出，缺側 `n/a`，總計拒絕相減。", "superseded_by": null, "applies_to": ["scripts/check/bare_48.py"]}
{"type": "lesson", "lesson_id": "eng-src-0020", "class": "source-reading", "rule": "**搬遷一個 Python 套件時，「引用舊名字」有兩種形態，而它們需要兩個不同的掃描。** (a) **路徑派生**：`dirname(...)`／`$VAR/..` 算出來的東西指到別的地方 —— 要問「算出來的路徑還在不在」。(b) **import 字串**：`from <舊套件名>.<子模組> import X` —— 要問「那個名字在 `sys.path` 上解析得到嗎」。第二種只在**執行到檔頭時**才失敗，所以症狀是「連 `--help` 都跑不到」，而不是「某個功能壞了」。★ 最毒的一種是**半轉換**：同一個檔案裡的 `sys.path` 已經按新佈局設定好了、import 那行還是舊名字，而**它上方那句註解已經寫著新名字** —— 那句註解會讓 review 通過，因為它說的是對的。", "because": "2026-09-17（E4 收尾時被問「E1–E3 是不是只差量測」）對 `agent_harness/tb_loop/` 做了一次全掃：`scripts/local_rehearsal.py` 第 42 行的註解在 E1 搬遷時就改成了 `tb_loop.agents.codebuff_api_agent`，第 48 行的 import 還是 `from agent_harness.agents.codebuff_api_agent import CodebuffApiAgent`；而第 43-46 行塞進 `sys.path` 的是 `tb_loop/` 與 `agent_harness/` ⇒ `agent_harness` 本身不在路徑上（它是 namespace package，只有 repo 根在路徑上時才解析得到）。E1 當時的掃描只涵蓋了 (a)，所以兩天沒有人發現。", "counterexample_observed": "`agent_harness/tb_loop/.venv/bin/python scripts/local_rehearsal.py --help` → rc=1、`ModuleNotFoundError: No module named 'agent_harness'`（連 argparser 都沒到）。修成 `tb_loop.agents.…` 之後 rc=0。★ **同批 135 支 `.py` 裡只有 2 支殘留**（另一支是 `agents/loopmoe_agent_adapter.py` 的 docstring 用法範例，使用者照抄會踩同一個錯），其餘 9 處 `--agent-import-path` 早就都是 `tb_loop.agents.…` ⇒ **2/135 正是它容易過關的原因**：掃過一遍看起來是乾淨的。修完後：0 處殘留、135 支全部可編譯、有 `--help` 的 entry 全部 rc=0。", "applies_to": ["agent_harness/tb_loop/scripts/local_rehearsal.py", "agent_harness/tb_loop/agents/loopmoe_agent_adapter.py", "agent_harness/tb_loop/agents/prime_agent_adapter.py", "agent_harness/tb_loop/config.env"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0052", "class": "gate-integrity", "rule": "**驗收條件寫的是什麼，就要用什麼環境去量它。** 當一個檢查的**執行環境**（哪個直譯器、哪個 venv、哪個 PATH）與被驗對象的**部署環境**不同時，紅燈可能只反映量具的環境，而綠燈可能只反映量具剛好夠用。判準：對每一個長期紅著的驗收項，**先問「它用的是哪一個直譯器」**，再問「那個環境是不是它實際會被執行的環境」。這個 repo 有現成的權威：`agent_harness/tb_loop/config.env` 的 `TB_VENV_PY` 就是「這支工具該用哪個 python」。★ 反過來也成立：先前用系統 python3 跑三支離線自測得到 `ModuleNotFoundError: numpy`，那是**真的缺依賴**（不是量具取錯）；兩者的區別是「那個東西裝在別的環境裡」還是「根本沒裝」。", "because": "`PLAN` §9 與 `agent_harness/README.md` 從 2026-09-16 17:2x 起寫著 E1「驗收只結清一半」，理由是 `.venv` 不存在、`terminal-bench` 未安裝、`import` 止於 `No module named 'terminal_bench'`。2026-09-17 實測：`tb_loop/.venv` **存在**（建立於 9/16 19:32）且 `terminal-bench` 裝在裡面 ⇒ 用對直譯器之後第一條驗收是綠的，而那個錯誤結論掛了將近一天。", "counterexample_observed": "`PYTHONPATH=agent_harness agent_harness/tb_loop/.venv/bin/python -c 'import tb_loop.agents.prime_agent_adapter'` → OK（解析到 `agent_harness/tb_loop/agents/prime_agent_adapter.py`）；同一條命令用系統 python3 → `No module named 'terminal_bench'`。**同一份原始碼、兩個相反的結論，差別只在直譯器。** 而驗收句寫的是「`import tb_loop.agents.prime_agent_adapter` 成功」，沒有指定直譯器 —— 那個模糊處就是錯誤結論的入口。（同一次實測還發現真正該修的東西：`local_rehearsal.py` 的 import 回歸，見 `eng-src-0020`。）", "applies_to": ["agent_harness/PLAN_ENGINE_LOOP_2026-09-15.md", "agent_harness/README.md", "agent_harness/tb_loop/config.env", "agent_harness/tb_loop/run_round.sh"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0053", "class": "measurement-hygiene", "rule": "**「缺 X」是一個要分層的結論：把 X 補上之後，再問一次還缺什麼。** 一句「缺 Docker daemon」是用**一個**觀察（`docker info` 失敗）推出來的，而那個觀察只證明了最外層。判準：對每一個「缺 X」的結論，**先把它補上、再跑一次**；若第二次仍然失敗，那第一個結論就不是「不完整」而是**會誤導**（它讓人以為補上就沒事）。這一條與 `eng-gate-0052`（驗收要用對環境量）互補 —— 那一條說**量具**要對，這一條說**結論**要跑到它自己失效為止。★ 「補上再跑」之所以是唯一值得先做的診斷，是因為它通常很便宜：起 colima 13 秒、`brew install` 1 分鐘，而它換掉的是三層猜測。", "because": "PLAN §9 與 `agent_harness/README.md` 從 2026-09-16 起把 E1 第 3 條記成「缺的是 **Docker daemon**（colima 已停），不是模型端點」。2026-09-17 15:13 起了 colima（daemon 通、`docker ps` OK、`docker info` 報 server 27.4.0）之後，`tb run --n-tasks 1` **連續失敗兩次**。", "counterexample_observed": "失敗序列（每一次都以為「這次補上了」）：① exit 125 `docker: unknown command: docker compose`；裝好 compose 外掛之後 ② exit 1 `error listing credentials - err: exec: \"docker-credential-desktop\": executable file not found in $PATH`；把 `credsStore` 移除之後 ③ **Resolved 1 / Unresolved 0 / Accuracy 100.00%**（`raman-fitting.easy`）。三個不同的「缺」，而原本那句結論抓到**零個**。★ 反面同樣要記：第一層的觀察本身沒有錯（daemon 真的不在），錯的是**把它當成完整答案**並寫進計畫表 —— 一份寫著「只差 Docker」的狀態表會讓下一個人重複我這 15 分鐘。", "applies_to": ["agent_harness/PLAN_ENGINE_LOOP_2026-09-15.md", "agent_harness/README.md", "agent_harness/tb_loop/README.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-gate-0053", "class": "gate-integrity", "rule": "**一個被移除的應用程式會留下兩類殘骸，而它們的失敗訊息都不含「沒有安裝」。** ① **斷掉的符號連結**：工具會**靜默忽略**它們（docker 對 `~/.docker/cli-plugins/` 底下指向不存在目標的外掛不報任何錯），於是子命令直接消失、`docker compose` 變成 `unknown command: docker compose` —— 讀起來像「這個 docker 版本沒有 compose 子命令」，而真正的原因是一個**已不存在的 app**。② **指向它的設定值**：`~/.docker/config.json` 的 `credsStore: \"desktop\"` 沒有連結可斷，錯誤要到**執行期**、而且是在一個看起來與憑證無關的操作（`compose build`）裡才出現。修一個環境時要同時找這兩類，否則會修到第三層才通。★ 偵測法：`for f in <dir>/*; do [ -e \"$f\" ] || echo DANGLE \"$f\"; done` —— **`[ -e ]` 對斷掉的連結是 false**，這正是它適合當這一類的偵測器（而 `ls` 用 lstat，照樣把斷連結列出來，所以「`ls` 看得到」不代表「它活著」）。", "because": "2026-09-17 E1 smoke。`/Applications/Docker.app` 已被移除（colima 是後來的 docker runtime），但它留下的痕跡還在兩個地方作用中。", "counterexample_observed": "`~/.docker/cli-plugins/` 底下 **16 個連結全部斷掉**（docker-agent／buildx／compose／desktop／scout／sandbox…），`/usr/local/bin/docker-compose`（root 擁有）也指向同一個消失的 app。症狀序列：`docker compose version` → `unknown command: docker compose`；只把連結指到 `brew install docker-compose` 裝好的 binary 之後 → `Docker Compose version 5.5.1`（**同一個 CLI、同一個 daemon，只換了那個連結**）；但 `docker compose build` 仍失敗於 credsStore（`auths` 是空的 ⇒ 移除該鍵不損失任何憑證）。三層都修完才 1/1 resolved。", "applies_to": ["agent_harness/README.md", "agent_harness/tb_loop/README.md", "agent_harness/PLAN_ENGINE_LOOP_2026-09-15.md"], "superseded_by": null}
{"type": "lesson", "lesson_id": "eng-mh-0054", "class": "measurement-hygiene", "rule": "**已發佈的「交付級」條件集（安靜 ≥1800 s ⇒ `COLD-STATE`，＋ 每個請求邊界讀到 `0/NOMINAL`）是必要條件，不是充分條件 —— 它可以用來否決一個數字，不可以拿來保證一個門檻。** 任何寫成「讀到 0 的那一臂，其 req1–req3 全部 ≥250」這種形式的述句，只要一次反例就必須降級成「該臂的讀數是 X」，而不是「該臂達標」。同理，「交付級」這個標籤要能背書一個具體門檻（≥250），就得先證明它對該門檻**有分離度**；只證明它對「冷/熱」有分離度是不夠的。**而且：條件欄位要在請求當下一起記錄**（背景 GPU/CPU 客戶端在無風扇 M4 上是共享熱/功耗包絡的），否則「未歸因」就無法排除；能裁決的直接變數是 `powermetrics` 的 GPU 時脈駐留（需 root），不是熱等級——熱等級只是它的粗代理。", "because": "2026-09-17 15:37，本線在一個安靜 **1940 s**（≥1800 ⇒ 臂報告自己判 `COLD-STATE`，並引用 whitepaper §11.14 說「其 prefill t/s 可以引用」）的窗口跑 `prefill250` 驗收臂，同 build、同 profile、同一份 2873-token prompt：prefill ＝ **242.42 / 227.27 / 249.06 t/s，三個全部低於 250**；發射前與 req1/req2/req3 **之前**全部 `0/NOMINAL`；`survived=yes`、無 crash report。外掛 2 Hz 序列（137 樣本）顯示 **15:37:15→15:38:24 連續 `0/NOMINAL`**，而 req1（15:37:5x 結束）與 req2（15:38:13 結束）都完全落在這一段之內 ⇒ 「熱」解釋不了那兩題。對照：同日 15:03 的臂標籤只是 `UNKNOWN-STATE`（無 state file）卻得 278.56 / 261.17 / 275.01 ⇒ **安靜秒數與熱等級都不預測這個 t/s**。兩臂的池 IO 幾乎相同（`pread_usec` 1501 s vs 1548 s、`us/job` 31733 vs 32533）⇒ 不是池路徑。⇒ 正確述句是「**未歸因**」，候選 (a) 背景 GPU 客戶端（發射時 load 1 分鐘值 2.93 vs 2.42，15:39 量到 6.18；`Freebuff Helper (GPU)`／`WorkBuddy Helper (GPU)`／`WebKit GPU`／`WindowServer` 都在跑）、(b) decode 側隨機性（今天各臂 acceptance 0.42857–1.00000），兩者都排不掉。", "counterexample_observed": "skill `cgc-prefill-thermal-delivery` §1 的分離度表寫 `0 Nominal → 6/6 全部 ≥250（253.42–271.64）`、§3 寫可交付的述句是「讀到 0 的那一臂，其 req1–req3 全部 ≥250」。本次樣本同時滿足該表的行與該述句的前提，卻得到 227–249。⇒ 該表是**該批樣本**的觀測，不是門檻的保證。", "superseded_by": null, "applies_to": ["Backup/run_req2_retest.sh", "scripts/check/prefill_certifiability.py", "Backup/run_thermal_gate.sh", "docs/PREFILL250_DECODE_MILESTONE_20260917_1510.html"]}
{"type": "lesson", "lesson_id": "eng-mh-0055", "class": "measurement-hygiene", "rule": "**`llama-bench` 的記錄裡，`tag`（臂名）不代表模型檔 —— 產出必須記下 `-m` 那一行。** `llama_bench_matrix.py` 的 json 只存 `tag`，而 tag 是 `PROFILE[:ENV=VAL]` 的組合，不是 `-m`。在 `ARMS` 表裡**只有 `prod25-stream-mtpoff` 設了 `CGC_SERVER_MTP`** ⇒ **`--arm prefill250` 與 `--arm prod25` 解析出來是同一個檔（`Nail-…-denseIQ4X.gguf`）**，只有顯式 `CGC_SERVER_MTP=0` 才換成 `Qwen3.6-35B-A3B-UD-IQ3_XXS.gguf`。所以「臂名不同 ⇒ 模型不同」是錯的，而「臂名相同 ⇒ 模型相同」也不可靠（profile 預設會變）。**判準：任何一次要跨時間／跨線比較的 llama-bench 產出，都必須把 matrix 印出的 `cmd` 行（含 `-m`）留檔**，或用 `--dry-run` 事後重放同一條臂去讀 `-m`。", "because": "2026-09-17 16:1x，本線要把 prefill 也統一 llama-bench 時，在 `MEMORY_PERF.md` 的 decode 列上看到標註「`prefill250+SPAC=1`、`-b512`、NOMINAL、**MTP=0 模型**」——若照這條讀，llama-bench 的 decode 基線（10.78／10.91）與 prefill 的 `pp2048` cell 就是**兩個不同的模型檔**，那一對數字根本不自洽。實際用 `prefill_certifiability.py --dry-run` 逐字讀 `-m` 行：`--arm prefill250` → `Nail-…denseIQ4X.gguf`、`--arm prod25` → **同一個檔**、`--arm 'prod25:CGC_SERVER_MTP=0'` → `Qwen3.6-35B-A3B-UD-IQ3_XXS.gguf`。⇒ 那條標註是錯的（或至少與現行 harness 不符），而 decode 的 10.79（09-15 `prod25-stream`）／10.89（今天）／10.78／10.91 **全部在同一支 denseIQ4X 上** ⇒ pp/tg 這一對其實是自洽的。⇒ 一個錯的模型標註，差一點讓我把一對本來可用的數字當成不可用。", "counterexample_observed": "修前：memory 寫「MTP=0 模型」；`--dry-run` 讀到的 `-m` 是 `Nail-Qwen3.6-35B-A3B-MTP-UD-IQ3_XXS-denseIQ4X.gguf`（兩個 arm 都是）。而 `--arms` 表裡 grep `CGC_SERVER_MTP` 只有 1 個命中（`prod25-stream-mtpoff`）。", "superseded_by": null, "applies_to": ["scripts/check/llama_bench_matrix.py", "scripts/check/prefill_certifiability.py", "scripts/check/decode_sweep.py", ".workbuddy/memory/MEMORY_PERF.md"]}
